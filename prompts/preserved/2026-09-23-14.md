HEAD: b2435efffc8cb6cac3c29f850c20581227ccbecf
Target subagent: coder
Type: build
Preserved: 2026-09-23T03:31:27Z by .claude/hooks/dispatch_guard.py
--- verbatim prompt follows ---
# Dispatch: extract Claude-kit v0.1 from Forager phase 1

**Type:** build
**Written:** 2026-09-23, by the planner, from chat, against the extraction pulse read 01:29 to 01:33Z at Forager main b2435ef.

## Role

You are the coder. You make no design decisions beyond those listed under "Decisions this dispatch leaves to you". An ambiguity or gap is a stop-and-ask. Validate this prompt structurally before acting and return any missing section by name.

## Base and state

- Target: `slayer8366/Claude-kit`. Private, empty, no default branch. Created by the owner at 2026-09-23T01:28:08Z.
- Source: Forager main at b2435ef, the 32 phase 1 files classified in the pulse. Read them with `git show` at that hash, never from a working tree.
- Nothing in Forager, forager-app or forager-forecast changes in this dispatch.
- Bootstrap: the kit repo has no hooks, so this run is unguarded, like phase 1's first commit. Work in a fresh clone of Claude-kit, not in any Forager worktree. Record the bootstrap in the kit's first record entry.

## Scope boundary

A faithful port, made generic. Same behaviour as b2435ef, with Forager's specifics moved into config. **No new guards and no fixes to known gaps.** Those are separate dispatches on the kit afterwards, each with its own evidence. If porting exposes a bug, record it and stop on it; don't fix it here.

## Closed decisions (owner, 2026-09-22 and 23)

- The kit repo is private. The privacy covers its workshop only: records, drafts, deliberations, unreleased versions. Released files are vendored into adopters that stay public, so everything released must be fit to publish.
- A release is a tag, and tagging is the owner's action. The owner reads the release diff first. You do not tag.
- Adopters vendor a pinned tag, with a lock file of hashes. Their CI and the drift checker never reach the kit repo.
- Decisions A to F and the backup rulings from Forager's `docs/process/accountability-design.md` at b2435ef are the design the kit implements. Cite them by their meaning ("builds and device runs need approval"), not by Forager's letters.

## Build, one commit each

Each step keeps the 49 hook tests and both checkers' self-tests passing. For every value moved into config, write a test that fails first against the hard-coded version, for example by setting a different package name and seeing the old code ignore it. Paste the failure.

### 1. Skeleton and record

- The layout:
  - `.claude/hooks/`
  - `.claude/agents/`
  - `.claude/settings.json`
  - `checkers/`
  - `templates/`
  - `install.py`
  - `check_kit.py`
  - `README.md`
  - `RECORD.md` (the kit's own)
- The first record entry is the intent for this dispatch. It records the bootstrap, and cites b2435ef and the pulse as its sources.
- Save this dispatch verbatim under `prompts/preserved/`, with a dispatch-note claiming it.

### 2. Config

- One adopter config file, `.claude/kit.json`. JSON, so no parser beyond the standard library is needed on Python 3.8.
- Keys, one for each hard-coded value in the pulse's Question 1 table:
  - Android package name, or null to switch the device guards off;
  - protected branches;
  - dispatchable agents, and the type-to-agent map;
  - required dispatch sections, per type;
  - prompt store path;
  - checker location (default: the repository root, as in Forager today);
  - environment-variable prefix for the guard overrides (default `KIT_GUARD_`).
- A missing config, or an invalid one, makes every hook block with a message naming the problem. The kit never fails open on its own config.

### 3. Hooks, made generic

- Replace every hard-coded value with a config lookup.
- Remove every Forager and incident reference from docstrings, comments and messages: the pulse's Question 6 list.
- Guard messages name the rule they enforce, never a decision letter.
- Incident history moves to the kit's `RECORD.md` as provenance, citing Forager's record by path.
- Drop `Grep` and `Glob` from the planner allowlist. They don't exist on 2.1.280 (pulse, unrequested findings). Record that the design listed them in error.

### 4. Tests, made generic

- No real identifiers, including in fixtures:
  - obviously fake serials (`TESTSERIAL01`);
  - a fake package (`com.example.kittest`);
  - no owner name;
  - no Resend or other real service names, where a placeholder `mcp__example__send` proves the same point.
- The device tests keep their fakes through the renamed environment-variable prefix.

### 5. Agents

`coder.md` and `pulse.md` with no project name in them. The record and store paths come from config, or from wording that holds for any adopter.

### 6. Checkers

- `check_record.py` and `check_prompts.py` as on b2435ef, with the dispatch-note logic.
- The attribution comments cite the kit's record, not Forager's (pulse Question 3).
- The upstream diff against EGD 35e39d1 kept in the README.

### 7. Install and drift

- `install.py --target <path> --tag <tag>` copies the release files into an adopter.
- It writes `.claude/kit.lock`: the tag, plus the SHA-256 of every vendored file.
- It never overwrites adopter-owned files: `kit.json`, `RECORD.md`, `CLAUDE.md`, `prompts/`. The first install writes `kit.json` from `templates/`.
- `check_kit.py` fails when any vendored file's hash differs from the lock, and names each file that differs.
- Tests, in a temporary git repo:
  - install, then check: passes;
  - edit one vendored file: check fails, naming it;
  - edit `kit.json`: check still passes.

### 8. Release gate

- `release_check.py` scans the release file set against a denylist of patterns: owner paths, adopter package names, account names, serial-like strings.
- The denylist lives in the private workshop at `workshop/denylist.txt` and is not in the release set, since the denylist itself names what must not be published.
- The release file set is defined in one place, and `install.py` reads it from there.
- Test: plant a denylisted string in a release file, and the check fails, naming the file and line.

### 9. CI

- GitHub Actions on the kit repo runs the hook tests, the checker self-tests, the install and drift tests and the release check.
- Python 3.8 and the current release.
- If 3.8 fails, do not fix it for 3.8. Raise the documented minimum to the lowest version that passes, and record that.

## Decisions this dispatch leaves to you

Declare each in the report under "Decisions I made":

- File naming inside `templates/` and `workshop/`.
- JSON key names.
- The exact denylist patterns, as long as every identifier type in pulse Question 5 is covered.

## Prediction

The planner's, to be graded:

1. The 49 hook tests port with no logic change, only fixture and config changes.
2. At least one Forager value is hard-coded somewhere the pulse missed, and it turns up when a test sets a different value.
3. Python 3.8 passes.

Write your own prediction into the intent entry before step 2.

## Finish line and abort conditions

**Finish:**

- Steps 1 to 9 are committed and pushed on a branch.
- CI is green.
- A PR is open against `main`.
- The release check passes on the release set.
- A terminal entry closes the intent, and both checkers pass after it.
- No tag.

**Abort:**

- Any behaviour change that the port needs in order to pass a test. Stop and report.
- The release check flags something that cannot be made generic without a design decision.
- Two failed fixes on one symptom. After the second miss, data only.

## Checks

- A diff of behaviour: run the phase 1 hook tests from b2435ef against the ported hooks, with a `kit.json` that reproduces Forager's values. They must pass unchanged, apart from fixture identifiers. This is the proof that the port is faithful.
- Every new test failing-first, with its message pasted.
- Sabotage for `check_kit.py` and `release_check.py`: disable the comparison, and the tests must fail.

## Out of scope

- New guards: `gh api` merges, agent-launched `claude` sessions (including `--bare` and `--settings '{"disableAllHooks": true}'`), egress.
- Changing any adopter repo, migrating Forager to the kit, and anything in forager-app or forager-forecast.
- Tagging a release.
- The autopilot addendum and the phase 2 items. They may be copied into the kit's `docs/` as reference, stripped of Forager references, and built from in no part.

## Device items

None.

## Report

Completion report in the kit's `docs/audits/` with its record entry, plus the PR description:

- the behaviour-diff result;
- the failing-first and sabotage messages;
- the release-check output;
- the CI links;
- **Decisions I made;**
- **Flags outside scope.**

## Addendum 1: owner rulings after this dispatch was written (2026-09-23), relayed by the planner

These are the owner's closed decisions. Where one contradicts the text above, it wins. Record every one of them in the kit intent entry's closed decisions (the owner's ruling 4 requires it).

1. **Execute this dispatch.** `main` now exists on `slayer8366/Claude-kit`: the owner created it (with a README; head `ff54f06` as read by the planner, so verify it). Branch from it. The "empty, no default branch" line in Base and state is superseded. The first record entry notes that `main` was created by the owner.
2. **The checkers stay at the repository root.** Drop `checkers/` from the layout in step 1. No path logic changes in the checkers. The checker-location config default stays "repository root". The dispatch contradicted itself here (a planner error); this ruling resolves it.
3. **Where to work.** Reuse the existing clone at `~/Zynergy/Claude-kit`. It was cloned by an earlier session while the repo was empty and holds no work. Confirm it is clean and has no local commits, fetch `main`, and branch `kit-v0.1` from it. That satisfies "a fresh clone of Claude-kit, not in any Forager worktree". Don't delete anything in it.
4. **Forager's record steps are deferred.** The dispatch hook preserves this dispatch in the Forager checkout it was sent from, as an untracked file under `prompts/preserved/`. The earlier merge dispatch's prompt, `2026-09-23-01.md`, sits there the same way. Your standing rule to sweep unclaimed prompts first does **not** apply to Forager in this dispatch: nothing in Forager changes, and both prompts wait for the next build dispatch on Forager. Your sweep, intent and terminal entries all happen in the kit's own `RECORD.md`, starting from the verbatim copy of this dispatch you save there in step 1.
5. **Known conflict, stated in advance so it is not a stop.** Step 3 drops `Grep` and `Glob` from the planner allowlist, while the behaviour-diff Check requires b2435ef's tests to pass unchanged. b2435ef's `test_allowlisted_tools_pass` expects both to pass, so those two subtests will fail the behaviour diff. The owner's dispatch mandates both. Report those two subtests as the expected, attributable difference, and treat any other behaviour-diff failure as the abort condition above.

## Addendum 2: owner ruling on the first run's stop (2026-09-23), verbatim, relayed by the planner

A first run of this dispatch (preserved in the Forager checkout as `prompts/preserved/2026-09-23-02.md`) stopped before step 1, with nothing built, committed or pushed, and raised the questions this ruling answers. This dispatch supersedes it. Where this ruling and anything above disagree, the ruling wins. Record it verbatim in the kit intent entry's closed decisions (its point 5 requires that).

---
Operator ruling, 2026-09-23 (kit v0.1, before step 1).

1. Option A. The intent entry's Dispatch-file claims the saved
   dispatch; its terminal may too. No dispatch-note for an
   executed build. (Planner error in step 1's wording.)
2. Commit the kit repo's own .claude/kit.json: package null,
   protected branch main, agents coder and pulse, Forager's
   section lists, checkers at root, prefix KIT_GUARD_. It is not
   in the release set.
3. Drop the prompt-store key. The store stays prompts/preserved
   in the hook and the checkers alike. (Planner error in the key
   list.)
4. Proposals 3-8 approved, except:
   (5) kit.json lives at .claude/kit.json, outside the vendored
       set, as dispatched.
   (6) The installer never overwrites an existing adopter
       settings.json. If one exists and differs from the kit's,
       stop and report. No merge, no replacement. Add a test.
   Commit the extraction pulse answer into workshop/ as the
   source the record cites.
5. Record this ruling in the intent entry's closed decisions.

Tally: two planner errors (step 1 claim wording, prompt-store
key). Prediction 2 confirmed, on the dispatch.
---

**What "Option A" and "Proposals 3-8" refer to** (planner's summary of the first run's report, which you don't have):

- **Option A:** the intent entry claims the saved dispatch through `Dispatch-file`, and its terminal may too, because `check_prompts.py:166` at b2435ef allows a shared claim only between an intent and its terminal. The alternative was a dispatch-note, which has no truthful Outcome for an executed build.
- **Proposal 3:** config keys are only the ones the dispatch lists (minus the prompt store, per point 3). The planner allowlist, the pulse tools, the pulse's adb reads and the dispatch tool names (`Agent`, `Task`) stay in code.
- **Proposal 4:** `Grep` and `Glob` are dropped only from the planner allowlist. They remain in role_guard's pulse allowlist (`role_guard.py:31`) and in both agents' `tools:` lines, and are flagged there.
- **Proposal 5 (point 4(5) amends it):**
  - Hooks read `.claude/kit.json`, the file beside the hooks directory, not `CLAUDE_PROJECT_DIR` or the payload's repository.
  - Unknown keys make the config invalid.
  - The checker location and the environment-variable prefix are optional, with the stated defaults; every other key is required.
  - A null package turns off all of `device_guard`.
  - The template ships a null package.
- **Proposal 6 (point 4(6) amends it):**
  - Vendored and locked: the hooks, the hook tests, the agents, `settings.json`, both checkers and `check_kit.py`.
  - `templates/kit.json` is written as `.claude/kit.json` only if it is absent.
  - Not released: `install.py`, `release_check.py`, `README.md`, `workshop/`, and the install and release tests.
  - The installer never overwrites an existing adopter `settings.json`; see point 4(6).
- **Proposal 7:** the install and drift tests tag only a throwaway temporary copy of the kit repo, never the kit repo itself. That is not tagging a release.
- **Proposal 8:** the upstream checker docstrings naming `OSCam` and `~/imx` (already public in upstream EGD at 35e39d1) stay as they are, and are not denylisted.

**Where things are:**

- **The extraction pulse** is not committed anywhere yet. Its only copy is the planner's chat message at 2026-09-23T01:34:40Z in the session log `~/.claude/projects/-home-zynergy-labs-Zynergy-Forager--claude-worktrees-bridge-cse-019uZR3mJKzHkDaCv5fGnsuk/1b897011-9b08-5256-851d-e0b3c0141e2e.jsonl`. The first run extracted it to `/tmp/kit_pulse.txt`. Verify that file against the log, then commit it into `workshop/` (point 4) and cite that path in the record.
- **The first run's scratch** is `/tmp/kitsrc`, an export of b2435ef, and `/tmp/egd`, the upstream checkers. You may use them after verifying them against their sources. Don't delete them.
- **Findings already made:**
  - `~/Zynergy/Claude-kit` is clean, with no local commits, and `origin/main` is `ff54f06` (one README). The `kit-v0.1` branch is not created.
  - Forager `origin/main` has moved to `624173b` (PR #115). The source stays pinned at b2435ef.
  - The prompt-store gap: `check_prompts.py:62,64` hard-codes `prompts` and `preserved`. That is the finding point 3 answers, and the one that confirmed prediction 2.
  - Hosted runners may not offer Python 3.8 on `ubuntu-24.04`. Check it, and record the runner you use.
