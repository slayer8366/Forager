HEAD: 188fa3982375db1124e1e11a0035cbd0dbb92646
Target subagent: coder
Type: device
Preserved: 2026-09-26T17:08:17Z by .claude/hooks/dispatch_guard.py
--- verbatim prompt follows ---
**Type:** device

# Role

You are the coder. You finish the landscape-capture dispatch, whose coder was stopped by the owner before it wrote its terminal. Your work has three parts:

1. Confirm the S22 Ultra's settings are back to where they started, and restore any that are not.
2. Close intent `2026-09-26-88`.
3. Merge PR #125 into `pre-main`.

You make no decision this dispatch has not made. The owner may be away. If a step needs a decision, record it and stop only on the abort conditions below.

# Base and state

Verify each item before relying on it. A mismatch is a stop only where the abort list says so.

**Branches.** The planner read these with `git`/`gh` just now:
- `origin/pre-main` is `188fa3982375db1124e1e11a0035cbd0dbb92646`.
- `origin/landscape-capture` is `00506680da268fafef2f15a89d446e5b223a7f63`. Its commits, newest first:
  - `0050668`: capture record and README row
  - `e70e932`: non-location assets
  - `688032c`: intent `2026-09-26-88`, with its store copy `prompts/preserved/2026-09-26-31.md`
  - `a19b7c1`: sweep, merge entry `-87` for #124
- PR #125 (`landscape-capture` into `pre-main`) is OPEN, and CI is green on `0050668` (run 36239627804).

**The previous coder's worktree.** `~/Zynergy/forager-wt/landscape-capture`, left as it was.
- Unverified: whether it holds uncommitted or untracked files. Report what `git status --short` shows. Do not discard anything. If you find uncommitted work, stop and report it.

**The capture evidence.** The capture directory `~/Zynergy/forager-landscape-capture/` holds `MANIFEST.md`.
- Its last rows show the camera opened at rotations 3, 1 and 0 with "Lock camera to portrait" ON, the last at 11:36:13Z, with `user_rotation=0` recorded.
- The manifest does not show whether the lock was turned back off, or whether the other settings were restored.

**Settings before the capture**, from its dispatch (`prompts/preserved/2026-09-26-31.md`), which the capture record may also restate:
- auto-rotate (`accelerometer_rotation`) = 0
- `user_rotation` = 0
- "Lock camera to portrait" = off
- "Automatically Save Location to Photos" = on, stored explicitly

Check the record's own "before" values against these, and use the record's values if they differ.

**The phone.** Only the S22 Ultra, serial `R5CT321008R`. Use `-s R5CT321008R` or `ANDROID_SERIAL` on every call, and never touch the owner's S26.
- Installed build: `1.0.899+g429edb96`, DEBUGGABLE.
- The previous runs had `input`, `uiautomator dump`, `run-as` and `screencap` available to the coder role.

**Record.** Your IDs are `2026-09-26-89` to `-99`.
- Intent `-88` is open.
- The planner worktree `…/bridge-cse_01BcqShzosraMo4pUXkqaqRp` is at `188fa39`. It holds this dispatch's store copy, untracked. Find it by its body.

**Next backup.** `2026-09-26-09`, per the capture dispatch. Confirm it is still free.

**Nothing else running.** No other build is running or will merge into `pre-main` while you run.

# Scope boundary

1. **Worktree.** Use the existing worktree `~/Zynergy/forager-wt/landscape-capture` on branch `landscape-capture`.
   - Confirm it is at `origin/landscape-capture` and clean.
   - Move this dispatch's store copy into it with `mv`, recording path, size and sha256.
2. **Continuation.** Commit a `continuation` entry (coder.md item 7) that continues `2026-09-26-88`, together with the store copy.
   - Dispatch-file: your copy.
   - Reason: "the dispatch's coder was stopped by the owner before its terminal; this finishes steps 7 to 9 and confirms the phone's settings."
   - Changes: none to the scope, predictions or finish line.
   - Push.
3. **Phone settings.**
   - Read `settings get system accelerometer_rotation` and `user_rotation`.
   - Read "Lock camera to portrait" and "Automatically Save Location to Photos" from the app's Settings screen, via a `uiautomator dump`. You may also read the DataStore file via `run-as` as extra evidence.
   - Restore any value that differs from its before value, then read it again.
   - Leave the app on its main screen in portrait.
   - Put before, after and restore values in the terminal.
4. **Terminal.** Close `-88`.
   - Outcome: `completed` if every capture step was done or recorded as not run, and every setting is confirmed restored. Otherwise `partial` is not allowed here; write `completed` or `abandoned` and explain.
   - The terminal cites the backup of step 6.
   - Push, and wait for CI to go green on the final commit.
5. **Checkers.** Run both before each commit that touches `RECORD.md`, and quote the final PASS lines.
6. **Merge.** Run each of these as its own Bash call, never chained:
   - `git fetch origin`
   - confirm `origin/pre-main` is `188fa39`
   - write backup `2026-09-26-09`
   - `gh pr merge 125 --merge`
   - `gh pr view 125 --json state,mergeCommit`

   If history_guard refuses the merge only because of its form, retry once in the bare form.
7. **Update checkouts.** Run the merged `update_worktree.py` on the main checkout and on the planner worktree: a dry run first, then `--apply`. Report every output.

**Files.** `RECORD.md` and your store copy only. Everything else is already committed.

**Phone.** Settings reads and restores, `uiautomator dump`, `run-as` reads, and taps to reach Settings. Never install, uninstall, clear data, or create or edit app data.

# Closed decisions

The owner, in the planner session, 2026-09-26:
- "Merge 125 when ready"
- "Let's go with Option 1". Option 1 was offered as: "Close it out, then build. A new coder records the stopped dispatch, checks and restores the phone settings, writes the terminal, merges #125, and then the design document follows."

The planner's call: this is a continuation of `-88`, not a new intent, because its scope and finish line are unchanged.

Write record entries with Write or Edit, never with a shell heredoc.

# Prediction

Planner, outcome:
1. The worktree is clean at `0050668`.
2. `accelerometer_rotation` reads 0 and `user_rotation` reads 0 (manifest line for 11:36:13Z). The camera lock may still be ON, since the manifest's last camera rows show it ON. If so, it is restored to off.
3. CI is green after the continuation and terminal commits. The merge goes through, and both checkouts fast-forward.

Coder: put your mechanism prediction in the continuation.

# Finish line and abort conditions

**Finish line.**
- The continuation and terminal are pushed.
- Every setting is confirmed at its before value.
- PR #125 is merged into `pre-main` with backup `2026-09-26-09`.
- Both checkers pass.
- Both checkouts are updated.

**Abort, stop and report, on any of these:**
- uncommitted work in the worktree
- a device other than `R5CT321008R` would be touched
- a setting cannot be restored
- an ID collision
- any guard or classifier refusal on a repository step, apart from the one form-only merge retry
- CI red
- two failed attempts at one step

# Checks

- The settings table: each setting's before value (from the capture dispatch or record), its value as found, any restore, and its final value.
- Checker PASS lines, the CI run id, the backup folder with its `merge.json` and `INDEX.md` line, and the merge commit with its parents.
- The `update_worktree.py` outputs.

# Out of scope

- Any new capture.
- Any design.
- App changes.
- The S26.
- Rebase, amend, force-push, delete.

# Device items

Settings verification only, as in step 3. There are no pass/fail app checks.

# Merge

authorised: PR #125 (`landscape-capture`) into `pre-main` only, per coder.md item 10(a). Write the backup first and run the merge command bare. Nothing into `main`.

End the hand-back with "Decisions I made" and "Flags outside scope".