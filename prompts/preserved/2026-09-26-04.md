HEAD: a3d7417f6f142551d8b355bfeb450ae232bc17bf
Target subagent: coder
Type: build
Preserved: 2026-09-26T02:37:16Z by .claude/hooks/dispatch_guard.py
--- verbatim prompt follows ---
**Type:** build

# Role

You are the coder. This dispatch has two jobs:
- Merge current `main` into the three stacked camera-strip branches, so PRs #111, #112 and #113 stop conflicting.
- Record that work.

No app code is written. You make no decision this dispatch has not made; a gap is a stop-and-ask.

This dispatch replaces `preserved/2026-09-26-03.md`. That coder stopped correctly, on a wrong premise about this worktree. This version corrects it. This session follows the Claude-kit practice at `~/Zynergy/Claude-kit` `48c391d`, `.claude/agents/coder.md`, where Forager's tooling accepts it. See Closed decisions item E.

# Base and state

Verify each of these against the remote first. A mismatch is a stop, reported before anything is written.

- **`origin/main`** is at `af12a69603ab38295099ef27f0b3114c2a9ccd74`.
- **The stack.** Every PR targets `main`, and each branch contains the one below it. Heads are unchanged as of the planner's last read:
  - #111 `strip-housekeeping` at `1b82b15f30622b43b9a22b8456a4e95c42b37438`
  - #112 `strip-torch` at `1f196042e9146b3d2d77cb33886d0fb0accaf416`
  - #113 `strip-grid-level` at `6bcbe4c68d144e8cab05cacce383708c02e986f9`
- **This worktree.** `~/Zynergy/Forager/.claude/worktrees/bridge-cse_013QR4ELV3wyYrUUyVCEmDwt`, on branch `worktree-bridge-cse_013QR4ELV3wyYrUUyVCEmDwt`.
  - It is at `a3d7417f6f142551d8b355bfeb450ae232bc17bf`, one commit past `af12a69`.
  - It is on the remote at that same commit.
  - `a3d7417` is a sweep made by the coder of `2026-09-26-02`. It committed `prompts/preserved/2026-09-26-01.md` and dispatch-note `2026-09-26-01`: Type pulse, Outcome answered.
- **Untracked store files:**
  - `2026-09-26-02.md` is the first build dispatch: these merges plus three strip chips.
  - `2026-09-26-03.md` is the merge-only dispatch that stopped at its premise check and wrote nothing.
  - This dispatch's own copy, which the hook will save as the next number.
- **`strip-grid-level` is checked out** in the other worktree `bridge-cse_016ud6iSpE7PzdbwnmhuLqZe`, so `git worktree add` on that branch fails.
- **The `-02` coder may still be alive and working in this same worktree.** The planner has had no completion notice from it. Its last visible act is `a3d7417` at 2026-09-26T02:30:48Z. The owner, told this, said: "No need to stop, keep going."
- **The expected conflict.** The only path both sides changed since the merge base `89f53a4` is expected to be `docs/audits/README.md`: rows appended by both sides after the shared 2026-09-19 "device check v4 run" row. This is inferred from diffs by the planner's pulse and confirmed by nothing yet.

# Scope boundary

- **Merges.** Each is done in a new **detached** worktree of this clone at `~/Zynergy/forager-strip-merge/<branch>`, then pushed with `git push origin HEAD:<branch>`, in this order:
  1. `origin/main` into `strip-housekeeping`.
  2. The new `strip-housekeeping` into `strip-torch`.
  3. The new `strip-torch` into `strip-grid-level`.
- **Merge rules.** Use `git merge` with a merge commit. Never rebase, amend or force-push. The only permitted hand edit is resolving `docs/audits/README.md` as stated under Checks.
- **Record work** is on this worktree's branch only: `RECORD.md` and the store files `-02`, `-03` and this dispatch's copy. It is pushed, and a record-only PR against `main` is opened.
- **Not touched:**
  - the other worktrees, including `bridge-cse_016ud6iSpE7PzdbwnmhuLqZe`
  - `main`
  - app code
  - hooks
  - `CLAUDE.md`
  - the phone
  - Claude-kit
  - commit `a3d7417` and dispatch-note `2026-09-26-01`, which are already done and not re-written

# Closed decisions

**A. The owner's instructions**, in chat, 2026-09-26: "Go ahead and have a coder perform the merges" and, on being told the `-02` coder may still be running, "No need to stop, keep going." The three chips from `-02` are not built here.

**B. README row order.**
- Keep every row from both sides.
- Order rows by their date column. On a shared date, main's rows come first, then the branch's, each side in its existing order.
- A row that is byte-identical on both sides appears once.
- No row's text is edited.

**C. Record `-02` and `-03`**, following the precedent of RECORD.md `2026-09-22-01` and `-02`. For each, write an intent after the fact claiming its store file, with fields taken from that dispatch, and a terminal closing it with Outcome `superseded`, pointing at this dispatch's intent.
- **`-02`'s Notes:**
  - Its coder made and pushed `a3d7417` (the sweep), and nothing else was observed.
  - Its hand-back never reached the planner.
  - Include what the remote and `git worktree list` show at the time you write the entry.
- **`-03`'s Notes:**
  - It stopped at its premise check and wrote nothing.
  - The premise was the planner's error. It stated the branch was not on the remote, and that `-02` had left no commit.

**D. The `-01` pulse.** It is already recorded by `a3d7417`. Write nothing more for it.

**E. Claude-kit practice as applied.**
- **Adopted:**
  - The sweep (C) first, committed and pushed.
  - Then this dispatch's store copy and its intent in one commit, then the work.
  - Two misses, then data.
  - Scope is a wall.
  - Cite or qualify.
  - Push before you tidy.
  - Agents delete nothing: the merge worktrees stay and are listed.
  - The hand-back ends with "Decisions I made" and "Flags outside scope".
- **Not adopted:** the kit's `merge`, `continuation` and `revert` kinds and its `stopped` and `merged` outcomes. Forager's `check_record.py` does not accept them.

**F. The concurrency guard, because the `-02` coder may be alive.**
- **Before each merge, before each push and before each commit to `RECORD.md`:** re-read the three strip heads and this branch's head with `git ls-remote`, and this worktree's `HEAD` and `git status --short`.
- **Stop and report on anything you did not cause:** a head moved, a new commit on this branch, a changed tracked file, or a new untracked store file.
- **A rejected push is a stop, never a retry with force.**

# Prediction

Planner, outcome:
1. **Merge 1** conflicts only in `docs/audits/README.md`.
2. **Merges 2 and 3** conflict at most in that file.
3. **The pushes.** After them, GitHub reports #111, #112 and #113 MERGEABLE, and CI "Build, test, publish APK" passes on each new head.
4. **Each branch's diff** from its old head to its new head lists only paths `main` changed since `89f53a4`, and none under `app/`.

Coder: write your mechanism prediction in the intent before merging.

# Finish line and abort conditions

**Finish line.** All of these hold:
- The sweep (C) is pushed.
- The intent is pushed.
- The three merges are pushed.
- Each PR is MERGEABLE, with CI green on its new head. Wait for the runs, or report any still pending with its run ID.
- A terminal closes this intent. Its Deviations name the planner's wrong premise in `-03`.
- Both checkers pass.
- The record branch is pushed and its PR is open.

**Abort, and stop and report, on any of these:**
- A premise above is wrong.
- F's guard fires.
- A conflict appears in any path other than `docs/audits/README.md`.
- A resolution would need a row edited or dropped.
- CI fails on a new head. Report it; do not fix it.
- Any guard blocks a step. Quote its text.
- Two failed attempts at one step.

# Checks

- **Row counts.** For each merge, count index rows (lines starting `| 20`) in the base, in each side and in the result. The result equals the base plus each side's added rows, minus the rows identical on both sides. Show the arithmetic.
- **Markers.** `git diff --check` shows no conflict markers after each resolution.
- **Paths.** Show `git diff --name-only <old head> <new head>` per branch, and confirm no path is under `app/`.
- **Pushes.** Read `git ls-remote` after each push.
- **Checkers.** Run `python3 check_record.py` and `python3 check_prompts.py` before every commit touching `RECORD.md`, and quote the final result of each.

# Out of scope

- Building the flash, timer or location chips.
- Merging any PR into `main`.
- Updating the local `strip-grid-level` in the other worktree. Report that it is now behind its remote.
- Tidying, deleting or re-pointing any worktree or branch.

# Device items

None. No app code changes.

# Merge

Not authorised. No PR is merged. The merges above are `main` merged into feature branches, not PR merges. Forager's history_guard blocks `gh pr merge` for every role (`.claude/hooks/history_guard.py:31`, per RECORD.md 2026-09-23-02). The owner merges #111, #112, #113 and the record PR.

End the hand-back with "Decisions I made" and "Flags outside scope".