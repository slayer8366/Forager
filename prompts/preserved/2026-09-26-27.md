HEAD: 60173da62c4877d907643e20e1a88467589eccb0
Target subagent: coder
Type: build
Preserved: 2026-09-26T07:28:39Z by .claude/hooks/dispatch_guard.py
--- verbatim prompt follows ---
**Type:** build

# Role

You are the coder. This dispatch moves the main checkout at `/home/zynergy-labs/Zynergy/Forager` onto `pre-main`, which is the kit's model (`Claude-kit@5ba1581:README.md:141`, "the main checkout sits on pre-main"). Before doing that it preserves the one piece of content that exists only there. It also writes the sweep for Part A's three merges. You make no decision this dispatch has not made; a gap is a stop-and-ask.

# Base and state

Verify each item before relying on it; a mismatch is a stop. The planner read all of these at about 07:30Z with read-only git.

**Branches and the planner worktree.**
- `origin/pre-main` is at `60173da62c4877d907643e20e1a88467589eccb0`, the merge of #113. Its first-parent chain since `39f6e74` is `2337047` (#111), `8b5414e` (#112), `60173da` (#113).
- `origin/main` is at `76905d4`.
- The planner worktree `/home/zynergy-labs/Zynergy/Forager/.claude/worktrees/bridge-cse_01BcqShzosraMo4pUXkqaqRp` is at `60173da` and clean, apart from this dispatch's own store copy. Find the copy by its body; do not assume its number.

**The record.**
- `pre-main`'s newest entries are `2026-09-26-50` to `-53`.
- The merges `2337047`, `8b5414e` and `60173da` have no `merge` entries.
- Their backups are `2026-09-26-03`, `-04` and `-05`, all written under intent `-52`, whose store copy is `prompts/preserved/2026-09-26-25.md`.

**The main checkout.** `git -C /home/zynergy-labs/Zynergy/Forager status --short --branch` gives `## claude/new-session-vto65i...origin/claude/new-session-vto65i [ahead 2, behind 2]` and ` M .gitignore`.
- The local HEAD is `e0b74af`, on top of `64b7e1d`.
- The remote branch is at `a787cd6`, on top of `934683d`.
- The pairs have identical titles and author/commit dates, 2026-09-20.
- `git diff e0b74af a787cd6` is exactly one deleted line in `docs/audits/README.md`: the row beginning `| 2026-09-19 | **Handoff, not a dispatch: PR #102 merged, with the check it asked for**`.
- That row appears nowhere on `origin/pre-main` (`git grep -c` returns nothing). It exists only in local commit `e0b74af`.
- The branch is PR #102's head, merged 2026-09-20.
- The uncommitted `.gitignore` change adds one line, `/signing/`, after `/signing.properties` at line 26. `origin/pre-main:.gitignore:26-27` already reads `/signing.properties` followed by `/signing/`.
- The main checkout has no `.claude/kit.json`, `.claude/hooks/` or `.claude/settings.json`.
- `.claude/worktrees/`, which holds every session worktree including the planner's, is under it and not tracked.
- `git stash list` is empty.
- Unverified: whether a local `pre-main` branch exists in the clone, and whether any ignored file in the main checkout sits at a path `pre-main` tracks.

**Remote refs.** No remote branch named `preserve/main-checkout-e0b74af` exists (planner `ls-remote` earlier today); re-check it.

**Nothing else is running.** No other build is running or will merge into `pre-main` while you run.

# Scope boundary

1. **Worktree and store copy.**
   - Create `git worktree add ~/Zynergy/forager-wt/main-checkout-fix -b main-checkout-fix origin/pre-main`.
   - Move your own store copy into it with `mv`, recording its path, size and sha256.
2. **Sweep.** Commit it first and push.
   - Write three `merge` entries, in first-parent order: #111 at `2337047`, #112 at `8b5414e`, #113 at `60173da`.
   - Take the fields from `gh pr view` and `git log`. Backups are `2026-09-26-03`, `-04` and `-05`. Merged-by is `coder preserved/2026-09-26-25.md`.
   - Carries is what each PR's diff added to `RECORD.md`. The planner expects `-50` to `-53` on #111 and `record only`/none new on #112 and #113; confirm from the diffs rather than trusting that.
   - Write no other entry unless coder.md item 1 finds one, and say which.
3. **Store copy and intent.** One commit, then push.
4. **Preserve `e0b74af`.** Run `git -C /home/zynergy-labs/Zynergy/Forager push origin e0b74af:refs/heads/preserve/main-checkout-e0b74af`.
   - Confirm with `git ls-remote` that the ref is there, at `e0b74af`.
   - Also record `64b7e1d` as its parent. Both local-only commits are preserved by this one ref.
   - Do not touch the remote `claude/new-session-vto65i`.
5. **Restore `.gitignore`.**
   - First show that the working-tree `.gitignore` equals `origin/pre-main:.gitignore` at the lines in question. Show `git -C <main> diff` and `git show origin/pre-main:.gitignore`, lines 24-28.
   - Then run `git -C <main> restore .gitignore`.
   - If the working-tree change contains anything other than that one `/signing/` line, stop.
6. **Switch the main checkout to `pre-main`.**
   - If no local `pre-main` exists, run `git -C <main> switch -c pre-main --track origin/pre-main`.
   - If one exists and is an ancestor of `origin/pre-main`, switch to it and fast-forward with `git merge --ff-only origin/pre-main`.
   - Any other state is a stop.
   - If git refuses the switch because a file would be overwritten, stop and report the paths. Do not move or delete anything.
   - After the switch:
     - Show `git -C <main> status --short --branch`. It should be clean, on `pre-main`, at `60173da` or later.
     - Show that `.claude/kit.json`, `.claude/hooks/` and `.claude/settings.json` now exist there.
     - Show that `git -C <main> worktree list` still lists every worktree, the planner's included.
7. **Terminal.** It cites the backup folder of step 9.
8. **PR.** Open a PR from `main-checkout-fix` into `pre-main`. CI must be green on its final commit.
9. **Merge.** Run each of these as its own Bash call, never chained:
   - `git fetch origin`;
   - confirm `origin/pre-main` is `60173da`;
   - write the backup to the next free `2026-09-26-NN`, expected `-06`;
   - `gh pr merge <N> --merge`;
   - `gh pr view <N> --json state,mergeCommit`.

   If history_guard refuses the merge for its form alone, run it again once in the bare form. Any other refusal is a stop.
10. **Update checkouts.** Run the merged `update_worktree.py` on the main checkout, then on the planner worktree. For each, run a dry run and then `--apply`, and report every output. If either dry run exits 1, report it and do not apply.

**Record IDs.** Use only `2026-09-26-70` to `-79`. Before each commit touching `RECORD.md`, and before the merge, confirm none of them is on `origin/pre-main`. Your own IDs arriving through your own merge are expected.

**Files.** Your branch changes only `RECORD.md` and your store copy. In the main checkout, the only changes are the `.gitignore` restore and the switch.

**Not touched.**
- `claude/new-session-vto65i`, local or remote. It stays where it is, and nothing is deleted.
- Every other worktree, including `bridge-cse_016ud6iSpE7PzdbwnmhuLqZe`.
- `main`.
- `.claude/` files, `ci.yml`, app code.
- GitHub settings.
- The phone.

# Closed decisions

The owner, in the planner session, 2026-09-26:
- "Let's fix the main checkout".
- The owner chose "A coder, queued after Part A". The planner offered it as follows: "push e0b74af to preserve/main-checkout-e0b74af, restore .gitignore, then switch to pre-main."
- The earlier ruling "merge into pre main without device check" covers this PR's merge into `pre-main`.

The planner decided the following; record them as the planner's calls:
- Preserve by pushing a new remote ref rather than keeping the local branch as the only copy. That follows CLAUDE.md, "Push before you tidy".
- Leave `claude/new-session-vto65i` as it is, with no reset and no delete. Whether the Handoff row belongs in the index is left open.
- Write merge commands bare. Two coders today had `gh pr merge` refused for chaining.

**Record entries.** Write them with Write or Edit, never with a shell heredoc.

# Prediction

Planner, outcome:
1. **The preserve push** succeeds, and `ls-remote` shows `preserve/main-checkout-e0b74af` at `e0b74af`.
2. **The `.gitignore` working-tree diff** is exactly `+/signing/` at line 27, and after the restore `git status` shows it clean.
3. **The switch** succeeds with no file conflict, because `.claude/worktrees/` is untracked on both sides. It leaves the main checkout clean on `pre-main` with the kit files present, and every worktree still listed.
4. **CI** is green on the PR. The merge goes through. The main checkout's `update_worktree.py` fast-forwards it to the new merge commit; this is inferred from item 3, since the checkout will be on `pre-main` at `60173da`.

Coder: write your mechanism prediction in the intent.

# Finish line and abort conditions

**Finish line.**
- Steps 1 to 10 are done.
- `preserve/main-checkout-e0b74af` is on the remote.
- The main checkout is clean, on `pre-main`, at `origin/pre-main`.
- The planner worktree is at `origin/pre-main`.
- The PR is merged with a backup.
- Both checkers PASS.

**Deferral is not allowed.**

**Abort, and stop and report, on any of these:**
- A wrong premise.
- The `.gitignore` change is more than the one line.
- The preserve push fails.
- The switch is refused.
- A local `pre-main` exists in an unexpected state.
- An ID collision.
- Any guard or classifier refusal, apart from the one form-only merge retry. Quote it, and do not route around it.
- CI is red.
- `update_worktree.py` exits 1 on the dry run.
- Two failed attempts at one step.

# Checks

- **Before and after.** Main checkout `status --short --branch` before, after the restore, after the switch, and after the update.
- **The preserved content.** `git ls-remote origin refs/heads/preserve/main-checkout-e0b74af`, and `git grep -c "Handoff, not a dispatch: PR #102 merged" origin/preserve/main-checkout-e0b74af -- docs/audits/README.md` returning 1.
- **Kit files present** in the main checkout after the switch.
- **Worktree list** before and after, with the same count.
- **Checkers.** Both run before every `RECORD.md` commit; quote the final PASS lines.
- **CI, backup, merge.** The CI run id, the backup folder with its `merge.json` and `INDEX.md` line, and the merge commit and its parents.

# Out of scope

- Deciding the Handoff row's fate.
- Deleting or resetting `claude/new-session-vto65i`.
- The stale `strip-grid-level` ref in `bridge-cse_016ud6iSpE7PzdbwnmhuLqZe`.
- ci.yml's concurrency behaviour.
- Part B.
- Promotion to `main`.
- Rebase, amend, force-push, delete.

# Device items

None.

# Merge

authorised: this dispatch's PR `main-checkout-fix` into `pre-main` only, per coder.md item 10(a), with the backup first and the merge command run bare. Nothing into `main`.

End the hand-back with "Decisions I made" and "Flags outside scope".