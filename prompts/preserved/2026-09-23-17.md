HEAD: b2435efffc8cb6cac3c29f850c20581227ccbecf
Target subagent: coder
Type: build
Preserved: 2026-09-23T06:39:47Z by .claude/hooks/dispatch_guard.py
--- verbatim prompt follows ---
# Dispatch: let the pulse role hand back its report

**Type:** build
**Written:** 2026-09-23, by the planner in session bridge-cse_019uZR3mJKzHkDaCv5fGnsuk.

## Role

You are the coder. You make no design decisions. An ambiguity or a gap is a stop-and-ask. Validate this prompt structurally before acting and return any missing section by name.

## Base and state

These are premises. Verify each one before acting.

- Forager `main` is `8f4454c153e55091297a910a7b7d7bbf0d3e1a03`. PR #116, the record cleanup, is merged into it, merged by the owner at 2026-09-23T06:02:51Z.
- On `main`, `.claude/hooks/role_guard.py:31` is `PULSE_TOOLS = {"Read", "Grep", "Glob", "Bash"}`. The role guard denies the pulse role every tool not in that set.
- Subagents deliver their final report through a tool the harness calls `SubagentHandback`. That name is how this session's coder reports arrived. It is not yet confirmed as the tool name in a hook payload.
- **The reported symptom:** every pulse since the Phase 1 exercises has run and delivered nothing. Cases:
  - the forager-forecast session's D32 pulse;
  - the two "frozen session" pulses, `prompts/preserved/2026-09-23-14.md` and `-15.md` on `main` after #116, according to that session's cleanup report (verify the numbers);
  - the pulse claimed by record entry `2026-09-23-06`.
- **Not yet explained:** pulses did return reports in the Phase 1 live exercises at about 21:23–21:38Z on 2026-09-22.
- **This dispatch's own preserved prompt** is written by the dispatch hook into the checkout it was sent from, `/home/zynergy-labs/Zynergy/Forager/.claude/worktrees/bridge-cse_019uZR3mJKzHkDaCv5fGnsuk`, as an untracked file under `prompts/preserved/`. The dispatch hook's numbering makes it `2026-09-23-05.md` there. Verify that.
- **That checkout also holds four untracked originals,** `prompts/preserved/2026-09-23-01.md` to `-04.md`. They are superseded copies, already claimed on `main` under new names by #116. They must not be swept again.

## Scope boundary

- `.claude/hooks/role_guard.py`: add the hand-back tool to `PULSE_TOOLS` only.
- `.claude/hooks/tests/test_role_guard.py`: add tests.
- The record, `RECORD.md` and `prompts/preserved/`, on your branch.
- A PR against `main`.
- No other hook, agent, setting or checker changes. Nothing in any other repository, Claude-kit included.

## Closed decisions (owner, 2026-09-23, confirmed in chat: "Confirmed, send the pulse fix")

1. **Fix the pulse hand-back** in Forager by adding the hand-back tool to the pulse role's allowlist.
2. **Establish the tool name from evidence before editing.** Use the `tool_name` in a real hook payload, or in a denied pulse's session log under `~/.claude/projects/`. Quote it with its source. Don't guess. If the evidence names more than one tool, or the denial turns out not to be the cause, stop and report.
3. **Planner and coder stay as they are.** Only the pulse role changes. The planner allowlist doesn't gain the tool; add a test that it's still denied to the planner.
4. **Work in a new worktree cut from `origin/main`,** not in any checkout a running session uses. Hooks load mid-session, so editing a running session's checkout changes its guards live.
5. **Record.**
   - Your intent claims this dispatch's prompt, copied byte for byte from the checkout above to the next free number on your branch, with its SHA-256 matched. The original stays in place as a superseded copy, and your intent says so.
   - The four older originals in that checkout are already claimed and are not swept.
   - Close your intent with a terminal entry.
6. **Live check authorized.** You may start `claude -p` sessions in your new worktree to exercise the fix:
   - one planner session that dispatches one pulse, run before the fix and again after it;
   - nothing else.

   The hand-back must fail before the fix and return the pulse's report after it. Quote the hook message and the returned report.

## Prediction

The planner's:
1. The denied tool is `SubagentHandback`, and role_guard denies it to the pulse with "the pulse role may not use SubagentHandback".
2. The new test fails first against `main` with that deny.
3. Adding the tool makes it pass, and no other test moves.
4. The live pulse returns nothing before the fix and returns its report after it.

## Finish line and abort conditions

**Finish:**
- the fix and tests are committed and pushed on a branch;
- the full hook suite passes, with counts;
- the before and after live runs are quoted;
- your intent and terminal are written, and both checkers pass;
- a PR is open against `main`;
- no tag, no merge.

**Abort if:**
- `main` isn't `8f4454c`, or doesn't contain #116;
- the evidence doesn't show role_guard denying the hand-back tool;
- any existing test fails after the change;
- the sabotage run can't be shown to have run;
- two fixes miss on one symptom (then gather data only).

## Checks

- The evidence for the tool name, quoted with its file and line or payload.
- The new tests failing first against `main`, messages pasted.
- **Sabotage:** remove the tool from `PULSE_TOOLS` again, using a saved copy (never `git checkout`). The new pulse test must fail with a message specific to that edit. Restore from the copy and confirm the forward change is present.
- The four hook test files' counts after the fix.
- The live runs, before and after.
- `check_record.py` and `check_prompts.py` before every commit that touches `RECORD.md`, and after the last.

## Out of scope

- The same fix in Claude-kit. Record it as a flag; it goes to the kit's v0.1 fix list.
- The history_guard parse issue.
- Dispatch numbering.
- The six superseded originals in the two other checkouts.
- Any other role's allowlist.

## Device items

None.
