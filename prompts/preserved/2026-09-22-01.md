# Dispatch: accountability setup, phase 1

**Type:** build
**Written:** 2026-09-22, by the planner, from chat. Not yet stamped by a hook; no hook exists yet.

## Role

You are the coder. This is a bootstrap run: the role restrictions this dispatch builds do not exist yet, so you are running in an unrestricted Claude Code session. Act as the coder anyway. You make no design decisions. An ambiguity or a gap in this prompt is a stop-and-ask. Stopping is compliant behaviour; guessing is not.

Before acting, validate this prompt structurally. It must contain: Role, Base and state, Scope boundary, Closed decisions, Prediction, Finish line and abort conditions, Checks, Out of scope, Device items. Return any missing section by name. Your validation cannot detect a decision you were never told about, so do not report the prompt as validated beyond its structure.

## Base and state

- Repo: Forager. Branch `accountability-phase-1` from `origin/main`. Report the hash you branched from.
- The strip stack (#111, #112, #113) may still be open. Branch from main regardless. This work touches none of their files.
- Design source: `accountability-design-claude-code.md`, supplied with this dispatch. Commit it as `docs/process/accountability-design.md`, unmodified, in the first commit.
- Autopilot addendum: `accountability-autopilot.md`, supplied with this dispatch. Commit it as `docs/process/accountability-autopilot.md`, unmodified, in the same commit. It is phase 4 and a reference only. Build nothing from it.
- EGD source: https://github.com/slayer8366/E-GD-Philosophy, branch `Test-1` @ 35e39d1. Clone it read-only and cite file:line at that hash.
- The planner has not read Forager's `.claude/` directory or its CLAUDE.md beyond line 253. Read both before building. If `.claude/` already holds agents, settings or hooks, stop and report them before changing anything.

## Scope boundary

Phase 1 only:

1. The record store.
2. The planner's tool restrictions.
3. Subagent definitions for `coder` and `pulse`.
4. The dispatch hook.
5. The device guards.
6. The history guards.

Everything else in the design is out of scope; see Out of scope.

## Closed decisions (operator, 2026-09-22)

- **A.** The planner has read-only access to the repository. Allowed: Read, Grep, Glob; Bash limited to read-only git and gh; the dispatch tool. Denied: Edit, Write, and mutating Bash.
- **B.** Operator approval is required for dispatches of Type `build` and `device`. Type `pulse` dispatches run without approval.
- **C.** EGD's record store is vendored into Forager: `RECORD.md` plus `check_record.py`, and `check_prompts.py` for the prompt store. Existing `docs/audits/` records stay where they are and are not migrated.
- Gates are hooks and tool restrictions. They are not instructions to the agents.
- The Restricted Object rule stays an instruction at CLAUDE.md:253. No hook references it.

## Build

One commit per step. Each hook is built under EGD's rule for new construction:

1. Write a test that feeds the hook a crafted JSON payload on stdin. See the test fail against a stub, for the stated reason.
2. Implement the hook. See the test pass.
3. Sabotage the hook in the specific way it claims to detect. See the test fail. Revert.

Paste the failure messages into the report.

### Step 0. Verify the platform

This step writes nothing. Report each item against the installed Claude Code version (`claude --version`), citing docs or observed behaviour:

- The dispatch tool's name as hooks see it (`Task`, `Agent`, or other).
- Whether a deny rule in `.claude/settings.json` applies to subagents as well as the main session.
- Whether PreToolUse input identifies the calling agent (main session versus a named subagent).
- Whether a PreToolUse hook can return an "ask" permission decision.
- Where subagent definitions load from, and whether they need a session restart.

**Stop condition:** decision A may be impossible on this version. That happens if a main-session deny also denies the coder, and no field in the hook input identifies the caller. If so, stop and report the options. Do not pick one.

### Step 1. Record store

- Vendor `check_record.py` and `check_prompts.py` from EGD @ 35e39d1. Adapt only the paths and names they need to run in Forager. List every line changed from upstream.
- Create `RECORD.md` with a header in Forager's own words, following EGD's header rule: specifics live in the checker, not the header.
- The first entry is the intent for this dispatch. It carries:
  - this dispatch's prediction;
  - the finish line and abort conditions below;
  - the closed decisions A to C;
  - Outcome prediction: `not authored`.
- Save this dispatch verbatim as the first file under `prompts/`. This is a bootstrap exception: after step 4 the hook does this, not you. Record the exception in the intent entry.

### Step 2. Planner restrictions

Implement decision A by whichever mechanism step 0 found works:

- settings deny rules,
- a PreToolUse hook keyed on the calling agent,
- or both.

Mutating Bash is blocked by pattern. The patterns must cover at least: `git commit`, `git push`, `git checkout`, `git reset`, `git merge`, `rm`, `mv`, redirection into a file (`>` or `>>`), `sed -i`, `gradle` and `gradlew`, and `adb` except the read-only subcommands the pulse role uses.

### Step 3. Subagents

- `.claude/agents/coder.md`:
  - Tools: Read, Grep, Glob, Edit, Write, Bash. No dispatch tool.
  - The system prompt carries BOOTSTRAP's coder section in Forager's terms, and the "Decisions I made" reporting rule from EGD PROTOCOL rule 6.
- `.claude/agents/pulse.md`:
  - Tools: Read, Grep, Glob, and Bash limited to read-only git, gh and adb reads (`getprop`, `dumpsys`, and `screencap` under the foreground guard).
  - No Edit or Write.
- Write each description for routing: say when to use the agent, not what it is good at.

### Step 4. Dispatch hook (PreToolUse on the dispatch tool)

- Reads the prompt's `Type:` line. A missing or unknown type blocks.
- Checks the required sections for that type:
  - **build and device:** Role, Base and state, Scope boundary, Closed decisions, Prediction, Finish line and abort conditions, Checks, Out of scope, Device items.
  - **pulse:** Role, Base and state, Rules, Questions.
  - A missing section blocks, and the hook names the missing sections to the planner.
- On pass:
  - writes the prompt verbatim to `prompts/<date>-<seq>.md`, headed with the HEAD hash, the target subagent and the type;
  - then returns "ask" for build and device, allow for pulse (decision B).
- `check_prompts.py` validates the store after a write.

### Step 5. Device guards (PreToolUse on Bash)

- Block `connectedAndroidTest` unless `-Pandroid.injected.androidTest.leaveApksInstalledAfterRun=true` is present.
- Block `adb uninstall`, `pm uninstall` and `pm clear`.
- Block `adb install` unless the APK's signing certificate matches the installed package's. If the package isn't installed, allow.
- Before `adb shell input` or `screencap`, read the resumed activity. Block unless it belongs to Forager's package.

### Step 6. History guards (PreToolUse on Bash)

Block:

- `git push --force` and `--force-with-lease`;
- `git merge` while on main;
- `git push` to main;
- `gh pr merge`.

## Prediction

The coder writes its mechanism prediction into the intent entry before step 1's hooks exist. The planner's, stated here so it can be graded:

1. Decision A is achievable on the installed version, but only through a hook that identifies the caller, not through settings alone.
2. Pattern-matched Bash guards are bypassable. At least one of these gets past at least one guard:
   - `sh -c '…'`
   - a variable holding the command
   - `bash script.sh` containing it
   - `xargs`
   - a gradle task alias
3. Every hook passes its sabotage test.

Try all five bypass variants against every Bash guard in steps 2, 5 and 6, and record each result. A bypass is a finding, not a failure to hide. Do not patch a bypass beyond the dispatch. Report it for the planner.

## Finish line and abort conditions

**Finish:**

- Steps 0 to 6 are committed and pushed.
- Every hook has a failing-first test and a sabotage result.
- The bypass table is complete.
- The live exercises below are run.
- A terminal entry closes the intent.
- `check_record.py` and `check_prompts.py` pass on the tree as committed, run after the terminal entry is written.
- A PR is open against main.

**Abort:**

- The step 0 stop condition.
- Any guard that, when tested live, lets a real destructive command execute.
- Two failed fixes on one hook. After the second miss, data only: log the payloads the hook actually receives.

## Checks

- Unit tests per hook, using crafted payloads.
- EGD's rule: real use before verified. Each guard is exercised once in a live Claude Code session, using a command that is harmless if the guard fails:
  - `adb uninstall com.example.doesnotexist`
  - `./gradlew connectedAndroidTest --dry-run`
  - `git push --force --dry-run`
  - `adb shell input keyevent 0` with Forager not in the foreground (see Device items)
  - the planner session attempting `Write` to a scratch path
  - a dispatch missing one section
  - a complete pulse dispatch, which should run unasked
  - a complete build dispatch, which should prompt for approval
- Report each: blocked or not, and the message shown.

## Out of scope

- The intent gate on Edit, Write and mutating Bash (phase 2).
- The coder SubagentStop report gate (phase 2).
- The auditor (phase 3).
- Anything from the autopilot addendum (phase 4): the Stop hook, scope files, `DEVICE_QUEUE.md`, `check_queue.py`, the fixed fence. If a phase 1 choice would make phase 4 harder to build later, flag it; do not build ahead for it.
- Any app code.
- The strip stack.
- Migrating `docs/audits/`.
- Editing CLAUDE.md beyond one short section pointing to `docs/process/accountability-design.md` and `.claude/agents/`. Report its text.

## Device items

The phone is connected over USB and unlocked. The owner puts it on the home screen before the foreground live exercise and says so. The agent does not press Home: that is an input, and input is what the guard governs.

1. With the home screen showing, attempt `adb shell input keyevent 0`.
   - **Pass:** blocked, with the message naming the foreground package.
2. The owner opens Forager. Attempt the same command.
   - **Pass:** allowed. Keyevent 0 does nothing on screen.
3. Attempt `adb uninstall com.example.doesnotexist`.
   - **Pass:** blocked.
   - If it runs, it fails harmlessly on the missing package. Record it as a guard failure and stop.

## Report

A completion report under `docs/audits/` with its index row (keep every row on conflict), plus the PR description. Include:

- the step 0 answers;
- the upstream diff for vendored files;
- failing-first and sabotage messages;
- the bypass table;
- live exercise results;
- **Decisions I made:** every choice this prompt did not make, even if it was correct;
- **Flags outside scope.**

After merge, the session must be restarted for the agents to load. Say so in the report.
