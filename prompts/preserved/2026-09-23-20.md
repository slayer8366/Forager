HEAD: b2435efffc8cb6cac3c29f850c20581227ccbecf
Target subagent: coder
Type: build
Preserved: 2026-09-23T07:02:36Z by .claude/hooks/dispatch_guard.py
--- verbatim prompt follows ---
# Dispatch: finish the pulse hand-back fix under the owner's option (b)

**Type:** build
**Written:** 2026-09-23, by the planner in session bridge-cse_019uZR3mJKzHkDaCv5fGnsuk.

## Role

You are the coder. You make no design decisions. An ambiguity or a gap is a stop-and-ask. Validate this prompt structurally before acting and return any missing section by name.

## Base and state

These are premises. Verify each one before acting.

- Branch `pulse-handback` is pushed at `9a3aa1d`. Its worktree is `/home/zynergy-labs/Zynergy/Forager/.claude/worktrees/pulse-handback`, and it is clean.
- On that branch, `role_guard.py:31` is `PULSE_TOOLS = {"Read", "Grep", "Glob", "Bash", "SubagentHandback"}`.
- Intent `2026-09-23-26` is open; it claims `preserved/2026-09-23-17.md`.
- Dispatch-note `2026-09-23-27` claims `preserved/2026-09-23-18.md`, the `claude -p` before-run pulse.
- The previous coder stopped because a `claude -p` pulse is never offered `SubagentHandback`, so a live check there can't fail. Only bridge sessions offer it.
- **The live "before" run was done by the planner.** From this bridge session, whose checkout (`bridge-cse_019uZR3mJKzHkDaCv5fGnsuk`, at `b2435ef`) has the unfixed guard, it dispatched one pulse at about 2026-09-23. The Agent tool returned: "The subagent ended without delivering a report through SubagentHandback, so no report was delivered. Its unsent text is not shown."
  - That pulse's agent ID is `aa9d17d77201c7af2`.
  - Its log should be `~/.claude/projects/-home-zynergy-labs-Zynergy-Forager--claude-worktrees-bridge-cse-019uZR3mJKzHkDaCv5fGnsuk/1b897011-9b08-5256-851d-e0b3c0141e2e/subagents/agent-aa9d17d77201c7af2.jsonl`. Verify the path.
- The dispatch hook preserved that pulse in the bridge checkout as an untracked file under `prompts/preserved/`. Find it by its title, "Pulse: live \"before\" check of the pulse hand-back".
- The dispatch hook preserved this dispatch in the same checkout. Both files are untracked there.

## Scope boundary

- **On branch `pulse-handback` only:** `RECORD.md` and `prompts/preserved/`, then a PR against Forager `main`.
- No code or test changes. The fix is done.
- Nothing in any other checkout or repository.

## Closed decisions (owner, 2026-09-23, in chat)

The owner's words, to the planner's proposal: "Send it". That proposal was:
1. the planner sends a live "before" pulse from this bridge session, done and quoted above;
2. the owner rules the previous coder's option (b): accept the unit tests, the sabotage run and the six logged denials as the evidence for the fix; write the terminal entry as completed, with the live check recorded as split; open the PR;
3. the "after" half of the live check is the first pulse from a bridge session on `main` once the owner merges this PR; the planner records that result later.

These follow from it:
- **Quote the denial.** From the before-pulse's log, quote the `SubagentHandback` call and its denial, by line. If the log shows no denial, stop and report: the "before" result would then be something else.
- **Claim the two new prompts.** Copy each of the two prompts preserved in the bridge checkout byte for byte to the next free numbers on your branch, with SHA-256 matched: the before-pulse's and this dispatch's.
  - The before-pulse's prompt gets a dispatch-note: Type `pulse`, Outcome `answered`, and a Note saying it ran, its report was denied delivery, and why.
  - This dispatch's prompt is claimed by a new intent for this finishing work, which you close with its own terminal.
  - The originals stay in place as superseded copies.
- **Close `2026-09-23-26`** with a terminal entry: completed, predictions 1–3 held, prediction 4 split as above.

## Prediction

- The before-pulse's log shows a `SubagentHandback` call denied with "the pulse role may not use SubagentHandback".
- Both checkers pass after the new entries.
- The PR shows only `role_guard.py`, its test file, `RECORD.md` and `prompts/preserved/`.

## Finish line and abort conditions

**Finish:**
- the denial is quoted from the log;
- both new prompts are claimed;
- `2026-09-23-26` is closed, and your own intent and terminal are written;
- both checkers pass;
- the branch is pushed;
- a PR is open against `main`;
- no merge, no tag.

**Abort if:**
- the branch isn't at `9a3aa1d`, or its worktree isn't clean;
- the log shows no denial;
- either prompt can't be found or copied identically;
- a checker fails for any reason other than a prompt you're about to claim.

## Checks

Quote each:
- the log lines;
- `sha256sum` and `cmp` for each copy;
- both checkers before each `RECORD.md` commit and after the last;
- `git diff --stat origin/main...HEAD`;
- the PR URL.

## Out of scope

- The Claude-kit fix.
- The pulse's missing Grep and Glob tools.
- Any other checkout's originals.
- The "after" live run.

## Device items

None.
