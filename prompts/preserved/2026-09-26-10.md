HEAD: a3d7417f6f142551d8b355bfeb450ae232bc17bf
Target subagent: coder
Type: build
Preserved: 2026-09-26T04:31:55Z by .claude/hooks/dispatch_guard.py
--- verbatim prompt follows ---
**Type:** build

# Role

You are the coder. Add a note to PR #118's branch that marks the point where Forager's recordkeeping moves to Claude-kit v0.2's protocols. The note records the owner's ruling on merge entries: they start with the open PRs, and there is no backfill.

Follow the kit's `.claude/agents/coder.md` as installed on that branch for this build's record entries. You make no decision this dispatch has not made. A gap is a stop-and-ask.

# Base and state

Verify each item against the remote before acting. A mismatch is a stop.

- **The branch.** `kit-v0.2-install` is at `588227b904263318c0d73aa34d504aa52eda15cc`. It is checked out and clean in `/home/zynergy-labs/Zynergy/Forager/.claude/worktrees/kit-v0.2-install`.
- **The PR.** PR #118 is open into main and mergeable, with CI green on `588227b`.
- **The record.** `RECORD.md` ends with terminal `2026-09-26-16`, which closes `-15`. No intent is open. The checkers are the kit v0.2 copies: 58 entries, and 36 of 36 claimed.
- **`origin/main`** is at `af12a69603ab38295099ef27f0b3114c2a9ccd74`, the PR #117 merge. Forager's `RECORD.md` holds no `merge` entry. The kit's `coder.md` item 1 would therefore have the first sweep write one for every merge commit on main's first-parent chain.
- **Open PRs into main.** #101, #104, #109, #111, #112, #113 and #118. Re-read the list with `gh pr list --state open`.
- **This dispatch's store copy.** This session's hook saves it untracked in `/home/zynergy-labs/Zynergy/Forager/.claude/worktrees/bridge-cse_013QR4ELV3wyYrUUyVCEmDwt/prompts/preserved/`. It is expected to be `2026-09-26-10.md`; the hook's name governs. The originals `-02` to `-09` beside it are already claimed on this branch. Do not copy or sweep them.
- **This session's guards** are Forager's pre-kit hooks, loaded from the planner worktree.

# Scope boundary

All commits go on `kit-v0.2-install`, pushed after each commit:

1. This dispatch's store copy, byte for byte, together with its intent.
2. **The note.** A new file, `docs/audits/2026-09-26-recordkeeping-protocol-shift.md`, plus one row appended to `docs/audits/README.md`.
3. **A pointer in `CLAUDE.md`.** One sentence appended at the end of the file, after the sentence the install added. It names the note and says the merge-entry cutoff is there. No existing line moves or changes.
4. The terminal.

Only those three files, `RECORD.md` and the store copy change. No existing line of `RECORD.md`, `docs/audits/README.md` or `CLAUDE.md` is edited, reordered or removed. Everything is appended.

# Closed decisions

- **The owner's words** in this planner session, 2026-09-26: "Start with the open PRs moving forward. Write a note stating this is when recordkeeping protocols shifted. No history should be changed." They answer the question raised in terminal `2026-09-26-14` and intent `-15`'s Notes: how far back the kit's first sweep writes `merge` entries.
- **What the note states.** Wording is yours; content is fixed:
  1. **When protocols shifted.** Recordkeeping moves to Claude-kit v0.2's protocols with PR #118, which installs the kit (tag `v0.2`, `a2b8025` pointing to `b1bb0bd`). The record kinds, outcomes and checkers from then on are the kit's, and so are the coder's rules: sweep, `merge` entries, `stopped` dispatch-notes, and merges after a backup.
  2. **Where merge entries start.** They start with the open PRs, moving forward. A `merge` entry is written for every merge into main whose merge commit is not an ancestor of `af12a69`, main's tip when the kit was installed. That holds whatever order the PRs merge in, including #118's own merge. The open PRs at the time of writing are listed by number.
  3. **No backfill.** Merges into main up to and including `af12a69` get no `merge` entry. The first sweep stops at `af12a69` rather than walking the whole chain as `coder.md` item 1 would otherwise do. This note is the owner's ruling that sets that limit.
  4. **No history changed.** Every entry written before the shift stands as written, in Forager's pre-kit forms. That includes the 2026-09-26 entries that recorded stopped coder dispatches as an intent plus an `abandoned` terminal, where the kit would now use a `stopped` dispatch-note. None is converted, re-dated or rewritten.
  5. **Source.** It quotes the owner's words and cites this dispatch's store file and intent ID.
- **The README row.** Dated 2026-09-26, linking the note, in the index's existing row format. Append it after the last row.
- **The `CLAUDE.md` pointer.** It goes at the end, so that the line numbers cited elsewhere do not move. This is the planner's choice: coders read `CLAUDE.md` before acting, and the kit's `coder.md` is vendored and must not be edited.
- **Writing and git.** Write record entries and file edits with the Edit or Write tool, not shell heredocs. Run one git command per Bash call.

# Prediction

Planner, outcome:

1. **No line removed.** `git diff 588227b HEAD -- RECORD.md docs/audits/README.md CLAUDE.md` shows only added lines. There are no lines starting with `-` apart from the `---` file headers.
2. **Drift check.** `python3 check_kit.py` still passes, since no vendored file changes.
3. **Checkers.** Both pass after the terminal: 60 entries, and 37 of 37 claimed.
4. **CI.** It passes on the final commit.

Coder: write your mechanism prediction in the intent.

# Finish line and abort conditions

**Finish line:**
- The commits are pushed.
- CI is green on the final commit.
- A terminal closes this intent.
- The PR #118 body gains one line naming the note. Use the REST endpoint (`gh api -X PATCH repos/slayer8366/Forager/pulls/118`), since `gh pr edit` fails in this repository.

**Abort. Stop and report on any of these:**
- A premise mismatch, including a moved head.
- Any removed or changed existing line in the three files.
- A checker or `check_kit.py` failure.
- A guard blocking a step. Quote it.
- Two failed attempts at one step.

# Checks

- The diff from `588227b` to your final commit, for the three appended files, with a count of removed lines, which must be 0.
- `check_kit.py` output.
- Both checkers, before every `RECORD.md` commit and at the end, with the final result quoted.
- The store copy's size and sha256, and that it is cmp-identical to the original.
- The note's text in full in your hand-back.

# Out of scope

- Writing any `merge` entry now. The first one belongs to the sweep after #118 or another open PR merges.
- Merging anything.
- The camera strip PRs.
- GitHub protection on main.
- Memory files.
- The kit repository.

# Device items

None.

# Merge

Not authorised. This session's guards deny every `gh pr merge`. The owner merges PR #118.

End the hand-back with "Decisions I made" and "Flags outside scope".