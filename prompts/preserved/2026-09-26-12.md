HEAD: a3d7417f6f142551d8b355bfeb450ae232bc17bf
Target subagent: coder
Type: build
Preserved: 2026-09-26T04:50:46Z by .claude/hooks/dispatch_guard.py
--- verbatim prompt follows ---
**Type:** build

# Role

You are the coder. Correct three of the owner's memory files, and the memory index, so they mark which repositories are research and which is the product. Record the work in Forager's `RECORD.md`, including the owner's decision B.

This re-sends `preserved/2026-09-26-11.md`, whose coder stopped correctly. The planner's dispatch had put a paraphrase of D57 in quotation marks. This version quotes D57's recorded owner's words exactly and changes nothing else in substance.

Follow the kit's `.claude/agents/coder.md` as it stands on `origin/main`. You make no decision this dispatch has not made. A gap is a stop-and-ask.

# Base and state

Verify each item before acting. A mismatch is a stop.

- **`origin/main`** is at `d3e73825542cdded9668cda3dfe4ab994ed20161`, the PR #118 merge. Its parents are `af12a69` and `1e9d941`. The owner merged it in the browser at 2026-09-26T04:40:23Z, and CI passed on it.
  - Main carries Claude-kit v0.2.
  - `.claude/kit.json` has `backup_dir` set to `~/Zynergy/forager-repo-backups`.
  - Main carries `docs/audits/2026-09-26-recordkeeping-protocol-shift.md`. That is the owner's ruling that `merge` entries start with merges after `af12a69`, with no backfill.
  - `RECORD.md` ends with terminal `2026-09-26-18`, and no intent is open.
- **The owed `merge` entry.** `d3e7382` is not an ancestor of `af12a69`, so the sweep owes the record's first `merge` entry, for PR #118.
  - No `INDEX.md` exists in `~/Zynergy/forager-repo-backups`, because the owner merged by hand. So Backup is `none` and Merged-by is `owner`.
  - #118 carried entries `2026-09-26-01` to `-18`.
  - The previous coder derived and checked all of these. Derive them again yourself.
- **The stopped dispatch.** `preserved/2026-09-26-11.md` stopped before its intent: 7401 bytes, sha256 `4129e3f337831d4b3fd2c554ef063a432a93c32e6234a10042a8c09b769a89b5`. Under the kit's `coder.md` item 8, the sweep records it as a dispatch-note with Outcome `stopped`, claiming its store copy.
- **The two store copies.** `-11` and this dispatch's copy, expected to be `2026-09-26-12.md` (the hook's name governs), are untracked in `/home/zynergy-labs/Zynergy/Forager/.claude/worktrees/bridge-cse_013QR4ELV3wyYrUUyVCEmDwt/prompts/preserved/`. The originals `-02` to `-10` beside them are already claimed on main. Do not copy those.
- **The worktree.** `/home/zynergy-labs/Zynergy/Forager/.claude/worktrees/record-memory-and-protection` exists. It is on local branch `record-memory-and-protection` at `d3e7382`, clean, with no commits of its own, and unpushed. The previous coder left it there. Use it.
- **The memory files** are in `/home/zynergy-labs/.claude/projects/-home-zynergy-labs-Zynergy-Forager/memory/`. Their sha256 before any edit, as read by the previous coder:
  - `forager-app-repo.md`: `4c29bdbd87177d6bdcb991eb7a1df497a30f64e53cae5638fc4a7d193f0117db`
  - `forager-forecast-repo.md`: `05ed4165e5fbf9a43c4fb5d69c8c6568ac5907160afe93e8c081c2bd11c5e659`
  - `forager-forecast-local-clone.md`: `937835844b1b745e675652e79c618fae557f86cb22c9cee9ae8329539a5e8aa7`
  - `MEMORY.md`: `2707ea968c12fc0c738efc5ba941e7770df271b1eacfefff34a32d2de6d23c36`

  A different hash is a stop.
- **D57.** forager-forecast's D57 is at `docs/planning/DECISIONS.md:11` on forecast `main` (`876156b`). The owner's words it records are quoted under Closed decisions, as the previous coder read them.
- **This session's guards** are Forager's pre-kit hooks, loaded from the planner worktree.

# Scope boundary

- **Branch and PR.** Work on `record-memory-and-protection`. Push it with `-u origin record-memory-and-protection`, not to its current upstream, `main`. Open a PR against `main`.
- **Record commits, in order:**
  1. **The sweep.** The `merge` entry for PR #118, then the `stopped` dispatch-note for `-11` with its store copy.
  2. **The intent.** This dispatch's store copy, byte for byte, together with its intent.
  3. **The terminal**, after the memory edits.
- **Memory edits.**
  - Edit only the four memory files named above, and only as stated under Closed decisions.
  - Read each file in full first.
  - Change no other memory file, and delete none.
  - Memory files are outside the repository and are not committed.
  - The terminal records each file's sha256 before and after, and the exact lines changed.

# Closed decisions

**The owner's words**, in this planner session on 2026-09-26:
- "Forager-app is the research repo"
- "The forecast repo is also research, branched from the forager-app repo."
- "No protection, add memory corrections markings"

**Decision B: no GitHub protection on Forager's `main`.**
- Record it in the intent's Notes as the owner's decision, quoting "No protection". It answers the decision B left open in intent `2026-09-26-12`.
- Nothing is changed on GitHub.

**D57, quoted exactly.** Wherever D57 is cited, use these words and no other text inside quotation marks:

"Forager-app is a placeholder app for researching advanced methods, such as this project here, to integrate into the already existing Forager app as the ultimate end result. So prepare for actual Forager integration as you plan the forager-app."

Check it against `DECISIONS.md:11` before writing it. A mismatch is a stop.

**`forager-app-repo.md`:**
- **Frontmatter `description`.** Set it to exactly: `~/Zynergy/Forager-app (slayer8366/Forager-app) is the research repo, not the product; the existing Forager app at ~/Zynergy/Forager is the end result; never run its build and the emulator together`
- **Body, first paragraph.** Replace the paragraph that says the app was "rebuilt from scratch", including the sentence "The older app at `~/Zynergy/Forager` is kept as prior art only. Read it, don't build on it.", with:

  > `~/Zynergy/Forager-app` (pushed to `slayer8366/Forager-app` on `main`) is a research repo, not the product. The Forager repo at `~/Zynergy/Forager` is the product and the end result (owner, 2026-09-26). forager-forecast's D57 (2026-09-21) records the owner's words: "Forager-app is a placeholder app for researching advanced methods, such as this project here, to integrate into the already existing Forager app as the ultimate end result. So prepare for actual Forager integration as you plan the forager-app." Session reasoning and open decisions live in `docs/WORKING-PLAN.md` in Forager-app.

- Everything else in the file is unchanged.

**`forager-forecast-repo.md` and `forager-forecast-local-clone.md`:**
- Insert this sentence as a new paragraph directly after the frontmatter's closing `---`, before the existing body:

  > forager-forecast is a research repo, branched from the Forager-app research repo; the Forager repo at `~/Zynergy/Forager` is the product (owner, 2026-09-26).

- Nothing else in either file changes.

**`MEMORY.md`:**
- On the line linking `forager-app-repo.md`, replace the hook text after the em dash with: `research repo, not the product; ~/Zynergy/Forager is the end result; never run its build and the emulator together`.
- On the two lines linking the forecast files, insert `(research) ` at the start of the hook text, directly after the em dash and its space.
- Each line keeps its link and its position. No other line changes.
- The stale "ends at D54" and "unmerged" claims the previous coder flagged are **not** changed here. They are flagged for a later ruling.

**Tools.** Use the Edit tool for every memory and record edit, never a shell heredoc. Run one git command per Bash call.

# Prediction

Planner, outcome:
1. **The sweep** writes one `merge` entry: PR 118, Merge-commit `d3e7382`, Pre-merge `af12a69`, Base `main`, Head `kit-v0.2-install`, Backup `none`, Merged-by `owner`, Carries `2026-09-26-01` to `-18`. It also writes one `stopped` dispatch-note for `-11`.
2. **The checkers.** Both pass after each record commit.
3. **The memory edits** change only the stated lines, and the three memory files keep valid frontmatter.
4. **CI** is green on the PR's final commit.

Coder: write your mechanism prediction in the intent.

# Finish line and abort conditions

**Finish line.**
- The three record commits are pushed.
- The four memory files are edited as stated.
- CI is green on the final commit.
- The PR is open.

**Abort, and stop and report, on any of these:**
- a premise mismatch, including a memory file whose hash differs from the one above;
- D57's text differing from the quotation;
- an unclaimed store file other than `-11` and this dispatch's copy;
- a checker failure;
- a guard blocking a step (quote it);
- two failed attempts at one step.

# Checks

- The `merge` entry's fields, set against `git log --format=%H%x20%P -1 d3e7382` and `gh pr view 118`.
- Both checkers before every `RECORD.md` commit, with the final result quoted.
- For each memory file:
  - the lines before and after, quoted;
  - the sha256 before and after;
  - a read-back after the edit.
- D57's line as read.
- The size and sha256 of both store copies, and that each is cmp-identical to its original.

# Out of scope

- Any other memory file, or any other line in these four.
- GitHub settings.
- The camera strip PRs.
- Merging anything.
- The kit repository.
- Writing anything in forager-forecast or Forager-app. Reading D57 is in scope.

# Device items

None.

# Merge

Not authorised. This session's guards are Forager's pre-kit hooks, which deny every `gh pr merge`. The owner merges the record PR.

End the hand-back with "Decisions I made" and "Flags outside scope".