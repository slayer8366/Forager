HEAD: b2435efffc8cb6cac3c29f850c20581227ccbecf
Target subagent: coder
Type: build
Preserved: 2026-09-23T03:06:54Z by .claude/hooks/dispatch_guard.py
--- verbatim prompt follows ---
# Dispatch: merge Forager PR #115

**Type:** build
**Written:** 2026-09-23, by the planner, from chat, at Forager HEAD b2435ef.

## Role

You are the coder. You make no design decisions. An ambiguity or a gap is a stop-and-ask. Validate this prompt structurally before acting and return any missing section by name.

## Base and state

- Target: PR #115 on `slayer8366/Forager`, branch `record-pr114-merge` into `main`.
- As read by the planner at 2026-09-23 (verify every item before acting; these are premises, not facts):
  - state OPEN, mergeable MERGEABLE, mergeStateStatus CLEAN;
  - head commit `b86da59a3ef8140fc2162dc8919fef873b64f9d7`;
  - one check, "Build, test, publish APK", conclusion SUCCESS;
  - `main` at `b2435ef`.
- The owner's instruction, in chat to the planner, 2026-09-23: "Instruction a coder to merge rather since you're read onkt", following the planner's report that #115 was ready and the planner could not merge it. The owner's earlier ruling on #115 said "The owner merges"; this instruction delegates that merge to you for this one PR.

## Scope boundary

- One action: merge PR #115 with a merge commit, pinned to its head SHA.
- Read-only checks before and after it.
- No file changes, no commits, no pushes, no branch deletion, in any repository.

## Closed decisions (owner, 2026-09-23; planner decisions marked)

- The owner authorizes this merge, and only this one.
- Route: the GitHub REST merge endpoint via `gh api -X PUT repos/slayer8366/Forager/pulls/115/merge -f merge_method=merge -f sha=<verified head SHA>`. `gh pr merge` is blocked by the history guard, by design. The REST route is the gap recorded in Forager RECORD.md 2026-09-23-02 (on #115's own branch). It is used here only because the owner authorized this merge explicitly. Do not use it for anything else, and do not try any other route if this one is blocked.
- Merge method: merge commit, matching how #114 landed. Keep the branch.
- Planner decision, for the owner to approve with this dispatch: this dispatch writes no RECORD.md entry and makes no sweep commit, because either would need its own branch and PR to reach main, and so another merge. The prompt the dispatch hook preserves for this dispatch stays unclaimed until the next build dispatch's sweep writes its dispatch-note. Your report is the record until then.

## Prediction

The planner's: the history and role guards return no decision for `gh api -X PUT .../pulls/115/merge` from the coder (observed on the equivalent command in the kit-extraction pulse), so the call runs and GitHub returns `merged: true` with a new merge commit on `main` whose second parent is `b86da59`.

## Finish line and abort conditions

**Finish:** PR #115 merged. `main`'s new head is a merge commit whose parents are `b2435ef` and `b86da59`. Branch `record-pr114-merge` still exists. Report the merge commit SHA.

**Abort, before merging, if any of these holds:**

- the head SHA is not `b86da59a3ef8140fc2162dc8919fef873b64f9d7`;
- the PR is not open, not mergeable, or not CLEAN;
- the check is not SUCCESS;
- `main` has moved from `b2435ef`.

**Abort, and do not retry or try another route,** if a guard blocks the merge call. Report the guard's message verbatim.

## Checks

- Before: `gh pr view 115 --repo slayer8366/Forager --json state,mergeable,mergeStateStatus,headRefOid,statusCheckRollup` and `git ls-remote https://github.com/slayer8366/Forager.git main` (or `gh api repos/slayer8366/Forager/branches/main`). Quote the results.
- The merge call's JSON response, quoted.
- After: `gh api repos/slayer8366/Forager/pulls/115` (merged, merged_at, merge_commit_sha), `gh api repos/slayer8366/Forager/commits/<merge sha>` (both parents), and `gh api repos/slayer8366/Forager/branches/record-pr114-merge` (still exists). Quote the results.

## Out of scope

- Any other PR, including #109, #104 and the strip stack.
- The Claude-kit build.
- Any fix to the gh api merge gap.
- Editing any file, RECORD.md included.

## Device items

None.
