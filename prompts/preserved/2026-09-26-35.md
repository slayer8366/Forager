HEAD: bc54f6dc12e82c8b7be265516c8a3162ac42f9d1
Target subagent: coder
Type: build
Preserved: 2026-09-26T18:01:34Z by .claude/hooks/dispatch_guard.py
--- verbatim prompt follows ---
**Type:** build

# Role

You are the coder. This is build step B1 of `docs/plans/landscape-phone-design.md`: height-aware window classification, the navigation rail on the charger-port side, and correct insets at both landscape rotations. You make no design decision beyond what this dispatch and that document state. A gap is a stop-and-ask.

# Base and state

Verify each premise before relying on it; a mismatch is a stop. The planner read these at `bc54f6d` unless marked otherwise.

- **Remote and checkouts.** `origin/pre-main` is at `bc54f6dc12e82c8b7be265516c8a3162ac42f9d1` (merge of #126, the design document; parents `f7727fe` and `20e84bd`). The main checkout and the planner worktree are both at `bc54f6d`.
- **Record and backup.**
  - #126's merge has no `merge` entry yet. Backup `2026-09-26-10`, intent `-93`, `preserved/2026-09-26-34.md`, Carries `-92` to `-94`; confirm from the diff.
  - Next free backup: `2026-09-26-11`.
- **Design and its open questions.** The design is `docs/plans/landscape-phone-design.md`. Read all of it, including its 13 open questions and its drift list. The planner's resolutions are in Closed decisions below.
- **Window classification** (planner read):
  - `ui/adaptive/WindowWidthClass.kt:39-45` classifies by `screenWidthDp` only: COMPACT below 600, MEDIUM below 840, else EXPANDED.
  - Call sites: `AvailabilityScreen.kt:984`, which feeds the branch at `:2081`, and `AvailabilityResultsUi.kt:245`, which feeds `:251`.
- **The branch at `:2081`** (planner read). COMPACT gets `ModalNavigationDrawer(gesturesEnabled = false, …, content = compactMainScaffold)` (`:2082-2111`). Everything else gets `PermanentNavigationDrawer` plus `mainScaffold` (`:2117-2136`).
- **The bottom navigation** (planner read). `ForagerBottomNav` is defined at `:2191`. It is called in two places:
  - `:1622`, the compact Scaffold's `bottomBar`, whose `contentWindowInsets` is at `:1605`;
  - `:4045`, inside the Map tab's overlay.
- **Rotation-to-edge mapping** (planner read), in `ui/log/CameraArrangement.kt`:
  - `cameraArrangement(lockToPortrait, windowIsLandscape, displayRotation)` at `:125-139`: ROTATION_270 → `LandscapePortLeft`, else landscape → `LandscapePortRight`.
  - `ScreenEdge` at `:153-166`.
  - `portEdge(arrangement)` at `:178-182`.
  - `punchHoleEdge` at `:185`.
- **On-device facts** (from the capture, `docs/audits/assets/2026-09-26-landscape-capture/r1-window-displays.txt` and `r3-…`):
  - The system 3-button bar is on the RIGHT at ROTATION_90 and the LEFT at ROTATION_270. It is 135px, which is 48dp.
  - The cut-out is on the opposite side, 75px (27dp).
  - The status bar is 84px (30dp).
  - The window is `w823dp h384dp`.
- **Robolectric reports zero window insets** (CLAUDE.md). No test here can check inset values; the device check (B4) does.
- **Nothing else is running.** No other build is running or will merge into `pre-main` while you run.

# Scope boundary

1. **Worktree.** `git worktree add ~/Zynergy/forager-wt/landscape-b1 -b landscape-b1 origin/pre-main`. Unset the upstream, push with `-u`, and move your store copy in with `mv`.
2. **Sweep.** Write the `merge` entry for #126, plus any unclaimed copy found by coder.md item 1. A pulse copy the planner sent is claimed as answered in the planner's session. If a copy's outcome is unknowable, stop.
3. **Intent.** Commit the store copy with the intent.
4. **Design addendum.** Append a dated "Resolutions" section to `docs/plans/landscape-phone-design.md`, recording Closed decisions R1 to R11 below with their attribution. Append only; no earlier line changes. Commit it.
5. **Baseline suite.** Run the full unit suite at your base.
6. **The build.** Tests-only commits come first, failing for their stated reason. Then the implementation. Push after each commit.
   - **(a) The classifier.** Add a new function or class beside `WindowWidthClass`, for example `isShortWindow()`, reading `screenHeightDp < 480`. Do not change `currentWindowWidthClass()` itself.
   - **(b) Which tree.** At the `:2081` branch, a short window takes the compact tree whatever its width. Every other window behaves exactly as today.
   - **(c) The port edge.** A shared, non-camera helper gives the port edge and the punch-hole edge for the main window, from `windowIsLandscape` and the display rotation. It reuses the camera's mapping rather than duplicating it. Move or extract the pure mapping so both callers use one function. The camera's behaviour and its tests stay unchanged; its tests are the evidence.
   - **(d) Landscape rail.** In a short, landscape window, `ForagerBottomNav` is replaced at both call sites by a Material 3 `NavigationRail` on the port edge, next to the system navigation bar. The rail has:
     - the same destinations, labels, icons and selection behaviour, in the same order from top to bottom;
     - the `navigationBars` inset on its own side only.

     Content lies beside the rail. Portrait keeps `ForagerBottomNav` exactly as today.
   - **(e) Content insets beside the rail.**
     - On the punch-hole side, controls are padded by `displayCutout`. Map tiles may draw under it.
     - At the top, `statusBars`.
     - Nothing reserves space at the bottom for a system bar that is not there in landscape.

     The map's own controls (cluster, strip, search, HUD) are **not moved in B1**; they stay as they are until B2. Record in the terminal how they look in the interim, from the Robolectric layout only.
7. **Docs, terminal, merge.**
   - Write a completion report `docs/audits/2026-09-26-landscape-b1-completion-report.md` and add one README row.
   - Run the full suite again.
   - Write the terminal, open a PR into `pre-main`, and get CI green on its final commit.
   - Merge, running each of these as its own Bash call, never chained and with no `cd` prefix: `git fetch origin`; confirm `origin/pre-main` is `bc54f6d`; write backup `-11`; `gh pr merge <N> --merge`; `gh pr view <N> --json state,mergeCommit`. If the merge is refused on form alone, retry once, bare.
   - Run `update_worktree.py` on the main checkout and on the planner worktree: dry run first, then `--apply`.

**Record IDs.** `2026-09-26-95` to `-99`. Before each `RECORD.md` commit and before the merge, check that none is on `origin/pre-main`. If you need more than five, stop.

**Files.** App code and tests under `app/`, the design addendum, the completion report, one README row, `RECORD.md`, and your store copy. No new dependency and no version change. If `NavigationRail` needs a dependency the app lacks, stop.

**Not touched.** The wide tree's behaviour for tall windows, the camera's behaviour, `.claude/`, `CLAUDE.md`, `ci.yml`, `main`, and the phone.

# Closed decisions

**The owner, planner session, 2026-09-26.**
- O1: the whole app in landscape; it "looks broken", "not designed at all".
- O2: "Keep the icon strip at the port side. That's my design." That is the bottom nav bar, which becomes a port-side rail.
- O3: a sideways phone gets the compact redesign, adapted. Tablets and foldables keep the wide tree.
- O4: "You're the designer so pick something based on your mobile UX research."
- **New:** "Classify by window". Any window under 480dp tall gets the sideways-phone layout, whatever the device. A full-screen tablet is never that short. This resolves the document's P1/O3 open question.
- "merge into pre main without device check. defer device check for later". The merge is authorised.

**The planner's resolutions of the open questions (R1 to R11)**, under O4. Record them in the addendum.
- **R1. P1/O3.** Owner ruling "Classify by window", as above.
- **R2. P12 corrected.** Back unwinding the drawer's Settings panel to the rest of the Tools drawer is documented behaviour (`AvailabilityScreen.kt:2478-2479`, `:2848`) and stays. The planner's "bug" label was wrong. The only fix P12 keeps is that a scrim tap closes the drawer, which `:865` and `:2335-2338` expect. It must not re-enable swipe-to-open over the map. That fix belongs to B3, not B1.
- **R3. P6.** "Inside the cut-out inset" means inboard of it: the cluster sits clear of the cut-out.
- **R4. Cluster side memory.** In landscape, the cluster's side is remembered as *port side* or *punch-hole side*, not left/right. Turning 90↔270 keeps it on the same device edge. Portrait keeps today's memory. B2.
- **R5. Search sheet and HUD.** They share the rail side. The search sheet is modal and covers the HUD while open. B2.
- **R6. Keyboard.** In the search sheet the field sits at the top and results shrink with the IME. No full-screen extract UI. B2.
- **R7. Information architecture.** Settings and Album stay inside the tools drawer, as in the compact tree. No new destinations.
- **R8. P7 is landscape-only.** Portrait's compass strip is unchanged.
- **R9. Shared helpers.**
  - The rotation-to-edge mapping is shared with the camera, with no camera behaviour change. That is B1's step 6(c).
  - `READABLE_CONTENT_MAX_WIDTH` (`AvailabilityResultsUi.kt:303`, `private`) becomes shared when B3 needs it; not in B1.
- **R10. Implementation checks, to be made while building.**
  - Whether Material 3 `1.5.0-alpha26` can anchor a drawer or side sheet to the end edge. B2/B3 need this; B1 only records what it finds.
  - Whether Robolectric can set ROTATION_90 and ROTATION_270 distinctly, for example with `ShadowDisplay`. If it can't, test the shared mapping function directly for both rotations and leave the composed side to B4. Say which you did.
- **R11. The drift list** in the document stays as recorded. No edits to older plans.

**Record entries and README rows.** Write them with Write or Edit, never a shell heredoc.

# Prediction

Planner, outcome:
1. A short-window classifier plus one changed condition at `:2081` routes the phone's landscape window to the compact tree. The existing wide-window tests (`AvailabilityScreenWideWindowLayoutTest`, at `w840dp-h1024dp`) pass unchanged.
2. Portrait layout tests pass unchanged.
3. The camera's tests pass unchanged after the mapping is shared.
4. The full suite grows by 10 to 30 tests with 0 failures, and skipped stays at the baseline.

Coder: write your mechanism prediction in the intent.

# Finish line and abort conditions

**Finish line.**
- Steps 1–7 are done.
- Every tests-first commit was seen failing for its reason, then passing.
- The revert checks are done.
- The suite has 0 failures and CI is green.
- The PR is merged with backup `-11`.
- Both checkouts are updated.
- Both checkers PASS.

**Deferral is not allowed.**

**Abort, stop and report, on any of these:**
- a wrong premise;
- a needed dependency;
- a suite failure outside this work (report it; don't touch it);
- a tests-first commit passing before its implementation;
- a revert-check build log with compile errors;
- an ID collision or more than five IDs;
- any guard or classifier refusal, apart from the one form-only merge retry;
- CI red;
- two failed fixes on one symptom.

# Checks

**Entry point.** Drive the real `AvailabilityScreen` through the existing test harness.

**Required cases.**
- `w823dp-h384dp-land`: the compact tree is used. There is no `PermanentNavigationDrawer`, and no `ForagerBottomNav`. A `NavigationRail` shows with the same destinations in order. Selecting a rail item switches tab.
- The port edge at each landscape rotation: through composition if Robolectric can rotate, otherwise through the shared mapping function (R10).
- `w840dp-h1024dp`: the wide tree, unchanged.
- `w360dp-h640dp`: the bottom nav, unchanged.
- Rotating from portrait to short-landscape keeps the selected tab. That is the stale-state failure P15 names; this checks it is gone for tab state.
- A coordinate `performTouchInput` long-press on the map at points beside the rail, in the map area, reaches the map. Sample several points along the rail's inner edge (CLAUDE.md: a finger is not a point).

**Revert checks.** Do one each for:
- the height condition at the branch;
- the rail replacing the bottom nav;
- the port-side choice.

Follow CLAUDE.md's rule: save a copy first; check the build log for compile errors before reading results; restore from the copy, not from git; confirm the forward change is present afterwards. Quote each failure message.

**Suites and CI.** Report the full suite before and after (classes, tests, failures, skipped), and both checkers' PASS lines. Report CI run ids, the backup's `merge.json` and `INDEX.md` line, the merge commit and its parents, and the `update_worktree.py` outputs.

# Out of scope

- B2 (map controls, search sheet, HUD, fullscreen, cluster memory).
- B3 (other destinations, drawer, scrim fix, readable width).
- B4 (device check).
- The wide tree's tall-window behaviour.
- The camera.
- The Claude-kit `SendUserFile` change.
- The phone.
- Rebase, amend, force-push, delete. Worktrees you add are left in place.

# Device items

None are run here; B4 runs them at both rotations. List them in the completion report:
- the rail sits next to the system bar on the port side at ROTATION_90 and ROTATION_270;
- no rail item is under the system bar;
- no control sits in the cut-out band;
- the status bar doesn't cover content.

# Merge

authorised: the `landscape-b1` PR into `pre-main` only, per coder.md item 10(a). Backup first; merge command bare. Nothing into `main`.

End the hand-back with "Decisions I made" and "Flags outside scope".