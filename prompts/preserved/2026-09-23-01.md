Operator ruling, 2026-09-23.

1. Record entry: the owner authorized the PR #114 merge. Cite
   the session log file and line 1121 only. Record the route
   (REST PUT from a worktree with no hooks) and that the history
   guard passes `gh api` merges even when present.
2. Copy that session log into ~/forager-backups/ with an INDEX.md
   line.
3. Move /tmp/kitprobe into ~/forager-backups/ with an INDEX.md
   line. Don't delete it.
