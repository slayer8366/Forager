HEAD: 0eade34fd15e1e94a48c2b98d5f2698715a56042
Target subagent: coder
Type: build
Preserved: 2026-09-26T06:46:41Z by .claude/hooks/dispatch_guard.py
--- verbatim prompt follows ---
**Type:** build

# Role

You are the coder. This is Part A of finishing the camera strip. You merge `pre-main` into the three stacked strip PRs, retarget them to `pre-main`, and merge them into `pre-main` in order. You write no app code. You make no decision this dispatch has not made; a gap is a stop-and-ask.

# Base and state

Verify each item against the remote before relying on it. A mismatch is a stop.

- **Remote heads.**
  - `origin/pre-main` is at `0eade34fd15e1e94a48c2b98d5f2698715a56042`, the PR #120 merge. Its parents are `76905d4` and `c5693d4`.
  - `origin/main` is at `76905d4`.
- **The stack.** Each PR's base is `main`, and GitHub reports each as `CONFLICTING`:
  - #111 `strip-housekeeping` at `1b82b15f30622b43b9a22b8456a4e95c42b37438`
  - #112 `strip-torch` at `1f196042e9146b3d2d77cb33886d0fb0accaf416`
  - #113 `strip-grid-level` at `6bcbe4c68d144e8cab05cacce383708c02e986f9`
- **Findings from a pulse, read at `76905d4` and `6bcbe4c`.** Each one is a premise to re-check:
  - The merge base with main is `89f53a4`.
  - Main changed nothing under `app/` between `89f53a4` and `76905d4`.
  - `docs/audits/README.md` is changed on both sides, with pure insertions after the same line 175. Main adds 5 rows; the stack adds 4.
  - `docs/audits/2026-09-21-camera-strip-basics-decisions.md` exists only on the stack.
  - The planner infers that the README is the only conflict. Since then, pre-main has also gained #120's commits (`RECORD.md`, `prompts/preserved/`, two notes, two README rows, kit.json). Re-check the conflict set against `0eade34`, not against `76905d4`.
- **The planner worktree.** `/home/zynergy-labs/Zynergy/Forager/.claude/worktrees/bridge-cse_01BcqShzosraMo4pUXkqaqRp` is at `0eade34` and clean.
  - This dispatch's store copy is written there as an untracked `prompts/preserved/2026-09-26-NN.md`. Find it by its body; do not assume the number.
  - **A second build dispatch runs in parallel**: the ci.yml change, on its own branch `ci-pre-main`. Its copy lands there too. Touch only your own copy.
- **The record.** The newest entry on `pre-main` is `2026-09-26-34`. PR #120's merge (`0eade34`) has no `merge` entry yet.
- **Backups.** `~/Zynergy/forager-repo-backups/` holds `2026-09-26-01` (#120) and `INDEX.md`.
- **Last recorded full suite on the stack:** 211 classes, 1630 tests, 0 failures, 24 skipped, at `42e3dd3` (`docs/audits/2026-09-22-grid-level-completion-report.md:179`).

# Scope boundary

Create worktrees with `git worktree add` under `~/Zynergy/forager-wt/`, one per strip branch. Leave the planner worktree alone, apart from the two things below: moving your own store copy out of it, and step 8.

1. **Merge pre-main into `strip-housekeeping`.** In its worktree, run `git merge origin/pre-main` with a merge commit.
   - Resolve `docs/audits/README.md` by keeping every row from both sides. Order rows by date; where the date is the same, pre-main's rows come first. Edit no row's text.
   - Any conflict in another path is an abort.
2. **Record, on `strip-housekeeping`.** Records now travel with the PR, the kit v0.2 way; see `merge` entries' `Carries` field.
   - First, move your own store copy with `mv` from the planner worktree into this worktree's `prompts/preserved/`, and record its original path, size and sha256.
   - Commit a: the sweep. It writes the `merge` entry for PR #120 per coder.md item 1:
     - Merge-commit `0eade34`, Pre-merge `76905d4`, Base `pre-main`.
     - Backup `2026-09-26-01`, Merged-by `coder preserved/2026-09-26-23.md`, Carries `2026-09-26-25` to `-34` as the PR carried them. Confirm this from the diff.
     - The planner assigned this entry to you alone. The parallel ci.yml dispatch writes none.
   - Commit b: the store copy together with your intent.
   - Push after each commit.
3. **Merge upward.** Merge the updated `strip-housekeeping` into `strip-torch`, then the updated `strip-torch` into `strip-grid-level`. Push each.
4. **Terminal.** Write the terminal on `strip-housekeeping`. Merge it upward again as in step 3, and push. Every branch then carries the whole record. Per coder.md item 3, the terminal comes before the merges into pre-main. It cites the backup folder you will use for #111, and says that #112's and #113's backups follow.
5. **CI.** Wait for CI to go green on each branch's final head.
6. **Retarget.** Retarget #111, #112 and #113 to base `pre-main`. `gh pr edit --base` failed earlier on a gh GraphQL deprecation error, so use `gh api -X PATCH repos/slayer8366/Forager/pulls/<N> -f base=pre-main`. State in each PR body that it is stacked and merges in order.
7. **Merge, in order: #111, then #112, then #113.** For each:
   - `git fetch`;
   - write a backup of the current `origin/pre-main` tip, per coder.md item 10, as the next free `2026-09-26-NN` folder. The parallel coder writes there too, so if a name is taken, take the next one;
   - `gh pr merge <N> --merge`.

   If `pre-main` moved between your fetch and the merge because the ci.yml PR merged, history_guard will deny the merge. Re-fetch, write a new backup and retry. That counts as the first attempt. After each merge, confirm the next PR is still MERGEABLE, with CI green on its head.
8. **Update the planner worktree.** After #113 merges, run the merged `update_worktree.py` on the planner worktree only: a dry run, then `--apply`. Report both outputs. If the dry run exits 1 because of untracked store copies other than your own, do not apply; report it. Do not touch the main checkout.

**Record IDs.** Use only `2026-09-26-50` to `-69`. Before each commit that touches `RECORD.md`, and again before each merge into pre-main, confirm that none of your IDs appears in `origin/pre-main`'s `RECORD.md`. If one does, stop.

**Files.** Only `docs/audits/README.md` (the conflict resolution), `RECORD.md` and your store copy. No new notes, and no new README row: the index is a serialization point while a parallel dispatch runs.

**Not touched:**
- app code, and every other file, beyond what the merges bring in;
- `.claude/` and `ci.yml`;
- `main`;
- the main checkout;
- GitHub settings;
- the phone.

# Closed decisions

The owner, in the planner session, 2026-09-26:
- "We need to finish the camera to basic functionality. That means populate the camera strip with basic camera tools."
- Chose "Land the stack, then extend".
- "merge into pre main without device check. defer device check for later".
- "step 1 done", after creating `pre-main` and setting `protected_branches`.

**The merges of #111, #112 and #113 into `pre-main` are authorised** by that ruling.

**Planner calls:**
- Records ride on the stack branches.
- The #120 merge entry is yours alone.
- There is no README row this time.

**Record entries.** Write them with Write or Edit, never with a shell heredoc.

**Parallel dispatches.** Store copies other than your own belong to other dispatches. Leave them alone.

# Prediction

Planner, outcome:
1. **The first merge.** Merging `origin/pre-main` into `strip-housekeeping` conflicts only in `docs/audits/README.md`. The two upward merges conflict at most there.
2. **README row count.** The result's index row count (lines starting `| 20`) equals pre-main's rows plus the 4 the stack added.
3. **No app changes.** `git diff --stat <old head> <new head> -- app/` is empty for all three branches.
4. **CI.** It is green on each final head. The PRs become MERGEABLE against `pre-main`.
5. **The merge chain.** #111, then #112, then #113 merge without conflict. `pre-main`'s tree at the end equals `strip-grid-level`'s final tree, plus anything the ci.yml PR merged meanwhile.

Coder: write your mechanism prediction in the intent.

# Finish line and abort conditions

**Finish line:**
- steps 1 to 8 done and pushed;
- three backups written;
- #111, #112 and #113 merged into `pre-main`;
- both checkers PASS on `strip-grid-level`'s final head;
- the planner worktree updated or, as allowed in step 8, reported.

**Deferral is not allowed.**

**Abort, and stop and report, on any of these:**
- a wrong premise;
- a conflict outside `docs/audits/README.md`;
- keeping every row needs a row edited or dropped;
- a non-empty `app/` diff from a merge;
- an ID collision;
- CI red;
- any guard or classifier refusal, other than the one stale-backup retry in step 7. Quote it, and do not route around it;
- two failed attempts at one step.

# Checks

- **README resolution.** Row counts on each side and in the result, and the resulting rows around the join.
- **App diff.** `git diff --stat` from each branch's old head to its new head, and the `app/` diff showing empty.
- **Checkers.** Run both before every commit that touches `RECORD.md`; quote the final PASS lines.
- **CI.** Run id and conclusion on each final head.
- **Backups.** Each folder, its `merge.json` and its `INDEX.md` line.
- **Merges.** Each merge commit and its parents; `origin/pre-main` at the end.
- **Planner worktree update.** The `update_worktree.py` outputs.

# Out of scope

- Part B (flash modes, timer, location), which comes after.
- Any fix to what the stack already contains.
- Branch protection.
- Promotion to `main`.
- Rebase, amend, force-push, delete. Worktrees you add are left in place and listed in the hand-back.

# Device items

None run. The strip's device check is deferred to promotion, by owner ruling.

# Merge

authorised: #111, #112 and #113 into `pre-main`, in that order, per coder.md item 10(a), each with its own backup first. Nothing into `main`.

End the hand-back with "Decisions I made" and "Flags outside scope".