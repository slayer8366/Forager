HEAD: b070a2e378526e8e993734f859c687ee71746367
Target subagent: coder
Type: build
Preserved: 2026-09-26T05:40:44Z by .claude/hooks/dispatch_guard.py
--- verbatim prompt follows ---
**Type:** build

# Role

You are the coder. This dispatch removes the operator-approval prompt for `build` and `device` dispatches. It does so by changing `.claude/kit.json` on the planner worktree's own branch, so the hooks that this worktree's sessions run pick it up at once. It replaces the dispatch preserved as `prompts/preserved/2026-09-26-16.md`, which stopped before any work: its finish line needed `update_worktree.py` runs that could not succeed. You make no decision this dispatch has not made; a gap is a stop-and-ask.

# Base and state

Verify each item before relying on it. A mismatch is a stop.

- **Where you work.** The planner worktree `/home/zynergy-labs/Zynergy/Forager/.claude/worktrees/bridge-cse_01BcqShzosraMo4pUXkqaqRp`, on branch `worktree-bridge-cse_01BcqShzosraMo4pUXkqaqRp`.
  - It is at `b070a2e`, pushed, with PR #120 (record-only) open against `main`.
  - Untracked, and nothing else: `prompts/preserved/2026-09-26-16.md` (the stopped dispatch, 7426 bytes, `Preserved: 2026-09-26T05:37:59Z`) and this dispatch's copy, expected at `-17`.
- **`origin/main`.** At `76905d4`.
- **Hook tests.** The previous coder observed at base `b070a2e`:
  - `.claude/kit.json:15` is `"approval_exempt_types": ["pulse"]`.
  - kit.json is not in `kit.lock`.
  - `check_kit.py` PASS, 26 files.
  - `python3 -m unittest discover -s .claude/hooks/tests -p 'test_*.py'` ran 179 tests, OK.
  - Re-run the tests and `check_kit.py` yourself before the edit.
- **Why editing this branch is enough.** `guardlib.py:37-40` reads the `kit.json` beside the running hooks directory, and each hook call is a new process. So a committed or even saved edit here takes effect on the next tool call in any session rooted in this worktree. Confirm by reading those lines, and cite them.

# Scope boundary

In the planner worktree, on its branch, in this order, pushing after each commit:
1. **The sweep.** A `dispatch-note` claiming `2026-09-26-16.md` with Outcome `stopped`, per coder.md item 8, plus anything else item 1 requires.
2. **Intent.** This dispatch's store copy together with its intent.
3. **Tests-only step.** Record the positive control's base result (below) in the intent or in the note. No test file is added, because hooks and tests are out of scope.
4. **The edit.** `.claude/kit.json`: `"approval_exempt_types": ["pulse", "build", "device"]`. No other key changes.
5. **The decision note.** A new `docs/audits/2026-09-26-approval-exempt-build-device.md`, holding:
   - the owner's rulings, quoted
   - what still gates builds and device work: role_guard, device_guard, history_guard, and the dispatch_guard type-target binding, section check and preservation
   - the rejected alternatives: keeping `device` gated, and changing Claude-kit upstream
   - the stopped `-16` dispatch and why it stopped
   - the stale wording found under Checks, listed and not edited
   - the flags about the main checkout (below)
6. **Index.** One row in `docs/audits/README.md`, keeping every row.
7. **The terminal.**
8. **PR #120.** Edit its title and body so they say it now carries the kit.json change and the note as well as the record. Use `gh pr edit`. No new PR.

Not touched:
- any hook script or test
- `.claude/settings.json`
- `.claude/kit.lock`
- `.claude/agents/*`
- `CLAUDE.md`
- Claude-kit's repository
- app code
- the strip branches
- the main checkout at `/home/zynergy-labs/Zynergy/Forager`
- the phone

No merge of any PR, and no `update_worktree.py --apply`.

# Closed decisions

The owner, in the planner session, 2026-09-26:
1. "Mark my approval in advance".
2. "Can't have me hitting buttons. Those hooks need to change".
3. The owner chose **"build and device"** when asked which types run without approval. The option was worded: "No prompts at all. The other guards still apply: role_guard, device_guard, and history_guard, which blocks merges without a backup, force-pushes and the like. Nothing asks before a coder touches the phone."
4. "The kit has a way of dealing with this". The planner reads that as `approval_exempt_types`: `guardlib.py:145-146`, "Approval fails closed: every Type asks the operator unless it is listed here. Required, so that every exemption is written down." Record that reading in the note as the planner's, not the owner's.

The planner's calls, to be recorded as such:
- Apply the edit on the planner branch rather than a separate PR plus merge. The reason: the gate reads this worktree's kit.json, so no merge is needed for the owner's goal.
- The `-16` dispatch's merge authorisation is withdrawn.

**Record entries.** Written with Write or Edit, never a shell heredoc.

# Prediction

Planner, outcome:
1. **Before and after.** At the base, dispatch_guard returns `ask` for well-formed build and device payloads. After step 4 it returns `allow` for both, naming approval_exempt_types.
2. **Nothing else changes.** Pulse stays `allow`. An unknown type, a wrong target and a missing section stay `deny`.
3. **Tests and drift.** The hook suite stays at 179 OK, and `check_kit.py` stays PASS.
4. **Checkers.** `check_record.py` and `check_prompts.py` pass after each record commit.

Coder: write your mechanism prediction in the intent.

# Finish line and abort conditions

**Finish line.**
- Steps 1 to 8 are pushed.
- The positive control shows `ask` before and `allow` after.
- The suite is 179 OK, `check_kit.py` is PASS and both checkers PASS.
- `git status --short` is empty, and PR #120 is updated.

**Deferral is not allowed.**

**Abort, and stop and report, on any of these:**
- a wrong premise
- an untracked file other than the two named
- a hook test failing
- `check_kit.py` failing
- any guard blocking a step: quote its text
- two failed attempts at one step

# Checks

- **Positive control.**
  - Drive `dispatch_guard.guard()` in a temporary git repository via `.claude/hooks/tests/harness.py`, or built the same way. No file may appear under this worktree's `prompts/preserved/` from the check: show `ls` before and after.
  - Cases: build to coder, device to coder, pulse to pulse, an unknown type, build to pulse, and a build with a section missing.
  - Run it with the temp repo's kit.json first as the base and then as the edited version. Quote the decision and reason for each.
- **Suite and drift.** The hook suite and `check_kit.py` run before and after the edit.
- **Stale wording.** Run `git grep -n -i "approv"` over `.claude/`, `CLAUDE.md` and `docs/process/`. List every line that now overstates the gate, in the note only.
- **Checkers.** Both run before every commit touching `RECORD.md`; quote the final PASS lines.

# Out of scope

- The main checkout at `/home/zynergy-labs/Zynergy/Forager`: it is on `claude/new-session-vto65i`, has diverged from its remote, has an uncommitted `.gitignore` edit and has no `.claude/kit.json`. Record the observation in the note's flags; do not touch it.
- The empty backup directory.
- Merging, rebase, amend, force-push, delete.
- The camera-strip work.

# Device items

None.

# Merge

Not authorised. PR #120 is merged by the owner, at the owner's choosing. The gate change is live without it.

End the hand-back with "Decisions I made" and "Flags outside scope".