Operator ruling, 2026-09-22.

Design: land the attached revision on PR #114's branch, replacing
docs/process/accountability-design.md, unmodified, in its own
commit. Commit message: supersedes the committed version; "Revert
paths" is a phase 2 proposal and nothing in it is built.

Flag 1, before merge: test it. From the planner session, dispatch
the general-purpose agent with a harmless read-only MCP call (a
list or get). Record whether it runs. If it does, fix it on #114:
the dispatch hook allows only subagent types `coder` and `pulse`
and blocks every other type, built-ins included. Build it
failing-first, then run the same dispatch live and confirm it is
blocked. Merge waits on this.

Transcripts: check Claude Code's local session logs for the
live-exercise and device-run sessions. Report what exists. Copy
anything found into ~/forager-backups/ with an INDEX.md line.
Don't delete anything.

Coder egress: no change now. In your report, state whether
Claude Code 2.1.280's sandboxing can limit network egress, with
the source.
