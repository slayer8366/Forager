HEAD: 2753a8359067dd1acf944d45f42bbed052e0e0f0
Target subagent: pulse
Type: pulse
Preserved: 2026-09-26T09:26:04Z by .claude/hooks/dispatch_guard.py
--- verbatim prompt follows ---
**Type:** pulse

## Role

You are the pulse. You are read-only. Answer with evidence: the command you ran and its output, trimmed. Change nothing on the phone or in the repository. Do not install, uninstall, grant, clear, tap, launch or reboot anything. Reads only: `adb devices`, `getprop`, `dumpsys`, `pm list`/`pm path`, `settings get`, `wm size`/`wm density`, `screencap`. For `screencap`, only if a question asks for it, and save the output under /tmp, never in the repo.

## Base and state

- The planner assumes the S22 Ultra test phone (model SM-S908U) is connected over adb. Verify this. The owner's daily driver is an S26 Ultra and must not be read at all. If more than one device is connected, identify each by `ro.product.model` and read only the S908U. If the S908U is not connected, say so and stop.
- Premise to check: `.claude/kit.json:2` names the package `com.zynergylabs.forager.app`. The debug build's package may carry a suffix, so check `app/build.gradle.kts` for `applicationIdSuffix`.
- Repository base: `pre-main` at `2753a83`. A coder is building the strip's Part B on branch `strip-flash-timer-location` at the same time. Its code is not merged yet.
- device_guard governs adb use. If it refuses a command, quote its text.

## Rules

- Every claim carries the command and its output, or a `path:line` in the repo, or is marked unverified.
- Keep read, observed and inferred apart.
- Be compact.
- Do not quote, summarise, cite or name the Restricted Object.

## Questions

1. **Device.** `adb devices -l`. For the S908U, give its serial, `ro.product.model`, `ro.build.version.release`, `ro.build.version.sdk` and `ro.build.display.id`.
2. **Installed Forager builds.** Which Forager packages are installed (`pm list packages | grep -i forager`)? For each, give `versionName`, `versionCode`, `firstInstallTime`, `lastUpdateTime` and the signing certificate digest (`dumpsys package <pkg>`, the signatures / signing-info section).
3. **Signatures.** Which signing identity do the debug build and the CI APK use? Answer from the repo only: `app/build.gradle.kts` and the `resolveSigningIdentity` it references, the debug keystore `app/debug.keystore`, and `ci.yml`'s build step. Then say whether an `adb install -r` of a debug APK built from this repo would replace the installed package without uninstalling it. Mark it inferred if you can't compare digests directly.
4. **Screen and cut-out.** Physical size and density (`wm size`, `wm density`), the screen size in dp (computed, and marked as computed), and the display cut-out bounds (`dumpsys display` or `dumpsys window displays`, the cutout section).
5. **Camera.** From `dumpsys media.camera`: the camera ids, which is back and which is front, whether the back camera has a flash unit, and the current torch status. Then say whether that dump shows the torch state in a form a later check could read before and after toggling the torch chip.
6. **App data at risk.** For the installed Forager package: its data size (`dumpsys diskstats` or `du` via `run-as`, the latter only if the app is debuggable), and whether `connectedAndroidTest` or an uninstall would wipe it. Answer from the repo's own warning; the pulse report at `docs/audits/2026-09-22-grid-level-completion-report.md` or the stack's run record mentions it, so give file and line.
7. **Settings value.** The current "Automatically Save Location to Photos" value, if it is readable without changing anything. It lives in a DataStore file, so use `run-as <pkg> cat files/datastore/...` only if the package is debuggable. Otherwise say it is not readable.
8. **Test tooling on the phone.** Is `uiautomator dump` available? (`adb shell which uiautomator`, or run the dump to /sdcard and `cat` it; that dump is read-only.) Is a camera-permission grant for Forager already in place (`dumpsys package <pkg>`, runtime permissions for CAMERA and ACCESS_FINE_LOCATION)?

End with **Could not determine**.