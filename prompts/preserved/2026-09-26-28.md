HEAD: 2753a8359067dd1acf944d45f42bbed052e0e0f0
Target subagent: coder
Type: build
Preserved: 2026-09-26T09:01:10Z by .claude/hooks/dispatch_guard.py
--- verbatim prompt follows ---
**Type:** build

# Role

You are the coder. This is Part B of finishing the camera strip. It adds three basic tools to the in-app camera's strip, in order, on one branch:
- **(F)** flash-on-capture inside the existing flash chip;
- **(T)** a self-timer chip;
- **(L)** a save-location chip.

When all three pass, you merge the branch into `pre-main`. You make no design decision this dispatch has not made. Any gap is a stop-and-ask.

# Base and state

Verify each item before relying on it; a mismatch is a stop. The planner read these at `2753a83` with read-only git (`git grep`, `git diff`) unless a line says it came from a pulse.

**Branches and checkouts**
- `origin/pre-main` is at `2753a8359067dd1acf944d45f42bbed052e0e0f0` (merge of #122). `origin/main` is at `76905d4`.
- The main checkout `~/Zynergy/Forager` is on `pre-main` at `2753a83`. The planner worktree `…/.claude/worktrees/bridge-cse_01BcqShzosraMo4pUXkqaqRp` is at `2753a83`, clean except for this dispatch's own store copy. Find that copy by its body; do not assume its number.
- `git diff --stat 6bcbe4c 2753a83 -- app/` is empty. So the pulse's line numbers, read at `6bcbe4c`, still hold for `app/`.

**Record and backups**
- The newest record entries are `2026-09-26-70` to `-74`.
- The merge of #122 (`2753a83`, backup `2026-09-26-06`, dispatch `preserved/2026-09-26-27.md`) has no `merge` entry yet.
- Backups `2026-09-26-01` to `-06` exist.

**Flash, checked by the planner**
- `photo/CameraCaptureSession.kt:84` is `fun setFlashMode(mode: FlashMode)`, `:92` is `internal enum class FlashMode { Off, Torch }`, and `:95` is `FlashMode.next()`.
- `photo/CameraXCaptureSession.kt:339` is `installImageCapture`, `:449` is `setFlashMode`, and `:465` is `installFlash`.

**Flash, from the pulse**
- The pulse inferred that `setFlashMode` (`:449-457`) calls `switch(mode == FlashMode.Torch)` and nothing else. No `ImageCapture` flash mode is set anywhere. The `ImageCapture.Builder` at `:271-273` sets only `CAPTURE_MODE_MINIMIZE_LATENCY`.
- The torch-failure resync at `:485-487` restores only `Torch`/`Off`.
- `close()` (`:299-321`) turns the torch off and resets the mode to `Off` before `unbindAll`.
- `CameraXCaptureSessionShotRotationTest` already installs a real unbound `ImageCapture` through `installImageCapture` and reads it back.
- `FlashChip.kt` reads `session.flashMode` and taps call `setFlashMode(mode.next())`. It has an exhaustive `flashGlyph` (`FlashOff` / "Flash off", `FlashlightOn` / "Torch on").
- `FakeCameraCaptureSession` is generic over the enum.

**Flash test tied to the old cycle, checked by the planner**
- `InAppCameraDialogTest.kt:552` asserts `"five taps from Off end on Torch", FlashMode.Torch`.
- From the pulse: `CameraFlashChipTest.kt:51-63` also expects the Off/Torch cycle.

**Shutter, Back and the ViewModel, checked by the planner**
- `ui/log/InAppCameraDialog.kt:202` is `BackHandler { onDismiss() }`.
- `:461` is `private fun stripChips(session, gridMode, onGridModeChanged)`. It has one call site, `:299` (from the pulse).
- From the pulse: `onShutter` is `:254-279`, with an `isCapturing` guard. `photosTaken`, `captureError` and `isCapturing` are `rememberSaveable` at `:153-155`, and `scope = rememberCoroutineScope()` is at `:156`.
- From the pulse: `InAppCameraViewModel` is Activity-scoped (`MainActivity.kt:148`) and is not passed to the dialog.
- From the pulse: the dialog's parameters are at `:133-150`.
- From the pulse: `Modifier.rotateWithDevice` is at `RotateWithDevice.kt:57`.

**Location path, checked by the planner**
- `ui/availability/AvailabilityViewModel.kt:880` is `fun onAutoSaveLocationToPhotosChanged(enabled: Boolean)`.
- `ui/availability/AvailabilityScreen.kt:2152` is `lockToPortrait = uiState.lockCameraToPortrait,`.
- `ui/log/InAppCameraHost.kt:36` is `typealias InAppCameraSlot`.

**Location path, from the pulse**
- The handler updates `uiState` before the write (`:881`) and only logs a failed write (`:885`).
- `uiState.autoSaveLocationToPhotos` and the handler are both in scope in `AvailabilityScreen` near `:2149` and `:485`.
- The slot lambda has three implementations: `InAppCameraHost.kt:53`, `AvailabilityScreenInAppCameraTest.kt:109` and `InAppCameraHostTest.kt:64`.
- `InAppCameraDialog` has six direct test callers.

**Glyphs, from the pulse**
- `material-icons-extended` comes in through the Compose BOM (`libs.versions.toml:77`, `app/build.gradle.kts:491`).
- `FlashOff`, `FlashlightOn` and `LocationOn` are used in the app today.
- `FlashAuto`, `FlashOn`, `TimerOff`, `Timer3`, `Timer10` and `LocationOff` are used nowhere. That they exist in the artifact is unverified.

**Strip layout, from the pulse**
- `STRIP_ROW_HEIGHT = 48.dp`, `Spacing.sm = 8.dp`, no scrolling.
- Four chips take 216dp. The landscape tests use `h360dp`.
- The portrait width under Robolectric is unverified.

**Last recorded full suite**
- 1630 tests, 0 failures, 24 skipped, at `42e3dd3` (`docs/audits/2026-09-22-grid-level-completion-report.md:179`).
- Measure your own baseline at `2753a83` before any change.

**Nothing else running**
- No other build is running or will merge into `pre-main` while you run.

# Scope boundary

1. **Worktree.** Run `git worktree add ~/Zynergy/forager-wt/strip-flash-timer-location -b strip-flash-timer-location origin/pre-main`. Move your own store copy into it with `mv`, and record its path, size and sha256.
2. **Sweep.** Write one `merge` entry for #122: Merge-commit `2753a83`, Pre-merge `60173da`, Base `pre-main`, Backup `2026-09-26-06`, Merged-by `coder preserved/2026-09-26-27.md`. Take Carries from the diff (the planner expects `-70` to `-74`). The planner accepts the previous coder's reading: unclaimed hook copies sitting in other worktrees are not part of this store and not part of this sweep. Commit and push.
3. **Intent.** Commit the store copy and the intent together, and push.
4. **Baseline.** Run the full unit suite (`./gradlew testDebugUnitTest`) at your branch's base. Record classes, tests, failures and skipped.
5. **F, then T, then L.** Each feature gets tests-only commits first, each run and failing for the reason its message states, then the implementation commits that make them pass. Push after every commit. The behaviour is set under Closed decisions B, C and D.
6. **Docs.**
   - An append-only **B8** addendum at the end of `docs/audits/2026-09-21-camera-strip-basics-decisions.md`, recording A to F below. Mark them as the planner's choices under the owner's delegation, and include the reason for each choice that departs from the abandoned 2026-09-26-02 plan.
   - A completion report, `docs/audits/2026-09-26-strip-flash-timer-location-completion-report.md`, listing the deferred device items.
   - One row in `docs/audits/README.md`.
7. **After-suite and terminal.** Run the full suite again, then write the terminal. Open a PR into `pre-main` and get CI green on its final commit.
8. **Merge.** Run each of these as its own Bash call, never chained:
   - `git fetch origin`
   - confirm `origin/pre-main` is `2753a83`
   - write the backup under the next free `2026-09-26-NN`
   - `gh pr merge <N> --merge`
   - `gh pr view <N> --json state,mergeCommit`

   If history_guard refuses only the form, run it again once, bare. Any other refusal is a stop.
9. **Update checkouts.** Run the merged `update_worktree.py` on the main checkout and on the planner worktree: a dry run, then `--apply`. Report every output. If a dry run exits 1, do not apply.

**Record IDs.** Use only `2026-09-26-80` to `-99`. Before each commit that touches `RECORD.md`, and before the merge, confirm none of them is on `origin/pre-main`.

**Files.** App code and tests under `app/`, the B8 addendum, the completion report, one README row, `RECORD.md` and your store copy. No new dependency and no version change.

**Not touched:**
- `.claude/`, `ci.yml`, `CLAUDE.md`, `check_*.py`
- `main`
- every other worktree
- the phone: no `connectedAndroidTest`, since the stack's run record warns it wipes app data on this phone

# Closed decisions

**Owner rulings, in the planner session, 2026-09-26:**
- "finish the camera to basic functionality. that means populate the camera strip with basic camera tools. dealer's choice on what they are, since they will change in the near future anyway."
- Chose "Land the stack, then extend" and "Use the recorded plan", meaning flash-on-capture, a self-timer and save-location, as the abandoned 2026-09-26-02 scope named them.
- "merge into pre main without device check. defer device check for later".
- Chose "One coder, three features in a row".

Decisions B1 to B7 in the decisions record stand. Everything below is the planner's choice under that delegation.

**A. Strip order from the start: Flash, Timer, Grid, Location.**
- Flash stays hidden without a flash unit, as today.
- Timer and Location are always shown.

**B. Flash: flash-on-capture joins torch in the one chip (extends B2).**
- `FlashMode` becomes `Off, Auto, On, Torch`. A tap cycles Off → Auto → On → Torch → Off.
- Each mode sets two things:

  | Mode | Torch | `ImageCapture` flash mode |
  |---|---|---|
  | Off | off | `FLASH_MODE_OFF` |
  | Auto | off | `FLASH_MODE_AUTO` |
  | On | off | `FLASH_MODE_ON` |
  | Torch | on | `FLASH_MODE_OFF` |

- A mode set before bind is applied to the `ImageCapture` when `installImageCapture` runs.
- It lasts one session only: the mode resets to Off on `close()`. The existing torch-off-before-unbind order stays unchanged.
- No flash unit: `setFlashMode` refuses and logs, as today.
- The torch-failure resync keeps its rule that the reported mode matches the hardware. A successful change to Auto or On is never overwritten by it.
- Glyphs and labels: `FlashOff` "Flash off", `FlashAuto` "Flash auto", `FlashOn` "Flash on", `FlashlightOn` "Torch on".
- `InAppCameraDialogTest.kt:552`, `CameraFlashChipTest`'s cycle tests, and any other test asserting the old two-state cycle change their expected values to the new cycle, because the behaviour changes on purpose. For example, five taps from Off end on Auto. Name each changed assertion in the commit that changes it. That is not silencing a test; weakening or deleting an assertion is still out of bounds.
- Unverified premise, to go in the device items: whether CameraX honours the `ImageCapture` flash mode while the torch is on.

**C. Timer chip, new.**
- **Cycle.** A tap cycles Off → 3 s → 10 s → Off.
- **Lifetime.** One session only, default Off. It is held in the dialog and resets when the camera closes.
- **Starting.** A shutter press with the timer set starts a countdown, and the capture happens once when it reaches zero. The capture goes through the existing `onShutter` capture path unchanged: the `isCapturing` guard, `photosTaken`, `captureError`, `onPhotoCaptured`.
- **Countdown display.** Large numerals, centred over the viewfinder, showing the remaining whole seconds. They are kept upright with `rotateWithDevice` and take no pointer input.
- **Cancelling.** Pressing the shutter again during a countdown cancels it with no capture. During a countdown the shutter's content description is "Cancel timer". Back during a countdown cancels it and closes the camera as today, with no capture afterwards.
- **Changing the timer during a countdown** affects the next press only.
- **Photo count.** It changes only on an actual capture.
- **Flash** applies at the capture.
- **Where the countdown lives.** In a plain Kotlin class `CaptureCountdown` in the `photo` package, with no Compose or Android UI imports. It takes a `CoroutineScope` and is testable headless under virtual time. The dialog holds one instance with `remember`, on its `rememberCoroutineScope()`, so the countdown is cancelled when Back or anything else removes the dialog.
  - This departs from the -02 plan, which put it in the ViewModel. The reasons, all from the pulse: `InAppCameraViewModel` is Activity-scoped and not passed to the dialog. A ViewModel-held countdown survives Back unless it is cancelled explicitly. Hoisting it would touch `MainActivity`, `AvailabilityScreen`, the host and every slot lambda.
  - Record this reason in B8.
- **Glyphs and labels.** `TimerOff` "Timer off", `Timer3` "Timer 3 seconds", `Timer10` "Timer 10 seconds".
- **Test hang pitfall.** Read the CLAUDE.md entry on unstopped poll loops. Every test that starts a countdown ends it inside the test body, in a `finally`.

**D. Location chip: the B3 mirror.**
- It shows and sets "Automatically Save Location to Photos". It holds one state, not a per-shot override.
- **Where it gets its state.** Thread it the way `lockToPortrait` is threaded: `uiState.autoSaveLocationToPhotos` and `onAutoSaveLocationToPhotosChanged`, from `AvailabilityScreen` → `InAppCameraHost` → the `InAppCameraSlot` typealias → `CameraXInAppCamera` → `InAppCameraDialog`. It never writes the repository directly.
- **New parameters.** They are required, with no defaults, so the compiler finds every caller. Update the six test callers and the three slot implementations explicitly.
- **Wording.** The content description is "Save location: On" or "Save location: Off". Per B3's wording rule, no label, comment, commit or document calls it a geotag.
- **Glyphs.** `LocationOn` and `LocationOff`.
- **Known behaviour of the handler.** It updates optimistically, at `:881`, and does not revert when the write fails, at `:885`. Do not change it. Report it under Flags outside scope. The chip shows whatever Settings shows.

**E. Not in this build:**
- pinch zoom and zoom buttons
- tap to focus or meter
- exposure
- a lens switch
- RAW
- aspect ratio
- a last-shot thumbnail
- B7d's crosshair

B8 records them as not built.

**F. Glyph availability.** If any named icon does not resolve at compile time, stop and report which one. Do not substitute.

**Record entries.** Write them with Write or Edit, never with a shell heredoc.

# Prediction

Planner, outcome:
1. **Flash.** Flash-on-capture needs only `ImageCapture.setFlashMode`, carried through `installImageCapture`, plus the enum and the chip. The existing torch tests pass unchanged apart from the cycle assertions named in B.
2. **Timer.** The countdown needs no change above `InAppCameraDialog`.
3. **Location.** The chip needs no new repository and no new DataStore key: one value and callback pair threaded from `AvailabilityScreen`, and no `MainActivity` change.
4. **Suite.** The full unit suite grows by 25 to 50 tests with 0 failures. Skipped stays at the baseline count.
5. **Layout.** Four chips fit within the strip's bounds in portrait and in both landscape arrangements under the existing test qualifiers.

Coder: write your mechanism prediction in the intent.

# Finish line and abort conditions

**Finish line:**
- steps 1 to 9 done and pushed;
- every tests-only commit seen failing for its stated reason, then passing;
- the revert checks done;
- the full suite at 0 failures;
- CI green on the PR's final commit;
- backup written and the PR merged into `pre-main`;
- both checkouts updated;
- both checkers PASS.

Deferral is not allowed.

**Abort, and stop and report, on any of these:**
- a wrong premise;
- a glyph that doesn't resolve (F);
- a suite failure outside this work (report it and don't touch it, per CLAUDE.md);
- a tests-first commit that passes before its implementation;
- a revert check whose build log shows a compile error;
- an ID collision;
- any guard or classifier refusal apart from the one form-only merge retry (quote it; do not route around it);
- CI red;
- `update_worktree.py` dry run exits 1;
- two failed fixes on one symptom.

# Checks

**Entry points (CLAUDE.md, Testing):**
- Drive chips through the real `InAppCameraDialog`.
- Also drive the location chip from `AvailabilityScreen` through the host, as `AvailabilityScreenInAppCameraTest` does for the grid chip.
- Assert on actual state:
  - the `ImageCapture`'s `flashMode`, read back from a real unbound `ImageCapture` via `installImageCapture`;
  - the torch calls;
  - `session.capture` call counts on the fake;
  - the value the location handler receives;
  - Settings' checkbox state where the test reaches Settings.

**Required cases:**
- **Flash:**
  - each of the four modes gives the right `ImageCapture` flash mode and torch state;
  - a mode set before install is applied on install;
  - the mode resets to Off on close;
  - no flash unit means a refused change.
- **Timer:**
  - with 3 s, no capture at 2.9 s of virtual time and exactly one at 3 s; the same for 10 s;
  - cancelling with the shutter gives no capture, ever;
  - Back mid-countdown gives no capture after close;
  - the photo count doesn't change until the capture;
  - changing the timer mid-countdown keeps the running countdown's length;
  - the shutter's content description is "Cancel timer" during a countdown.
- **Location:**
  - a tap on the strip calls the handler with the toggled value and the chip shows it;
  - a changed `uiState` value shows on the strip at the next open.
- **Layout:** in portrait and both landscape arrangements, all four chips sit inside the strip's bounds and don't overlap. State that Robolectric reports zero insets, so this is not evidence about the cut-out.

**Coordinate touch:**
- Give each new chip, and the flash chip after its change, a real `performTouchInput` test at five points across its own bounds over the viewfinder. The existing inline pattern is at `InAppCameraDialogTest.kt:533-553`.
- A semantic `performClick` is wiring, not routing (CLAUDE.md).

**Revert checks.** Do one each for:
- the flash mode reaching `ImageCapture`;
- the timer delay;
- cancel-by-shutter;
- the location chip's call into the handler.

Follow CLAUDE.md's revert rule for each:
- save a copy of the file before editing;
- read the build log for compile errors before reading any result;
- restore from the saved copy, never from git;
- confirm the forward change is still present afterwards.

Quote each failure message and say why it belongs to that revert.

**Suite.** Run the full suite before step 5 and after step 6, and report classes, tests, failures and skipped for both.

**Checkers.** Run both before every commit that touches `RECORD.md`, and quote the final PASS lines.

**CI, backup, merge.** Report the run ids, the backup folder with its `merge.json` and `INDEX.md` line, the merge commit and its parents, and the `update_worktree.py` outputs.

# Out of scope

- Everything in Closed decisions E.
- Fixing the location handler's optimistic update, or anything else outside the files listed. Flag it instead.
- ci.yml's concurrency behaviour.
- The phone, including `connectedAndroidTest`.
- Promotion to `main`.
- Rebase, amend, force-push, delete. Worktrees you add are left in place and listed.

# Device items

None of these run in this dispatch; they are deferred to promotion by owner ruling. List them in the completion report, each with a pass condition:
1. **Four chips in portrait** show clear of the punch-hole.
2. **The same in both landscape arrangements.**
3. **Flash On** fires on capture.
4. **Flash Auto** fires in the dark and not in daylight.
5. **Torch** behaves as before. Say whether Torch plus capture fires the flash.
6. **Timer:** 3 s and 10 s count down and capture once each, and cancelling with the shutter works.
7. **Location:** the chip and Settings agree after a toggle in each direction and after an app restart.
8. **Glyph readability:** the owner's verdict on each new glyph.

# Merge

authorised: this dispatch's PR `strip-flash-timer-location` into `pre-main` only, per coder.md item 10(a). Write the backup first and run the merge command bare. Nothing goes into `main`.

End the hand-back with "Decisions I made" and "Flags outside scope".