HEAD: 188fa3982375db1124e1e11a0035cbd0dbb92646
Target subagent: pulse
Type: pulse
Preserved: 2026-09-26T11:18:11Z by .claude/hooks/dispatch_guard.py
--- verbatim prompt follows ---
**Type:** pulse

## Role

You are the pulse. You are read-only. Answer with `path:line` evidence at `origin/pre-main` (`188fa39`). Change nothing, and do not touch the phone. A device coder is capturing screenshots on the phone at the same time.

## Base and state

- `origin/pre-main` is at `188fa3982375db1124e1e11a0035cbd0dbb92646`. Read it with `git show`/`git grep` against that commit.
- The phone is 384x823dp in portrait, so 823x384dp sideways. It has a centred top punch-hole and a 3-button navigation bar.
- The owner has asked the planner to redesign the whole app in landscape. The owner says it "looks broken" and was "not designed at all". The planner needs a map of the current code before designing anything.

## Rules

- Every claim carries `path:line@188fa39`, or is marked unverified.
- Keep read, observed and inferred apart.
- Be compact. Don't paste whole files.
- Do not quote, summarise, cite or name the Restricted Object.

## Questions

1. **How the layout is chosen.** How does the app decide between its compact and wide layouts? Name the mechanism (WindowSizeClass, a width breakpoint, orientation, or something else), the function, and the threshold values. Then work through the numbers: which layout does an 823x384dp window get, and which does a 384x823dp window get? Is height considered anywhere?

2. **Screen inventory.** List every top-level screen and major surface: the map, search, the drawer, Settings, the Journal tabs and Records sub-tabs, log entry view and edit, the photo viewer, the album or gallery, Cartography, the navigation HUD, the in-app camera, and any others. For each one, say:
   - whether it has a distinct wide or landscape variant, or just reflows the compact one;
   - where the two branch (file:line);
   - what the wide variant arranges differently.

3. **Insets and cut-outs.** How does the app handle system bars and the display cut-out?
   - Where is `enableEdgeToEdge` called?
   - Which `WindowInsets` does each top-level scaffold use: `safeDrawing`, `systemBars`, `displayCutout`, or none?
   - Is anything specific to the side navigation bar or the cut-out when the phone is sideways?
   - List surfaces that consume no insets and draw edge to edge.
   - Cite CLAUDE.md's inset pitfall where it applies.

4. **Orientation handling.** From the manifest: each activity's `screenOrientation` and `configChanges`. Is `requestedOrientation` set anywhere else, for example by the camera's `RequestWindowOrientation` or by the "Lock camera to portrait" setting? Say which surfaces lock their orientation and which follow the window.

5. **Map-specific layout.** Where do these sit, and how are they anchored?
   - the icon cluster (position, snapped side, minimised flag);
   - the compass/elevation strip;
   - the return-to-vehicle row;
   - fullscreen mode;
   - the HUD.

   Which of them assume a portrait-shaped screen, such as hard-coded top or bottom placement, or a width-filling row? Cite CLAUDE.md's `Surface`/`fillMaxWidth` touch-interception pitfall and its UX-defaults entry on remembered cluster position where they bear on this.

6. **Tests.** Which tests run at landscape or wide qualifiers (Robolectric `@Config(qualifiers=...)`, `setScreenSize` or similar)? For each: the class, the qualifier, and what it asserts. Which surfaces have no landscape test at all?

7. **Decisions already recorded.** Search `docs/audits/`, `docs/` and the CLAUDE.md UX defaults for any recorded decision or known issue about landscape, wide layout, rotation, the side nav bar, or the cut-out. Give each document with a one-line quote of the decision.

End with a section headed **Could not determine**.