HEAD: b070a2e378526e8e993734f859c687ee71746367
Target subagent: coder
Type: build
Preserved: 2026-09-26T05:37:59Z by .claude/hooks/dispatch_guard.py
--- verbatim prompt follows ---
**Type:** build

# Role

You are the coder. This dispatch removes the operator-approval prompt for `build` and `device` dispatches in Forager by changing `.claude/kit.json`, then merges that change and brings the live checkouts up to it so the gate stops asking. You make no decision this dispatch has not made; a gap is a stop-and-ask.

# Base and state

Verify each item before relying on it. A mismatch is a stop.

- **`origin/main`.** At `76905d4993813caaead1caad5db6fd18da5c97e8` (PR #119 merge). If it has moved, say so, and state whether the new commits touch `.claude/`.
- **Planner worktree.** `/home/zynergy-labs/Zynergy/Forager/.claude/worktrees/bridge-cse_01BcqShzosraMo4pUXkqaqRp`, on branch `worktree-bridge-cse_01BcqShzosraMo4pUXkqaqRp` at `b070a2e`: terminal 2026-09-26-27, pushed, with a record-only PR open. `git status --short` is empty, apart from this dispatch's own store copy, expected at `prompts/preserved/2026-09-26-16.md`.
- **The gate.**
  - `.claude/kit.json:15` reads `"approval_exempt_types": ["pulse"]`.
  - `.claude/hooks/dispatch_guard.py:263-269` returns `allow` for a listed type and `ask` for every other one.
  - Premise to check: `.claude/kit.json` is not among the files hashed in `.claude/kit.lock`, so changing it causes no kit drift. Confirm with `check_kit.py` after the edit.
- **Hook tests.** 179 hook tests passed at the kit install (`6d686ad`). Re-run them at the base before changing anything and report the count.
- **Merging.** Premise to check: the vendored v0.2 `history_guard` permits `gh pr merge` once coder.md item 10's backup is written. Forager's older guard blocked it for every role (RECORD.md 2026-09-23-02). If it still blocks, that is an abort, not something to work around.

# Scope boundary

1. **Worktree and branch.** A new worktree from `origin/main`, via `git worktree add`, on a new branch `kit-approval-exempt-build-device`.
2. **The edit.** `.claude/kit.json` gets `"approval_exempt_types": ["pulse", "build", "device"]`. No other key changes.
3. **The decision note.** A new `docs/audits/2026-09-26-approval-exempt-build-device.md`, holding:
   - the owner's ruling, quoted
   - what the gate was, and what still gates builds and device work after this change (role_guard, device_guard, history_guard, the dispatch_guard section check and preservation)
   - the alternatives the owner rejected: keeping `device` gated, and changing the default in Claude-kit upstream
   - the stale wording in vendored files listed under Checks
4. **Index.** One row for that note in `docs/audits/README.md`, keeping every existing row.
5. **PR.** A PR against `main`. Merge it per coder.md item 10(a), with the backup first.
6. **Live checkouts.** After the merge, update the main checkout and the planner worktree above with `update_worktree.py`: a dry run for each, then `--apply`. Report every output.
7. **Record.** On the planner worktree's branch:
   - the sweep, which claims only unclaimed store files
   - this dispatch's store copy with its intent
   - the terminal
   Push after each. The next sweep records the merge entry, not this dispatch.

Not touched:
- any hook script or test
- `.claude/settings.json`
- `.claude/kit.lock`
- `.claude/agents/*`
- `CLAUDE.md`
- Claude-kit's own repository
- app code
- the strip branches
- the phone

# Closed decisions

The owner, in the planner session, 2026-09-26:

1. "Mark my approval in advance". This followed the planner's plan for five coder dispatches on the camera strip.
2. "Can't have me hitting buttons. Those hooks need to change".
3. Asked which types should run without approval, the owner chose **"build and device"**. The option was worded: "No prompts at all. The other guards still apply: role_guard, device_guard, and history_guard, which blocks merges without a backup, force-pushes and the like. Nothing asks before a coder touches the phone."

The owner rejected two alternatives: build only with device still asking (the planner's recommendation), and changing the default in Claude-kit upstream.

**The merge is authorised** for this PR only, so the change takes effect without the owner pressing anything. The planner derives that from ruling 2. The owner's approval of this dispatch is where it gets confirmed.

**Record entries.** Written with Write or Edit, never a shell heredoc.

# Prediction

Planner, outcome:
1. **The edit alone.** Changing only `approval_exempt_types` makes dispatch_guard return `allow` for a well-formed `build` or `device` dispatch, with the reason text naming approval_exempt_types. No code change is needed.
2. **Tests and drift.** The hook tests pass unchanged, at the same count as the base. `check_kit.py` still reports PASS, because kit.json is not locked.
3. **Merge.** The merge goes through the v0.2 history_guard once the backup is written.

Coder: write your mechanism prediction in the intent.

# Finish line and abort conditions

**Finish line.**
- The PR is merged.
- The main checkout and the planner worktree are updated, and each one's `.claude/kit.json` shows the new list.
- The note and the README row are on main.
- The backup folder is written and cited in the terminal.
- The terminal is pushed, and both checkers pass.

**Deferral is not allowed.**

**Abort, and stop and report, on any of these:**
- a wrong premise
- a hook test failing at the base, or after the edit
- `check_kit.py` failing
- any guard blocking a step: quote its text
- `update_worktree.py` exiting 1
- two failed attempts at one step

# Checks

- **Positive control, failing first.**
  - Drive `dispatch_guard.guard()` with a well-formed `build` payload and a well-formed `device` payload in a temporary repository. Use the existing `.claude/hooks/tests/harness.py`, or build one the same way, never the live store: no store file may appear under the real `prompts/preserved/` from this check.
  - At the base, both return `ask`. After the edit, both return `allow`, and a `pulse` still returns `allow`.
  - An unknown type, and a missing section, still `deny`.
  - Quote the decision and reason for each.
- **Hook tests.** Run the hook suite at the base and after the edit; report both counts.
- **Kit drift.** Show the `check_kit.py` output after the edit.
- **Stale wording.** `git grep -n -i "approv"` across `.claude/`, `CLAUDE.md` and `docs/process/`. List every line whose wording now overstates the gate. For example, the coder agent description says "the operator approves it before it runs". List them in the note; do not edit them. Vendored files change only through the kit.
- **Backup.** The folder path, `merge.json` and the `INDEX.md` line.
- **Checkers.** Both run before every commit that touches `RECORD.md`; quote the final PASS lines.

# Out of scope

- Editing any hook, test, agent file or `CLAUDE.md`, including the stale wording found above.
- The camera-strip work.
- Any other PR's merge, including the planner worktree's record-only PR.
- Rebase, amend, force-push, delete.

# Device items

None. This dispatch does not touch the phone.

# Merge

authorised: this dispatch's PR `kit-approval-exempt-build-device` into `main`, per coder.md item 10(a), with the backup first. No other PR.

End the hand-back with "Decisions I made" and "Flags outside scope".