HEAD: d3e73825542cdded9668cda3dfe4ab994ed20161
Target subagent: coder
Type: build
Preserved: 2026-09-26T05:28:05Z by .claude/hooks/dispatch_guard.py
--- verbatim prompt follows ---
**Type:** build

# Role

You are the coder. This dispatch brings the planner's worktree up to `origin/main` and re-files two store copies whose names collide with files already on `main`. It builds no app code. It is housekeeping that the camera-strip build waits on. You make no decision this dispatch has not made; a gap is a stop-and-ask.

# Base and state

Verify each item against the remote and the disk before relying on it. A mismatch is a stop.

- **Worktree.** `/home/zynergy-labs/Zynergy/Forager/.claude/worktrees/bridge-cse_01BcqShzosraMo4pUXkqaqRp`, on branch `worktree-bridge-cse_01BcqShzosraMo4pUXkqaqRp`.
  - HEAD is `d3e73825542cdded9668cda3dfe4ab994ed20161`, the merge of PR #118.
  - The branch is not on the remote: `git ls-remote origin` lists no such ref.
- **`origin/main`.** At `76905d4993813caaead1caad5db6fd18da5c97e8`, the merge of PR #119. `gh api compare` reports d3e7382 as 5 behind and 0 ahead.
- **Untracked files expected in the worktree.** There are two, and there should be nothing else untracked:
  1. `prompts/preserved/2026-09-26-11.md`: a pulse. Header `HEAD: d3e7382…`, `Target subagent: pulse`, `Preserved: 2026-09-26T05:21:16Z`.
  2. `prompts/preserved/2026-09-26-12.md`: this dispatch's own copy, written by dispatch_guard when it was sent.
- **Store files already on `main` under those names.**
  - `origin/main` already holds `prompts/preserved/2026-09-26-11.md`, `-12.md` and `-13.md` (from PR #119).
  - Main's `-11` has blob sha `6f24968b1a695440557775d94f51771ae3a62c52`, a different file from the untracked `-11`.
  - Premise to check: main holds no `2026-09-26-14.md` or higher.
- **Why the names collide.** The shared dispatch counter under the git common dir did not know about the store names on `main`. This dispatch does not fix that (see Out of scope).

# Scope boundary

In this worktree only:
1. **Move the untracked files aside.** Move both untracked store files out of the repository, to `~/Zynergy/forager-held-store/`. Before moving each one, record its original path, its size in bytes and its sha256.
2. **Fast-forward.** Run `git pull --ff-only origin main`. Confirm HEAD is `76905d4`.
3. **Sweep.** Carry out coder.md item 1 against the updated tree.
   - The pulse copy is re-filed into `prompts/preserved/` under the store's next free 2026-09-26 number. It is claimed by a `dispatch-note`: Type pulse, Outcome answered, Report "answered in the planner's session; not relayed to the record". The note states the original name, size, sha256 and the owner's ruling below.
   - Write any `merge` entries that coder.md item 1 and `docs/audits/2026-09-26-recordkeeping-protocol-shift.md` require for merges into `main` after `af12a69` that the record does not yet hold.
   - Commit and push.
4. **Intent.** This dispatch's copy is re-filed under the next free number after that, in one commit together with its intent. The intent's Notes record the original name, size, sha256 and the ruling.
5. **Terminal.** Close the intent with a terminal. Push the branch with `git push -u origin worktree-bridge-cse_01BcqShzosraMo4pUXkqaqRp` and open a record-only PR against `main`.

Files touched: `RECORD.md` and `prompts/preserved/` only.

Not touched:
- app code
- hooks
- `CLAUDE.md`
- the kit files
- the dispatch counter under the git common dir
- any other worktree or branch, including the strip branches
- the phone

# Closed decisions

**Owner, in the planner session, 2026-09-26.** The owner chose "I update the worktree first". The planner laid that option out as follows:

> You move the untracked pulse copy out of prompts/preserved/ and bring this worktree to origin/main… The coder's sweep re-files the pulse copy under the next free number, recording its original name, size and sha256 (item 5's pattern), with your ruling quoted.

Then the owner said: "Have the coder run that."

**This dispatch's own copy.** Sending this dispatch from the stale worktree necessarily saved it as a colliding `-12`. The planner applies the same ruling to it: re-file it under the next free number, with provenance. That is the only extension of the ruling. The planner will report it to the owner as the planner's call.

**Record entries.** Write them with the Write or Edit tools, never with a shell heredoc. A heredoc record entry tripped history_guard in the dispatch preserved as `2026-09-26-02` ("could not parse this push (No closing quotation)").

# Prediction

Planner, outcome:
1. **Fast-forward.** The pull fast-forwards cleanly to `76905d4` once the two files are moved out. There are no conflicts and no merge commit.
2. **Next free names.** Main's store holds nothing numbered above `2026-09-26-13`. The pulse therefore becomes `2026-09-26-14` and this dispatch `2026-09-26-15`.
3. **Checkers.** `check_record.py` and `check_prompts.py` pass after each commit that touches `RECORD.md`.
4. **CI.** CI on the record PR is green or runs no app build.

Coder: write your mechanism prediction in the intent, before the fast-forward.

Note on the order: coder.md puts the sweep first. Here the sweep has to run on the updated tree, or it would sweep a stale store. So steps 1 and 2 come before the sweep and change no tracked file. Say so in the intent.

# Finish line and abort conditions

**Finish line.**
- The worktree is at or past `76905d4`, with the sweep, the intent and the terminal pushed.
- Both copies are re-filed with provenance recorded.
- Both checkers pass.
- The record-only PR is open.
- `git status --short` in the worktree is empty.

**Deferral is not allowed.**

**Abort, and stop and report, on any of these:**
- a premise under Base and state is wrong
- an untracked file other than the two named
- the pull is not a fast-forward
- `main` holds a `2026-09-26-14.md` or higher, or the next free name is otherwise not what item 2 predicts
- a checker fails for a reason outside these entries
- any guard blocks a step: quote the guard's text
- two failed attempts at one step

# Checks

- **Before moving.** Show `git status --short` and the `ls prompts/preserved/2026-09-26-*` listing, both before the move and after the pull.
- **Round trip of each copy.** The sha256 of each file before the move equals the sha256 of its re-filed copy. Show both values.
- **Header dates.** Each re-filed copy's `Preserved:` date matches its new name's date. `check_prompts.py` checks this; quote its output.
- **Checkers.** Run both before every commit that touches `RECORD.md`, and quote the final PASS lines.
- **What was pushed.** `git log --oneline 76905d4..HEAD` and `git ls-remote origin worktree-bridge-cse_01BcqShzosraMo4pUXkqaqRp` after the final push.

# Out of scope

- Fixing or changing the shared dispatch counter, or any kit file. The counter not seeing names already on the remote is to be reported under Flags outside scope, with what you observe about it.
- The camera strip work, PRs #111 to #113, and any merge into `main`.
- Deleting anything: `~/Zynergy/forager-held-store/` keeps nothing after the re-file, but do not delete the directory.
- Rebase, amend, force-push, reset.

# Device items

None.

# Merge

Not authorised. The owner merges the record PR.

End the hand-back with "Decisions I made" and "Flags outside scope".