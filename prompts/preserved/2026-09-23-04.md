HEAD: 624173b5fec0c254aa7bebf5584e29ee8ef50448
Target subagent: coder
Type: build
Preserved: 2026-09-23T04:07:14Z by .claude/hooks/dispatch_guard.py
--- verbatim prompt follows ---
**Type:** build

# Resume: merge `d55-artifact-contract` into main in slayer8366/forager-forecast

## Role

You are the coder. You carry out one authorised merge in **forager-forecast** and record it in Forager `RECORD.md`. You make no design decisions. Any gap is a stop-and-ask.

## Base and state

A first coder ran this merge from preserved dispatch `prompts/preserved/2026-09-23-02.md` and stopped before touching forager-forecast. It left this state in the Forager worktree `/home/zynergy-labs/Zynergy/Forager/.claude/worktrees/bridge-cse_01Md1NYxk9qgG8y6g7CiSm3y`. Verify all of it before acting:

- `RECORD.md` has intent entry `2026-09-23-05` appended and uncommitted. Its `Dispatch-file` names `prompts/preserved/2026-09-23-02.md`.
- Two preserved prompts are untracked:
  - `prompts/preserved/2026-09-23-02.md` is the first merge dispatch.
  - `prompts/preserved/2026-09-23-03.md` is a planner pulse, "current state of every input to the D32 follow-up task".
- There is also this dispatch's own preserved prompt, the next number after `03`. Confirm its filename from the store.
- `check_prompts.py` failed on the unclaimed `03`.

The first coder checked the premises read-only in `~/Zynergy/forager-forecast-t0b`:
- `origin/main` = `82f28b65e1c46148c0518e30b411428ee0a7e2bb`.
- `origin/d55-artifact-contract` = `2d8cc8f9988899c5cc9a0d31e89147a0bbfe216f`, 0 behind and 5 ahead.
- The two differ in exactly 6 files: `docs/audits/2026-09-20-proposed-decision-d55-artifact-contract.md`, `docs/audits/2026-09-21-d55-vetting-report.md`, `docs/audits/README.md`, `docs/planning/DECISIONS.md`, `docs/planning/START_HERE.md`, `docs/planning/TASKS.md`.
- D50 at `docs/planning/DECISIONS.md:12` says the merge commit message is the D40 report when it records the authorisation, its wording and its time.
- The t0b clone is on `d55-artifact-contract` and clean.

Re-verify the two tips before merging; anything may have moved since.

`~/Zynergy/forager-forecast` belongs to another session. Do not write into it.

## Scope boundary

**In Forager, on this worktree's branch:**
1. The sweep: one `dispatch-note` for `2026-09-23-03` (content under Closed decisions).
2. Commit intent `2026-09-23-05` as written.
3. Your own intent for this dispatch, claiming this dispatch's preserved prompt.
4. A terminal entry closing `05` as `superseded`, with `Superseded-by` set to your intent's ID.
5. Your own terminal entry at the end.
6. Push with `git push -u origin worktree-bridge-cse_01Md1NYxk9qgG8y6g7CiSm3y`. Creating the remote branch is intended.
7. Do not merge that branch into Forager main.

**In forager-forecast:**
- One `--no-ff` merge commit of `d55-artifact-contract` into `main`, made in a new temporary worktree of the t0b clone cut from `origin/main`, then pushed to `origin/main`.
- Remove the temporary worktree afterwards.
- No other edit to any file there.

## Closed decisions

- **D40 authorisation.** The owner wrote in chat on 2026-09-22: "I authorize merging d55-artifact-contract into main". The merge message quotes it exactly.
  - Time wording: "given in chat on 2026-09-22; recorded as about 20:49 PDT, when the first merge dispatch reached the coder". That is 2026-09-23T03:49:10Z, from 02's Preserved line; confirm it there.
- **Record home.** The owner ruled that forager-forecast dispatches are recorded in Forager `RECORD.md`, following the coder protocol as written.
- **Pulse `03` note.** The owner ruled: `Outcome: answered`, `Report: none: the pulse ended without a hand-back; the planner received no report`.
- **Intent `05`.** The owner ruled: commit it as written, then close it with a terminal entry, `Outcome: superseded`, `Superseded-by: <your intent ID>`. Do not edit `05`'s text.
- **Merge form.** `--no-ff`. Keep every index row.

## Prediction

- The merge is clean.
- The merge commit's tree equals `2d8cc8f^{tree}`.
- `git diff --name-only 82f28b6 <merge>` lists exactly the 6 files.
- The highest DECISIONS.md row on the result is D60.
- The index row count on the result equals the branch's count.
- Both Forager checkers pass once `03`, `05`, `02` and your prompt are all claimed.

If any of these does not hold, a premise was wrong. Stop.

## Finish line and abort conditions

**Finish line:**
- `origin/main` in forager-forecast is a merge commit with parents `82f28b6…` and `2d8cc8f…`.
- The temporary worktree is removed.
- In Forager, every entry above is committed, both checkers pass, and the branch is pushed.
- You have written a report.

**Abort, and report without going further, if any of these happens:**
- Either tip has moved.
- The merge conflicts.
- The tree check fails.
- The secret check hits.
- A checker fails for a reason other than the four prompts above.
- The push is rejected. Never force-push.
- Another new unclaimed prompt appears in `prompts/preserved/`. Stop and report it; do not write a note for a dispatch whose outcome you have not seen.

## Checks

Quote the output of each:

1. `git fetch origin`, then `git rev-parse origin/main origin/d55-artifact-contract`.
2. `git merge-base --is-ancestor origin/main origin/d55-artifact-contract`.
3. After the merge:
   - `HEAD^{tree}` vs `2d8cc8f^{tree}`.
   - `git diff --name-only 82f28b6 HEAD`.
4. The table-row count of `docs/audits/README.md` at `82f28b6`, at `2d8cc8f` and on the result.
5. The highest D-row line in `docs/planning/DECISIONS.md` on the result, quoted.
6. Secret check:
   - In a subshell, source `~/.config/forager-forecast/gbif.env`.
   - Run `grep -rF -c` for the literal `GBIF_PWD` value over the tracked files of the result.
   - Report counts only, never the value. Expected: 0.
7. After the push, `git ls-remote origin main` equals the merge commit.
8. In Forager, `check_record.py` and `check_prompts.py` pass before each commit touching `RECORD.md`. Quote their final output.

## Out of scope

- The D32 follow-up task.
- TASKS.md's stale header and T1 line.
- Deleting branches.
- Any file in forager-forecast.
- Changing `check_record.py`'s outcomes.
- The other session's clone.

## Device items

None.

End the report with **Decisions I made** and **Flags outside scope**.