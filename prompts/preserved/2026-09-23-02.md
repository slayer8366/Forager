HEAD: 624173b5fec0c254aa7bebf5584e29ee8ef50448
Target subagent: coder
Type: build
Preserved: 2026-09-23T03:49:10Z by .claude/hooks/dispatch_guard.py
--- verbatim prompt follows ---
**Type:** build

# Merge `d55-artifact-contract` into main in slayer8366/forager-forecast

## Role

You are the coder. Carry out one authorised merge in the **forager-forecast** repository (not Forager) and record it. You make no design decisions. Any gap is a stop-and-ask.

## Base and state

Premises, all read by the planner through `gh api` on 2026-09-22. Verify each against the remote before acting:

- forager-forecast `main` = `82f28b6` ("Merge planner-handoff-2026-09-20 into main").
- `d55-artifact-contract` = `2d8cc8f`, **5 ahead, 0 behind** main. It touches exactly 6 files: `docs/audits/2026-09-20-proposed-decision-d55-artifact-contract.md`, `docs/audits/2026-09-21-d55-vetting-report.md`, `docs/audits/README.md`, `docs/planning/DECISIONS.md`, `docs/planning/START_HERE.md`, `docs/planning/TASKS.md`.
- From planner memory, **not re-read**: DECISIONS.md highest row is D54 on main and D60 on the branch.
- Local clones:
  - `~/Zynergy/forager-forecast-t0b` is the main clone. From memory, not verified, it is checked out on `d55-artifact-contract`, with `core.hooksPath=.githooks`.
  - `~/Zynergy/forager-forecast` belongs to another session. Do not write into it.
- Record side: this Forager worktree (`/home/zynergy-labs/Zynergy/Forager/.claude/worktrees/bridge-cse_01Md1NYxk9qgG8y6g7CiSm3y`) on its current branch. Forager `RECORD.md` holds the entries (owner ruling below).

## Scope boundary

- **In forager-forecast:** one no-ff merge commit of `d55-artifact-contract` into `main`, pushed to `origin/main`. Do it in a new temporary worktree of the t0b clone, cut from `origin/main`. Remove that worktree afterwards.
- **In Forager:** the protocol's sweep, intent and terminal entries in `RECORD.md` on this worktree's branch, pushed to that branch. Do not merge that branch into Forager main.
- Nothing else. Do not edit a single file in forager-forecast.

## Closed decisions

- **Authorisation (D40).** The owner wrote in chat on 2026-09-22: "I authorize merging d55-artifact-contract into main". That names the branch, so D40 is met.
  - The merge commit message must quote those words exactly and give a time. D50 makes the merge message the record; verify that against D50's row on main before writing the message.
  - The planner has no clock. Record the time as "about <time>, when the dispatch reached the coder", in the same form as the times in `docs/audits/2026-09-20-d32-stage-2b-report.md`.
- **Record home.** The owner ruled on 2026-09-22 that the record of forager-forecast dispatches goes in Forager `RECORD.md`, following the coder protocol as written.
- **Merge form: `--no-ff`**, as with every earlier merge into forecast main (1d27f08, 49a5ed5, bc7863a, 42f0743, 82f28b6).
- **Every index row is kept** (the standing rule). Since the branch is 0 behind, no conflict is expected.

## Prediction

- The merge is clean, with no conflicts.
- The merge commit's tree is **identical** to `2d8cc8f^{tree}`, because main has not moved since the branch was cut.
- `git diff 82f28b6 <merge>` names exactly the 6 files above.
- DECISIONS.md on the result has D60 as its highest row.
- If any of these fail, a premise was wrong. Stop; do not work around it.

## Finish line and abort conditions

**Finish line:**
- `origin/main` is the new merge commit, with parents `82f28b6` and `2d8cc8f`.
- The temporary worktree is removed.
- The `RECORD.md` entries are committed and pushed, and both checkers pass.
- A short report exists.

**Abort, touch nothing further, and report if:**
- `origin/main` is not `82f28b6`, or `origin/d55-artifact-contract` is not `2d8cc8f`.
- The merge conflicts.
- The tree check fails.
- The secret check (below) finds anything.
- D50 does not say the merge message is the record. Report what it does say.
- The push is rejected. Do not force-push.

## Checks

Run and quote the output of each:

1. `git fetch origin`, then `git rev-parse origin/main origin/d55-artifact-contract`. Both must equal the base above.
2. `git merge-base --is-ancestor origin/main origin/d55-artifact-contract` succeeds.
3. After the merge:
   - `git rev-parse HEAD^{tree}` equals `git rev-parse 2d8cc8f^{tree}`.
   - `git diff --name-only 82f28b6 HEAD` lists exactly the 6 files.
4. Audit index rows: count the table rows in `docs/audits/README.md` on `82f28b6`, on `2d8cc8f` and on the result. The result must equal the branch count, and the branch count must be at least main's.
5. Highest D-row in `docs/planning/DECISIONS.md` on the result. Quote the line.
6. **Secret check.** Source `~/.config/forager-forecast/gbif.env` inside a subshell. Grep the merge result's tracked files for the literal values of `GBIF_PWD` only, with `grep -rF -c` and output suppressed except counts. Report the counts only. **Never print any value.** The expected count is 0.
7. After the push, `git ls-remote origin main` equals the merge commit.
8. Forager: `check_record.py` and `check_prompts.py` pass before each commit that touches `RECORD.md`.

## Out of scope

- The D32 follow-up task. The planner is drafting it separately.
- Refreshing TASKS.md's stale "As of 2026-09-18" header, and T1's "waits on the weather-pull ruling" line.
- Deleting any merged branch.
- Any edit to a file in forager-forecast.
- Anything in the other session's clone.

## Device items

None.

Report as usual, ending with **Decisions I made** and **Flags outside scope**.