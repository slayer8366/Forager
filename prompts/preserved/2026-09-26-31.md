HEAD: 188fa3982375db1124e1e11a0035cbd0dbb92646
Target subagent: coder
Type: device
Preserved: 2026-09-26T11:17:57Z by .claude/hooks/dispatch_guard.py
--- verbatim prompt follows ---
**Type:** device

# Role

You are the coder. This dispatch captures how the app looks today with the phone sideways, as evidence for a landscape redesign the planner is about to write. You make no app changes. You make no design judgements beyond describing what is on screen. The owner is away from the phone. If a step needs a decision, record it as not run, give the reason, and carry on. Stop outright only on the abort conditions below.

# Base and state

Check every premise before relying on it. A wrong premise gets recorded. It is an abort only where the abort list says so.

**Branch and record state**
- `origin/pre-main` is at `188fa3982375db1124e1e11a0035cbd0dbb92646`: the merge of #124, the strip device-check run record.
- The main checkout and the planner worktree are both at `188fa39`.
- #124's merge has no `merge` entry yet. Backup `2026-09-26-08`, dispatch `preserved/2026-09-26-30.md`. Take Carries from the diff; the planner expects `-83` to `-86`.
- The next free backup is `2026-09-26-09`.

**Parallel dispatch**
- A read-only code pulse is sent at the same time. Its store copy lands in the planner worktree.
- A copy that appears after yours belongs to the next sweep (coder.md item 9). `check_prompts.py` may fail naming only such copies. Record which ones appeared.

**The phone**
- Only the S22 Ultra, serial `R5CT321008R`. Use `-s R5CT321008R` or `ANDROID_SERIAL` on every call. The owner's S26 must never be touched.
- Installed: `com.zynergylabs.forager.app` `1.0.899+g429edb96`. The app code is identical at `188fa39`, which added only docs.
- Screen: 1080x2316 px, density 2.8125, 384x823 dp.
- Portrait punch-hole: `Rect(512,0,568,75)`. At rotation 1 it was `Rect(0,512,75,568)`.
- 3-button nav bar.
- Settings recorded by the last run:
  - auto-rotate (`accelerometer_rotation`): 0
  - `user_rotation`: 0
  - "Lock camera to portrait": off
  - "Automatically Save Location to Photos": on, stored explicitly

**What the last run established**
- `adb shell settings put system user_rotation <n>` with auto-rotate off rotates the app's screens. The camera follows the physical phone by design.
- `input tap`, `input swipe`, `run-as`, `uiautomator dump` and `screencap` were all allowed to the coder role.
- device_guard refuses relative APK paths.

**The owner's rulings on the camera**
- With auto-rotate off, the camera following the phone's orientation is by design, and so is its effect on `user_rotation`.
- "Lock camera to portrait" is the way to stop it.

**Privacy precedent (the last run's decision 5)**
- Screenshots that show the phone's grid reference, map position or other location data are not committed.

# Scope boundary

1. **Worktree**
   - Create it: `git worktree add ~/Zynergy/forager-wt/landscape-capture -b landscape-capture origin/pre-main`.
   - Unset the upstream, then push with `-u` to the new branch.
   - `mv` your store copy into it, and record the copy's path, size and sha256.
2. **Sweep.** Write the `merge` entry for #124, commit it, and push.
3. **Store copy and intent.** Commit both together, and push.
4. **Capture.** Before starting, record every setting you will change.
   - Capture each surface below at `user_rotation` 1 and at 3, and once at 0 (portrait) for comparison.
   - For each capture, save a PNG from `adb exec-out screencap -p` and a `uiautomator dump`.
   - Also save `dumpsys window displays` cut-out and inset lines, once per rotation.
   - Navigate with taps; find the route from the UI dumps.
   - Surfaces:
     - the main map screen as it opens
     - the map with the icon cluster or panels expanded, if there is such a state
     - map fullscreen mode, if reachable, and its exit
     - search, with the dropdown open
     - the tools or settings drawer
     - Settings
     - each Journal tab, and each Records sub-tab
     - an existing log entry opened for viewing and for editing
     - the photo viewer, opened on an existing photo
     - the Album or Photo Gallery
     - the Cartography screen, and an entry in it
     - the navigation HUD, only if reachable without starting a recording or creating data (otherwise not run)
     - the in-app camera with "Lock camera to portrait" on (the owner named this as the way to hold it still), then turned back off
     - any other top-level screen the bottom nav or drawer reaches
   - Do not create, edit, save or delete app data. Open existing items read-only, and back out of edit screens without saving. If a surface can only be reached by creating data, record it as not run.
   - Restore every setting afterwards, with `user_rotation` back to 0. Record each change and each restore.
5. **Where the evidence goes**
   - All captures go to `~/Zynergy/forager-landscape-capture/`, outside the repo, with a `MANIFEST.md` listing each file: surface, rotation, time.
   - The planner reads them from there.
   - Commit into `docs/audits/assets/2026-09-26-landscape-capture/` only the captures that show no location data, and say which you left out and why.
6. **Capture record**
   - Write `docs/audits/2026-09-26-landscape-capture-record.md`. For each surface and rotation, record:
     - whether it was captured, or not run with the reason
     - what is visibly wrong, described factually and cited to the dump bounds (clipped, overlapping, under the cut-out or nav bar, off-screen, cramped, unused space)
     - no proposals
   - Add one README row.
7. **Terminal and PR**
   - Write the terminal, then open a PR into `pre-main`. CI must be green on its final commit.
8. **Merge.** Run each of these as its own Bash call, never chained:
   - `git fetch origin`
   - confirm `origin/pre-main` is `188fa39`
   - write backup `2026-09-26-09`
   - `gh pr merge <N> --merge`
   - `gh pr view <N> --json state,mergeCommit`

   If history_guard refuses the merge only for its form, retry once in bare form.
9. **Update checkouts.** Run the merged `update_worktree.py` on the main checkout and on the planner worktree: a dry run first, then `--apply`.

**Record IDs.** Use only `2026-09-26-87` to `-99`. Before each `RECORD.md` commit, and before the merge, check that none of them is on `origin/pre-main`.

**Files.** Only the capture record, its non-location assets, one README row, `RECORD.md` and your store copy. No app or test changes. No install.

**Phone**
- Allowed: taps, swipes, rotation, and the camera-lock setting, each restored afterwards.
- Never: install, uninstall, `pm clear`, clearing data, or creating, editing or deleting app data.

# Closed decisions

The owner, in the planner session, 2026-09-26:
- "we do need to redesign the sideways feature. Since the phone is rotated sideways idle, how about you design the sideways layout?"
- Scope chosen: "The whole app in landscape".
- Problem chosen: "Looks broken" and "Not designed at all".

The planner is writing the design, and this dispatch is its evidence. The planner's calls:
- Location-bearing captures stay outside the repo.
- No data is created.

**Record entries.** Write them with Write or Edit, never with a shell heredoc. The same applies to the README row.

# Prediction

Planner, outcome:
1. Every top-level surface is reachable by taps at both landscape rotations without creating data. The navigation HUD may not be.
2. On at least one surface, content sits under the cut-out, or behind the side navigation bar, at one of the two rotations. This is inferred from the owner's "looks broken" and CLAUDE.md's inset pitfall.
3. Every changed setting restores cleanly.

Coder: write your mechanism prediction in the intent.

# Finish line and abort conditions

**Finish line**
- Steps 1 to 9 are done.
- Every surface has been captured, or recorded as not run.
- Every setting is restored.
- The PR is merged with a backup.
- Both checkers pass.
- Both checkouts are updated.

**Abort, stop and report, on any of these:**
- any device other than `R5CT321008R` would be touched
- a command would clear, create, edit or delete app data
- the app crashes and does not relaunch
- a setting cannot be restored
- an ID collision
- a guard or classifier refusal on a repository step, apart from the one form-only merge retry. A refused phone command is "not run".
- CI red
- two failed attempts at one step

# Checks

- **Settings.** Show the value of every setting you changed before, during and after, and confirm it was restored.
- **Manifest completeness.** The number of files in `MANIFEST.md` equals the number of files in the capture directory.
- **Checkers.** Run both before every `RECORD.md` commit, and quote the final PASS lines.
- **CI, backup, merge.** Give the CI run id, the backup folder, its `merge.json` and `INDEX.md` line, the merge commit and its parents, and the `update_worktree.py` outputs.

# Out of scope

- Designing or proposing any layout.
- Fixing anything.
- Installing a build.
- Items deferred to beta testing.
- The S26.
- Rebase, amend, force-push, delete. Worktrees you add are left in place.

# Device items

This dispatch is the device run. It captures and does not judge. Nothing here is a pass or fail gate for the app.

# Merge

authorised: the capture-record PR `landscape-capture` into `pre-main` only, per coder.md item 10(a), with the backup first and the merge command bare. Nothing into `main`.

End the hand-back with "Decisions I made" and "Flags outside scope".