HEAD: 429edb96a11b8cd2ff29e9682d25b0ca79b004a7
Target subagent: coder
Type: device
Preserved: 2026-09-26T10:04:15Z by .claude/hooks/dispatch_guard.py
--- verbatim prompt follows ---
**Type:** device

# Role

You are the coder. You install the camera-strip build from `pre-main` on the S22 Ultra test phone and run the five device checks that need no human. You record the results as a run record, and merge that record into `pre-main`. The owner is away from the phone.

You make no decision this dispatch has not made. When something needs a decision, record the step as not run with the reason, and continue with the steps that don't depend on it. Asking cannot be answered while the owner is away, so do not stop to ask. Stop outright only on the abort conditions below.

# Base and state

Verify each premise before relying on it. If a premise turns out wrong, record it; it becomes an abort only where the abort list says so.

**Repository and record.**
- `origin/pre-main` is at `429edb96a11b8cd2ff29e9682d25b0ca79b004a7`, the merge of #123. `origin/main` is at `76905d4`.
- The main checkout `~/Zynergy/Forager` and the planner worktree `…/.claude/worktrees/bridge-cse_01BcqShzosraMo4pUXkqaqRp` are both at `429edb9`.
- The planner worktree holds two untracked files:
  - `prompts/preserved/2026-09-26-29.md`, a pulse about the phone that has been answered in the planner's session;
  - this dispatch's own copy. Find it by its body.
- The #123 merge has no `merge` entry yet. Its backup is `2026-09-26-07`, and it was dispatched as `preserved/2026-09-26-28.md`. It carries `-80` to `-82`; confirm this from the diff.
- The next free backup folder is `2026-09-26-08`.
- The completion report listing the eight device items is `docs/audits/2026-09-26-strip-flash-timer-location-completion-report.md`. Read it first.

**The phone, from a pulse at about 09:30Z (read-only adb):**
- `adb devices -l` shows exactly one device: serial `R5CT321008R`, model `SM-S908U`, Android 16 / SDK 36, build `S908USQSAGZH3`.
- **The owner's daily driver, an S26 Ultra, must never be touched.** Set `ANDROID_SERIAL=R5CT321008R`, or pass `-s R5CT321008R` on every adb and Gradle device call. If a second device appears, use only the S908U.
- Installed: `com.zynergylabs.forager.app`, `versionCode=784`, `versionName=1.0.784+g91cafb2a`. It is DEBUGGABLE, installed by `com.android.shell`, and is a CI debug build of PR #113's merge ref.
- CAMERA and ACCESS_FINE_LOCATION are granted.
- App data is about 1.4 MiB (`diskstats`, possibly stale).

**Signing, from the pulse's reading of the repo (inferred, not a digest comparison):**
- Debug builds and CI builds both sign with the committed `app/debug.keystore` (`app/build.gradle.kts:264-270`, `ci.yml:102`).
- An `adb install -r` of a debug APK from `429edb9` therefore replaces the package without an uninstall.
- device_guard compares the certificates at install time and refuses a mismatch (`.claude/hooks/device_guard.py:106-141`).

**Screen, from the pulse:**
- The display is 1080x2316 at density 2.8125, which is 384x823dp.
- The punch-hole cut-out's top bounding rect is `Rect(512, 0 - 568, 75)` px in the natural orientation.
- The status bar is 75 px and the navigation bar is 3-button, 135 px.
- The back camera is `"0"` with a flash unit; the front cameras have none.
- **`dumpsys media.camera` shows no torch state on this phone.**

**What the pulse role was refused.** The pulse's role hook refused `pm`, `wm`, `run-as`, `which` and `uiautomator`. It has not been checked whether your role allows these or `input`.
- If a command a step needs is refused, the step is recorded **not run**. Quote the refusal, and do not use another command to get the same result.
- device_guard also refuses `connectedAndroidTest` unless `-Pandroid.injected.androidTest.leaveApksInstalledAfterRun=true` is passed. It matches on the command text.

**The strip at `429edb9`, read by the planner with `git grep`.** Test tags are `in-app-camera-flash-chip`, `-timer-chip`, `-grid-chip`, `-location-chip`, `-countdown`, `-count`, `-shutter` (`FlashChip.kt:63`, `TimerChip.kt:47`, `GridChip.kt:47`, `LocationChip.kt:51`, `InAppCameraDialog.kt:496-505`).
- Test tags are not visible to `uiautomator` unless the app exposes them as resource ids. Use content descriptions and visible text.
- The labels are in B and C of the decisions record's B8 addendum: "Flash off", "Timer off", "Timer 3 seconds", "Timer 10 seconds", "Save location: On/Off", and "Cancel timer" on the shutter during a countdown.

**Nothing else is running.** No other build is running or will merge into `pre-main` while you run.

# Scope boundary

1. **Worktree.** Create it with `git worktree add ~/Zynergy/forager-wt/strip-device-check -b strip-device-check origin/pre-main`, then unset the upstream and push with `-u` to its own branch.
   - Move your store copy and `2026-09-26-29.md` into it with `mv`. Record the path, size and sha256 of each.
2. **Sweep.** Write:
   - a `dispatch-note` for `-29`: Type pulse, Outcome answered, Report "answered in the planner's session; not relayed to the record";
   - the `merge` entry for #123.

   Commit, then push.
3. **Store copy and intent.** Commit them together, then push.
4. **Build and install.**
   - Run `./gradlew assembleDebug` at `429edb9`, and record the APK's `versionName`.
   - Run `adb -s R5CT321008R install -r <apk>`.
   - Confirm the new `versionName` from `dumpsys package`.
   - If the install is refused or needs an uninstall, stop. That is an abort.
5. **Device items**, below. Before you start, record the value of each setting a step might change, and put every one back afterwards: auto-rotate, user rotation, "Automatically Save Location to Photos", and "Lock camera to portrait". Record each change and each restore.
6. **Run record.** Write `docs/audits/2026-09-26-strip-device-check-run-record.md`:
   - each step's pass condition, the command and its output as evidence, and the verdict: pass, fail, not run, or unverified;
   - which settings you changed and restored;
   - what app data the run created;
   - the three items deferred to beta testing, by owner ruling.

   Put screenshots under `docs/audits/assets/2026-09-26-strip-device-check/`, as PNG from `adb exec-out screencap -p`. Add one README row.
7. **Terminal.** Write the terminal, then open a PR into `pre-main`. CI must be green on its final commit.
8. **Merge.** Run each of these as its own Bash call, never chained:
   - `git fetch origin`
   - confirm `origin/pre-main` is `429edb9`
   - write the backup `2026-09-26-08`
   - `gh pr merge <N> --merge`
   - `gh pr view <N> --json state,mergeCommit`

   If history_guard refuses the merge only for its form, retry once in the bare form. Any other refusal is a stop.
9. **Update.** Run the merged `update_worktree.py` on the main checkout and on the planner worktree: a dry run first, then `--apply` if the dry run exits 0.

**Record IDs.** Use only `2026-09-26-83` to `-99`. Before each commit that touches `RECORD.md`, and before the merge, confirm that none of them appears on `origin/pre-main`.

**Files.** You may add the run record, its screenshots, one README row, `RECORD.md` entries and the two store copies. You change no app code and no test code.

**Phone.**
- Allowed: install, launch, taps, rotation, the camera, and the setting changes in step 5, each restored afterwards.
- Never: uninstall, `pm clear`, clearing data, or a factory reset.
- Captures the steps create stay on the phone. Record them; do not delete them.

# Closed decisions

The owner, in the planner session, 2026-09-26:
- "Run the device check on the S22 ultra".
- "I'll be away from it".
- "Defer the ones that need me for the beta testing. Do the ones you can do without me".

The planner's split under those rulings:
- **Deferred to beta testing, and not run here:** item 3, Flash On fires on capture; item 4, Flash Auto fires in the dark and not in daylight; item 8, glyph readability.
- **Run here:** items 1, 2, 5, 6 and 7.

The pulse found no torch state in `dumpsys media.camera`. So item 5's evidence is the existing instrumented test `app/src/androidTest/java/com/zynergylabs/forager/app/photo/CameraXTorchInstrumentedTest.kt`, which reads CameraX's own `torchState`. It is run alone, with the flag device_guard requires. That is the planner's call.

**Record entries.** Write them with Write or Edit, never a shell heredoc.

# Prediction

Planner, outcome:
1. **Install.** `install -r` succeeds with no uninstall, and the new `versionName` ends `+g429edb9`. That ending is inferred from the pulse's `+g<sha>` pattern.
2. **Portrait layout.** All four chips appear in the order Flash, Timer, Grid, Location, and none of them intersects the cut-out.
3. **Landscape layout.** The same holds in both landscape arrangements, unless "Lock camera to portrait" is on. If it is on, the run turns it off for this step and restores it afterwards.
4. **Torch.** `CameraXTorchInstrumentedTest` passes on the device.
5. **Timer.** 3 s and 10 s each add exactly one capture, at or after their delay. Cancelling adds none.
6. **Location.** The chip and Settings agree in both directions and after a force-stop and relaunch.

Coder: write your mechanism prediction in the intent.

# Finish line and abort conditions

**Finish line:**
- steps 1 to 9 done;
- every item run or recorded as not run, with the reason;
- every changed setting restored;
- the run-record PR merged into `pre-main` with a backup;
- both checkers PASS;
- both checkouts updated.

A failing device item is a result, not an abort. Record it with evidence. Deferral in the kit's sense is not used.

**Abort, and stop and report, on any of these:**
- a device other than `R5CT321008R` would be touched;
- the install is refused, or would need an uninstall;
- any command would clear data or uninstall;
- the app crashes and does not relaunch;
- a setting cannot be restored;
- an ID collision;
- a guard or classifier refusal on a repository step, apart from the one form-only merge retry. A refused phone command is "not run", not an abort;
- CI red;
- two failed attempts at one step.

# Checks

The device items. Each gets a pass condition and evidence.

- **Item 1, portrait.**
  - Open the in-app camera from the app's UI. Find the entry point from the code: `InAppCameraHost`'s targets, the log entry, the Album, or the Cartography entry. Prefer the Album, since it creates the least.
  - **Pass:** a `uiautomator dump`, or an equivalent the role allows, shows four strip controls with the content descriptions of Flash, Timer, Grid and Location, in that order along the strip.
  - **Pass:** none of their bounds intersects `Rect(512,0,568,75)`, the cut-out as the current `dumpsys window displays` reports it.
  - Evidence: a screenshot, the dump excerpt, and the cut-out line.
- **Item 2, both landscapes.** Same pass condition, at rotation 1 and at rotation 3, against the cut-out bounds that `dumpsys` reports at each rotation. Evidence the same way.
- **Item 5, torch.**
  - Run: `ANDROID_SERIAL=R5CT321008R ./gradlew connectedDebugAndroidTest -Pandroid.testInstrumentationRunnerArguments.class=com.zynergylabs.forager.app.photo.CameraXTorchInstrumentedTest -Pandroid.injected.androidTest.leaveApksInstalledAfterRun=true`
  - **Pass:** the JUnit XML under `app/build/outputs/androidTest-results/` shows every test in that class passed on `SM-S908U`.
  - Check the build log for compile errors before you read results (CLAUDE.md).
  - Afterwards, confirm the app package is still installed with the same `versionName`, and that its data size did not drop to near zero.
- **Item 6, timer.**
  - Set the timer to 3 s, read the capture count, and press the shutter.
  - At about 1.5 s: the count is unchanged, and the countdown numeral shows.
  - At about 4 s: the count has gone up by exactly 1.
  - Repeat with 10 s, reading at about 5 s and about 12 s.
  - Cancel: start a 10 s countdown, press the shutter again at about 2 s, and after 12 s the count is unchanged. The shutter's description during the countdown is "Cancel timer".
  - Evidence: a timestamped dump or screenshot at each reading.
  - Screenshot timing is coarse. Say how the timestamps were taken, and treat readings closer than 1 s to a boundary as unverified.
- **Item 7, location.**
  - Record Settings' "Automatically Save Location to Photos" value, as it appears in the app's Settings UI.
  - Toggle the chip, close the camera: Settings shows the toggled value.
  - Toggle in Settings, open the camera: the chip shows it.
  - `am force-stop` the app, relaunch it: both the chip and Settings show the last value.
  - Restore the original value.
  - Evidence: dumps or screenshots at each stage.

**Repository checks.**
- Both checkers run before every `RECORD.md` commit; quote the final PASS lines.
- The CI run id, the backup folder with its `merge.json` and `INDEX.md` line, the merge commit and its parents, and the `update_worktree.py` outputs.

# Out of scope

- Items 3, 4 and 8: deferred to beta testing.
- Fixing anything a device item finds. Record it and flag it.
- The location handler's optimistic update.
- Any app or test change.
- The S26.
- Promotion to `main`.
- Rebase, amend, force-push and delete. Worktrees you add are left in place.

# Device items

This dispatch is the device run. Items 1, 2, 5, 6 and 7 are run here, as specified under Checks. Items 3, 4 and 8 are deferred to beta testing by owner ruling. The run record lists them with the pass conditions from the completion report.

# Merge

authorised: the run-record PR `strip-device-check` into `pre-main` only, per coder.md item 10(a), with the backup first and the merge command bare. Nothing into `main`.

End the hand-back with "Decisions I made" and "Flags outside scope".