HEAD: a3d7417f6f142551d8b355bfeb450ae232bc17bf
Target subagent: coder
Type: build
Preserved: 2026-09-26T03:40:18Z by .claude/hooks/dispatch_guard.py
--- verbatim prompt follows ---
**Type:** build

# Role
You are the coder for Forager's adoption of Claude-kit v0.2. Follow Forager's
.claude/agents/coder.md as it stands at your base, then the kit's coder.md once
installed, for the record entries this build writes. Stop-and-ask on any premise
that fails.

# Base and state
Premises to verify:
- Forager origin/main is af12a69 (PR #117 merge) or later; name the tip you build
  from. Forager main is unprotected on GitHub; the repository is public, so every
  vendored file must be fit to publish.
- Forager tracks the pre-kit hooks (.claude/hooks/*.py, last changed 0eba963),
  .claude/settings.json, .claude/agents/, RECORD.md, check_record.py,
  check_prompts.py and 27 store files 2026-09-22-01 to 2026-09-23-20. No
  .claude/kit.json, no .claude/kit.lock.
- Forager's .gitignore ignores `.claude/*` (line 34) and re-includes only
  settings.json, agents/ and hooks/ (lines 40-42), so kit.json and kit.lock
  would be invisible to git until re-included.
- Forager's settings.json lacks the kit's SessionStart block and hook timeouts,
  so a first install stops with "settings.json exists and differs" (kit
  install.py:191-197 at v0.2; kit test test_differing_settings_json_stops_and_writes_nothing).
- Hardcoded values the kit config replaces: package "com.zynergylabs.forager.app"
  (guardlib.py:18), protected branch main (history_guard.py:33), env prefix
  FORAGER_GUARD_ (device_guard.py:78-104). Everything else equals the template.
- The kit clone at ~/Zynergy/Claude-kit holds tag v0.2 (a2b8025 -> b1bb0bd).
- A Forager planner worktree (bridge-cse_013QR4ELV3wyYrUUyVCEmDwt) carries record
  entries dated 2026-09-26 not on origin/main; take your record IDs after them
  and merge nothing that would conflict with its branch.

# Scope boundary
Branch `kit-v0.2-install` from origin/main, in a fresh worktree (no Forager
checkout is on main today). In order, each its own commit:
1. Run Forager's own two checkers and the kit's check_record.py from the kit
   clone at v0.2 against Forager's RECORD.md and store; both must PASS before
   anything changes. Report the counts.
2. .gitignore: add `!.claude/kit.json` and `!.claude/kit.lock` below line 42,
   with a comment saying the kit's config and lock are shared config.
3. .claude/settings.json: replace with `git -C ~/Zynergy/Claude-kit show
   v0.2:.claude/settings.json`, byte for byte.
4. .claude/kit.json: the v0.2 template with android_package
   "com.zynergylabs.forager.app", guard_env_prefix "FORAGER_GUARD_", the rest
   unchanged. [Forager owner decision A, below, may add backup_dir and a Merge
   section.]
5. Install: `python3 ~/Zynergy/Claude-kit/install.py --target <this worktree>
   --tag v0.2`. Expect: hooks, tests, agents, both checkers and the six new root
   scripts written; kit.json left as written in step 4; kit.lock written. Then
   `python3 check_kit.py` PASS; the vendored hook tests PASS
   (`python3 -m unittest discover -s .claude/hooks/tests`); both checkers PASS
   on Forager's record.
6. CLAUDE.md lines 488-493: add one sentence naming .claude/kit.json, kit.lock
   and check_kit.py as the kit's config, lock and drift check, and the release
   tag.
7. RECORD.md: intent before step 2, terminal after step 6, per Forager's rules.

# Closed decisions
- Forager's owner: adopt Claude-kit v0.2. [Quote the owner's words and their
  source here.]
- Forager owner decision A (needed before step 4): do Forager coders merge pull
  requests? Yes means backup_dir set in kit.json and `Merge` added to the build
  and device required_sections, as the kit's own kit.json does; no means
  history_guard denies every `gh pr merge` and the owner merges in the browser.
- Forager owner decision B: GitHub protection on Forager's main, as on the kit's
  (pull request required, enforced for admins, CI checks). The owner sets it by
  hand or leaves it.
- Planner's choices: the branch name; the order of steps; FORAGER_GUARD_ kept.

# Merge
[not authorised until decision A is made; then per the kit's coder.md item 10]

# Prediction
- Step 1: both of Forager's checkers PASS; the kit's check_record.py at v0.2
  PASSes Forager's RECORD.md (its outcomes are within the kit's sets; its
  history has no merge commit in the simplified log).
- Step 5 before step 3 would stop with "settings.json exists and differs";
  after step 3 it writes 27 files and kit.lock, prints each path, and check_kit.py
  PASSes. The vendored hook tests: "Ran 179 tests" OK (or the count at v0.2).
- Device guard: with the package set, `adb shell input` while another app is in
  front is denied, as before the install.

# Finish line and abort conditions
Finish line: the seven commits pushed, CI green, a pull request open into main
with the install diff and check outputs in its body. Abort on: any checker
failing at step 1 (report, change nothing); install.py stopping for a reason
other than the predicted settings difference; check_kit.py or the hook tests
failing after install; a premise mismatch; decision A unanswered when step 4 is
reached.

# Checks
The checker outputs before and after; install.py's printed file list; check_kit.py;
the hook test count; `git status --short` showing kit.json and kit.lock tracked.

# Out of scope
Forager's open pull requests (#104 touches .claude/skills and .gitignore; do
not resolve it); Forager's stale main checkout; any Forager code; tags.

# Device items
None.