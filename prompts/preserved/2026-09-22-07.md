Operator ruling, 2026-09-22.

Before merge:
1. Confirm the MCP allow added for the flag 1 test has been
   removed. Cite the settings file and line, or state it was
   session-only.
2. Bind dispatch type to target in the dispatch hook: Type pulse
   only to `pulse`, Type build or device only to `coder`, any
   mismatch blocked. Build it failing-first, sabotage it, and
   test it live with a pulse-typed dispatch to `coder`, which
   must be blocked.

Recorded for phase 2, no change now:
- Agents start `claude` sessions from Bash only when a dispatch
  authorizes it.
- Coder egress: sandbox allowlist plus a separate agent identity
  on GitHub. socat is not installed.

Tally: planner error (the scope of "Don't delete anything" was
unclear). Coder deviation, self-reported (/tmp/f1).
