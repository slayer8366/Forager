HEAD: 29e4e09af4b2594f92ec82d0c8c0a46b8e9e369e
Target subagent: coder
Type: build
Preserved: 2026-09-26T06:19:44Z by .claude/hooks/dispatch_guard.py
--- verbatim prompt follows ---
**Type:** build

# Role

You are the coder. This dispatch does three things:
- It finishes the approval-gate work that intent `2026-09-26-29` started.
- It records the owner's adoption of the kit's pre-main branch model.
- It merges PR #120 into `pre-main`, the new working trunk.

You make no decision this dispatch has not made. A gap is a stop-and-ask.

# Base and state

Verify each item before relying on it. A mismatch is a stop.

- **Worktree.** `/home/zynergy-labs/Zynergy/Forager/.claude/worktrees/bridge-cse_01BcqShzosraMo4pUXkqaqRp`, on branch `worktree-bridge-cse_01BcqShzosraMo4pUXkqaqRp`, at `29e4e09`, which is also the remote head. Its history, newest first:
  - `29e4e09`, the owner's commit: kit.json `"protected_branches": ["pre-main", "main"]`.
  - `2d34a44`, the owner's commit: kit.json `"approval_exempt_types": ["pulse", "build", "device"]`.
  - `7519f5c`, intent 2026-09-26-29.
  - `c86555a`, the sweep: dispatch-note 2026-09-26-28 for the stopped `-16`.
  - PR #120's earlier commits.
- **Branches on the remote.**
  - `origin/pre-main` is at `76905d4`. The owner created it just now.
  - `origin/main` is at `76905d4`.
  - PR #120 is open with base `main`.
- **Untracked files.** Before this dispatch there were exactly two:
  - `prompts/preserved/2026-09-26-18.md`: a build dispatch that stopped before writing anything. It is 5989 bytes, sha256 `32f0dc26…2f4a8`. It continued -29 and stopped because a pulse's store copy appeared mid-run.
  - `prompts/preserved/2026-09-26-19.md`: a pulse, answered in the planner's session. It is 4990 bytes, sha256 `23d29c7f…092bc0`.
  - This dispatch's own copy should now be a third, expected at `-20`.
- **Intent 2026-09-26-29.** It is open. Its base half of the positive control is in its Notes. The script is `/tmp/approval_control/control.py`, if it still exists; if not, rebuild it the same way from `.claude/hooks/tests/harness.py`.
- **Backups.** `~/Zynergy/forager-repo-backups` exists and is empty, with no `INDEX.md`.
- **The main checkout.** `/home/zynergy-labs/Zynergy/Forager` is on `claude/new-session-vto65i`. It has diverged from its remote, has an uncommitted `.gitignore` change and has no `.claude/kit.json`.
- **The kit's branch model.** It is described in `~/Zynergy/Claude-kit` at `5ba1581`, `README.md:130-214`. Read it with `git -C ~/Zynergy/Claude-kit show 5ba1581:README.md`; do not check anything out there.

# Scope boundary

In this worktree, on its branch, pushing after each commit:

1. **Sweep.** Write dispatch-notes for:
   - `-18`, Outcome `stopped` (coder.md item 8);
   - `-19`, Type pulse, Outcome `answered`, Report "answered in the planner's session; not relayed to the record".

   Also write any `merge` entry that item 1 requires. The first protected branch is now `pre-main`, whose first-parent chain ends at `76905d4`, which is already recorded as 2026-09-26-24.
2. **Store copy, new intent and superseded terminal, in one commit.**
   - The new intent covers this dispatch. It is not a continuation, because this dispatch widens -29's scope.
   - The terminal closes -29 with Outcome `superseded`, naming the new intent.
   - Write the intent before that terminal, so that `Superseded-by` never names an intent that does not yet exist. Get this order wrong and `check_superseded_by` fails.
3. **After-control.** Run the six cases of -29's control against the current kit.json.
4. **Two notes, and a row for each in `docs/audits/README.md`,** keeping every existing row:
   - `docs/audits/2026-09-26-approval-exempt-build-device.md`. Its contents are as -29's step 5 and preserved `-18`'s step 3 specify. In addition it records:
     - the classifier refusal, quoted from -29's record;
     - that the owner made the edit personally at `2d34a44`;
     - the owner's live observation, relayed by the planner: the first build dispatched after the edit raised "No approval message";
     - that the hook suite cannot tell before from after, because `harness.py` uses its own TEST_CONFIG;
     - the stale "approv" wording lines, listed and not edited.
   - `docs/audits/2026-09-26-pre-main-working-trunk.md`, which records:
     - the owner's rulings (below);
     - the kit's model, cited to `Claude-kit@5ba1581:README.md:130-214`;
     - `pre-main` created at `76905d4` by the owner;
     - the owner's kit.json commit `29e4e09`;
     - that the device check is deferred to promotion;
     - what Forager does not yet match: the main checkout is not on `pre-main`, GitHub branch protection on `pre-main` is unverified (check it with `gh api repos/slayer8366/Forager/branches/pre-main/protection` and record the result), and the kit README describing pre-main is newer than the vendored v0.2 tag.
5. **Terminal.** It closes the new intent, and cites the backup folder from step 7.
6. **PR #120.** Retarget it: `gh pr edit 120 --base pre-main`. Update its title and body to describe what it now carries. CI must be green on its final commit.
7. **Merge PR #120 into `pre-main`** per coder.md item 10(a). First `git fetch`, then write the backup of `origin/pre-main`, then `gh pr merge 120 --merge`. coder.md item 3 orders these: the terminal is pushed before the merge.
8. **After the merge, update this planner worktree only,** with the merged `update_worktree.py`: first a dry run, then `--apply`. Report both outputs.
   - Do **not** update the main checkout. Report its state instead.
   - If the dry run exits 1 because of store copies that appeared during this dispatch, do not apply. Report it; the planner will decide.

**Record IDs.** Use only `2026-09-26-30` to `2026-09-26-39`, in order. If you need more than ten, stop. If the checker rejects one of these IDs, stop.

**Files.** Only `RECORD.md`, `prompts/preserved/` and the two new notes. `docs/audits/README.md` gets only its two new rows.

**Not touched.**
- `.claude/` in any form.
- `CLAUDE.md`.
- `check_*.py`.
- App code.
- Any other PR or branch, including the strip branches.
- The main checkout.
- GitHub settings.
- The phone.

# Closed decisions

The owner, in the planner session, 2026-09-26, in order:
1. "Can't have me hitting buttons. Those hooks need to change". The owner then chose "build and device" for `approval_exempt_types`, and later reported "Done" (`2d34a44`) and "No approval message".
2. Asked who merges the strip PRs: "merge into pre main without device check. defer device check for later".
3. "check the kit for pre main instructions". The planner found the model at `Claude-kit@5ba1581:README.md:130-214` and gave the owner a step 1: create `pre-main` at `76905d4` and set `protected_branches`. The owner replied "step 1 done" (`29e4e09`).

Three more points are **the planner's calls**, to be recorded as such:
- Merging #120 into `pre-main` is authorised under ruling 2. It carries record and config only, no app code.
- The main checkout is excluded from updates until the owner decides.
- The planner reads "The kit has a way of dealing with this" as referring to `approval_exempt_types`.

**Parallel dispatches.** A store copy that appears in this worktree after your own dispatch's copy belongs to the next sweep (coder.md item 9). It does not block this build. `check_prompts.py` may fail naming only such copies, and `git status` may show only them. Record which ones appeared.

**Record entries.** Write them with Write or Edit, never with a shell heredoc.

# Prediction

Planner, outcome:
1. **After-control.** Build to coder and device to coder return `allow`. Pulse stays `allow`. An unknown type, build to pulse, and a missing section stay `deny`, with the same reasons as the base run.
2. **Tests and drift.** The hook suite shows 179 OK, and `check_kit.py` passes 26 files. The protected_branches change does not break either.
3. **Merge.** history_guard lets the merge through once the backup exists, and the merge commit's first parent is `76905d4`.
4. **Worktree update.** `update_worktree.py` fast-forwards this worktree to the merge commit, because the branch head is its second parent.

Coder: write your mechanism prediction in the new intent.

# Finish line and abort conditions

**Finish line.**
- Steps 1–8 are done and pushed.
- The after-control matches prediction 1.
- The suite shows 179 OK, `check_kit.py` passes, and both checkers pass, apart from copies allowed under "Parallel dispatches".
- PR #120 is merged into `pre-main`, with a backup folder and an `INDEX.md` line.
- This worktree is at `origin/pre-main`.

**Deferral is not allowed.**

**Abort, and stop and report, on any of these:**
- A wrong premise.
- The after-control differs from prediction 1.
- A hook test fails.
- `check_kit.py` fails.
- Any guard or classifier refuses a step. Quote it; do not retry by another route.
- CI is red on #120.
- `update_worktree.py` exits 1 for a reason other than the parallel-copy case in step 8.
- Two failed attempts at one step.

# Checks

- **After-control.** Put each of the six cases next to the base result. Show `ls prompts/preserved/` before and after.
- **Stale wording.** Run `git grep -n -i "approv"` over `.claude/`, `CLAUDE.md` and `docs/process/`, and list the matches in the note.
- **Branch protection.** Show the `gh api` result for `pre-main`.
- **Checkers.** Run both before every commit that touches `RECORD.md`, and quote the final PASS lines.
- **CI.** Give the run id and its conclusion on the final commit.
- **Backup.** Give the folder, `merge.json` and the `INDEX.md` line.
- **Merge.** Give the merge commit and its parents.
- **Worktree update.** Give the `update_worktree.py` outputs, and `git log -1` afterwards.

# Out of scope

- Editing vendored files, including the stale wording.
- GitHub branch protection changes.
- The main checkout.
- The camera strip.
- Promotion to `main`.
- Rebase, amend, force-push, delete.

# Device items

None. The device check for the strip is deferred to promotion by ruling 2.

# Merge

authorised: PR #120 into `pre-main` only, per coder.md item 10(a), with the backup first. No other PR, and nothing into `main`.

End the hand-back with "Decisions I made" and "Flags outside scope".