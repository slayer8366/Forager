HEAD: af12a69603ab38295099ef27f0b3114c2a9ccd74
Target subagent: coder
Type: build
Preserved: 2026-09-26T02:24:29Z by .claude/hooks/dispatch_guard.py
--- verbatim prompt follows ---
**Type:** build

# Role

You are the coder. This dispatch does two things in order. Part A merges current `main` into the three stacked camera-strip PRs so they stop conflicting. Part B builds the rest of the strip's basic tools on a new branch stacked on them: flash-on-capture inside the existing flash chip, a self-timer chip, and the save-location chip the owner already decided (B3, B6). You make no design decision this dispatch has not made. A gap is a stop-and-ask.

The owner asked that this session use the Claude-kit system (`~/Zynergy/Claude-kit` at `48c391d`, `.claude/agents/coder.md` there). Forager runs its own older hooks and checkers, so apply the kit's practice only where Forager's tooling accepts it. The concrete differences are listed under Closed decisions, item K.

# Base and state

Verify each of these against the remote before relying on it. A mismatch is a stop.

- Forager `origin/main` at `af12a69603ab…` (merge of PR #117).
- This session's worktree is on branch `worktree-bridge-cse_013QR4ELV3wyYrUUyVCEmDwt`, clean, at `af12a69`. It is not yet on the remote. It holds one untracked store file, `prompts/preserved/2026-09-26-01.md`, which is the planner's read-only pulse sent just before this dispatch. This dispatch's own copy will appear next to it.
- The stack. Every PR targets `main`, and each branch contains the one below it:
  - #111 `strip-housekeeping` at `1b82b15f30622b43b9a22b8456a4e95c42b37438`
  - #112 `strip-torch` at `1f196042e9146b3d2d77cb33886d0fb0accaf416`
  - #113 `strip-grid-level` at `6bcbe4c68d144e8cab05cacce383708c02e986f9`
- From the pulse. Each item is a premise to re-check, not a fact:
  - The merge base with main is `89f53a4`.
  - The only path both sides changed is `docs/audits/README.md`. Both sides append rows directly after the shared line 175, the 2026-09-19 "device check v4 run" row.
  - This is inferred from diffs. `git merge-tree` was blocked for the pulse, so no merge was observed.
  - CI "Build, test, publish APK" is green on all three heads, against the old main.
- Camera facts from the pulse, read at `6bcbe4c`:
  - **Session and capture.** The session is `photo/CameraXCaptureSession.kt`: bind at `:275`, `takePicture` at `:379`, `ImageCapture.Builder` at `:271–272` with only the capture mode set.
  - **Flash modes.** `FlashMode` is `enum class FlashMode { Off, Torch }` in `CameraCaptureSession.kt`. Its doc reads "Auto and flash-on-capture are a later dispatch".
  - **The flash chip.** `FlashChip.kt:37–47` holds its state on the session, with no ViewModel. It resets on `close()` (`CameraXCaptureSession.kt:299–307`).
  - **The grid chip.** It uses a ViewModel and a DataStore repository (`CameraGridModeViewModel.kt`, `DataStoreCameraGridModeRepository.kt`).
  - **The strip.** It is `CameraStrip` (`CameraBands.kt:130–164`). The chip list is `stripChips` (`InAppCameraDialog.kt:461–464`): flash only when `session.hasFlashUnit`, then grid.
  - **Strip layout.** 48 dp chips, 8 dp spacing, no scrolling. About 6 chips fit on this phone's short edge; that figure is derived, not measured.
  - **The save-location setting.** It is `PhotoLocationPreferenceRepository`. Settings reads it once at init into `AvailabilityViewModel`'s `uiState` (`AvailabilityViewModel.kt:137`, `:861–864`) and writes it at `:880–883`. The camera does not read it today. `lockCameraToPortrait` reaches the camera from `AvailabilityScreen.kt:2149–2154`, which is the pattern to follow.
  - **Tests.** Each existing chip has one coordinate-touch test at five points (`InAppCameraDialogTest.kt:533–548`, `CameraGridChipTest.kt:122–141`). The fakes are `FakeCameraCaptureSession` (`flashUnitOnOpen`, `setFlashModeCalls`), `FakeLevelProvider` and `FakeCameraGridModeRepository`.
  - **Last full suite.** 211 classes, 1630 tests, 0 failures, 24 skipped, at `42e3dd3`.
- A phone is connected: the S22 Ultra test phone, SM-S908U. This dispatch does not touch it.

# Scope boundary

**Part A, stack merges.**
1. Merge `origin/main` into `strip-housekeeping`.
2. Merge the updated `strip-housekeeping` into `strip-torch`.
3. Merge the updated `strip-torch` into `strip-grid-level`.
4. Push each branch.

Use `git merge`, never rebase. The only permitted hand edit is resolving `docs/audits/README.md` by keeping every row from both sides. No other file is edited in Part A.

**Part B, new work.**
- **Branch.** The work goes on a new branch `strip-flash-timer-location`, cut from the updated `strip-grid-level`, with a PR against `main` whose body says it is stacked on #113.
- **Files.**
  - App code and tests under `app/`.
  - An append-only addendum, B8, at the end of `docs/audits/2026-09-21-camera-strip-basics-decisions.md`.
  - A completion report `docs/audits/2026-09-26-strip-flash-timer-location-completion-report.md`.
  - One row in `docs/audits/README.md`.
- **Dependencies.** No new dependency, and no version change in `gradle/libs.versions.toml`.

**Record.**
- **Where.** Everything goes on this worktree's branch `worktree-bridge-cse_013QR4ELV3wyYrUUyVCEmDwt`: the sweep note, this dispatch's store copy with its intent, and the terminal.
- **Push and PR.** Push the branch and open a record-only PR against `main`.
- **Stays out of the camera PRs.** No `RECORD.md` or `prompts/` change goes on any camera branch. No README row goes on the record branch.
- **Where the strip work happens.** Do it in separate worktrees of this clone (`git worktree add`), leaving this worktree on its branch.

**Not touched.** The phone, `main` itself, any hook, `CLAUDE.md`, and Claude-kit.

# Closed decisions

**Owner, in chat, 2026-09-26.** Two instructions:
- "Work on #3. We need to finish the camera to basic functionality. That means populate the camera strip with basic camera tools. Dealer's choice on what they are, since they will change in the near future anyway." Item #3 was unsticking the three strip PRs.
- "Look at the Claude-kit repo, use that system for this session."

The tool set below is the planner's choice under that delegation. Record it as such in B8. B1 to B7 in the decisions record stand unchanged.

**A. The strip, in order from its start:** Flash, Timer, Grid, Location.
- Flash stays hidden without a flash unit, as today.
- Timer and Location are always shown.

**B. Flash chip: flash-on-capture joins torch in the one chip (extends B2).**
- `FlashMode` becomes `Off, Auto, On, Torch`. A tap cycles Off, then Auto, then On, then Torch, then back to Off.
- Auto and On set the `ImageCapture` flash mode (`FLASH_MODE_AUTO`, `FLASH_MODE_ON`) with the torch off.
- Torch keeps today's behaviour, with the capture flash mode `FLASH_MODE_OFF`.
- Off is torch off and `FLASH_MODE_OFF`.
- A mode chosen before bind is applied to the `ImageCapture` when it is installed.
- Session-only, like torch today: the chip resets to Off when the camera closes.
- The torch-off-before-unbind order is unchanged.
- Glyphs are Material's flash-off, flash-auto and flash-on icons, and the existing torch glyph.
- Convention, recalled by the planner and unverified: this matches Open Camera's cycle, without red-eye.

**C. Timer chip, new.**
- **Cycle.** A tap cycles Off, then 3 s, then 10 s, then back to Off.
- **Lifetime.** Session-only, default Off. The planner's reason is that a forgotten persisted 10 s timer costs a missed shot.
- **Shutter with a timer.** A shutter press with the timer set starts a countdown. The capture happens once, when the countdown reaches zero.
- **Countdown display.** The remaining whole seconds show as large numerals centred on the viewfinder. They are upright under `rotateWithDevice`, like the other glyphs, and take no pointer input.
- **Cancel by shutter.** Pressing the shutter again during a countdown cancels it with no capture. During the countdown the shutter's content description is "Cancel timer".
- **Cancel by Back.** Back during a countdown cancels it, and the camera closes as today with no capture afterwards.
- **Changing the timer mid-countdown.** It affects the next press only; the running countdown keeps its length.
- **Photo count.** It increments only on an actual capture.
- **Flash and torch.** Flash applies at the capture, not at the start of the countdown.
- **Where the countdown lives.** In the camera's ViewModel, or in a plain Kotlin class the ViewModel owns, never in a composable. It is a bounded loop.
- **Tests and the hang pitfall.** Read the CLAUDE.md entry on unstopped poll loops anyway. Any test that starts a countdown ends it inside the test body, in a `finally`.
- **Glyphs.** Material's timer-off, timer-3 and timer-10 icons.
- **Convention.** Recalled by the planner and unverified: a top-bar timer as in Samsung Camera, with cancel-by-shutter as in Pixel and Samsung.

**D. Location chip: the B3 mirror, built as B3 and B6 state.**
- **One state.** It shows and sets "Automatically Save Location to Photos". There is one state, not a per-shot override.
- **Path.** It goes through `AvailabilityViewModel`'s existing handler and `uiState`, handed to the camera the way `lockCameraToPortrait` is. It never writes the repository directly, because that would leave Settings' copy stale.
- **Wording.** The content description is "Save location: On" or "Save location: Off". Per B3's wording rule, no label, comment, commit or doc calls it a geotag.
- **Glyphs.** Material's location-on and location-off icons.
- **If Settings updates optimistically.** If `AvailabilityViewModel` changes `uiState` before the write succeeds, or shows a changed state after a failed write, do not change that handler. Report it under Flags outside scope. The chip shares whatever Settings shows.

**E. Out of this dispatch:** pinch zoom, zoom buttons, tap to focus or meter, exposure, a lens switch, RAW, aspect ratio, a last-shot thumbnail, and B7d's crosshair. B8 records them as not built.

**F. Glyph availability.** Use icons already available through the project's existing dependencies. If one named above is not available, stop and report which one. Do not substitute.

**K. Claude-kit practice, as applied here.**
- **Adopted:**
  - A sweep first.
  - The store copy and the intent in one commit, then the work.
  - Checks fail first, for the stated reason.
  - Two misses, then data.
  - Scope is a wall.
  - Cite or qualify.
  - Push before you tidy.
  - The hand-back ends with "Decisions I made" and "Flags outside scope".
  - A Merge section is carried (below).
  - Agents delete nothing. Worktrees you add are left in place and listed in the hand-back.
- **Not adopted:** the kit's `merge`, `continuation` and `revert` record kinds, and the `merged` and `stopped` dispatch-note outcomes. Forager's `check_record.py` accepts only intent, terminal and dispatch-note, with outcomes answered, declined or exercise. The record stays inside what that checker passes.

# Prediction

Planner, outcome:
1. **The stack merges.** Merging main into `strip-housekeeping` conflicts only in `docs/audits/README.md`. The two chained merges conflict at most in that same file. After the push, GitHub reports all three PRs MERGEABLE, and CI is green on the three new heads.
2. **Flash.** Flash-on-capture needs only `ImageCapture.setFlashMode` plus carrying the mode through `installImageCapture`. The existing torch tests pass unchanged, except where they enumerate `FlashMode` or its cycle.
3. **Location.** The chip needs no new repository and no new DataStore key: one value and callback pair threaded from `AvailabilityScreen` to `InAppCameraDialog`.
4. **Suite.** The full suite grows by 20 to 45 tests with 0 failures. Skipped stays at 24.

Coder: write your mechanism prediction in the intent, before any work.

# Finish line and abort conditions

**Finish line.**
1. **Record start.** The sweep is committed and pushed on the record branch. It is one dispatch-note for `prompts/preserved/2026-09-26-01.md`, Type pulse, Outcome answered, Report "answered in the planner's session; not relayed to the record". Then this dispatch's store copy and its intent follow in one commit.
2. **Part A.** The three merges are pushed, each PR reports MERGEABLE, and CI is green on each new head.
3. **Part B.** Pushed in commits that each hold one change, with tests-only commits first, each failing for its stated reason.
4. **Part B PR.** Open against main, with CI green on its final commit.
5. **Docs.** The B8 addendum, the completion report and the README row are on the Part B branch.
6. **Record end.** A terminal closes the intent. Both checkers pass, the record branch is pushed, and the record PR is open.

**Deferral is not allowed:** every item lands or the build stops.

**Abort, and stop and report, on any of these:**
- A premise under Base and state is wrong.
- Any Part A conflict is outside `docs/audits/README.md`.
- Keeping every row would need a row edited or dropped.
- A glyph is unavailable (F).
- The suite shows a failure outside this work. Report it and do not touch it, per CLAUDE.md.
- Two failed fixes on one symptom.
- Any guard blocks a step. Report the guard's text.
- A new unclaimed store file appears whose outcome you did not see.

# Checks

- **Part A resolution.**
  - Order: keep every row from both sides, ordered by date, with main's rows first on a shared date. Edit no row's text.
  - Count the index rows (lines starting `| 20`) on each side and in the result. The result equals the sum of the rows each side added past the base, plus the base's rows.
  - `git diff --name-only` from each branch's old head to its new head lists only paths main changed. Show it.
- **Part B tests fail first.** Each tests-only commit is run and fails for the reason stated in its message. It then passes after the fix commit.
- **Entry points** (CLAUDE.md, Testing):
  - Chips are driven through the real `InAppCameraDialog`.
  - The location chip is also driven from `AvailabilityScreen` through the host, the way `AvailabilityScreenInAppCameraTest` does the grid.
  - Assert on actual state: the `ImageCapture`'s `flashMode` read back from a real unbound `ImageCapture` via the existing seam; `takePicture` call counts on the fake session; the repository's stored value, and Settings' checkbox state.
- **Required cases.**
  - **Flash.** Each of the four modes gives the right `ImageCapture` flash mode and torch state. A mode set before install is applied on install. It resets to Off on close.
  - **Timer, no early capture.** With 3 s, there is no capture at 2.9 s of virtual time and exactly one at 3 s. The same holds for 10 s.
  - **Timer, cancel by shutter.** No capture, ever.
  - **Timer, Back mid-countdown.** No capture after close.
  - **Timer, photo count.** It is unchanged until the capture.
  - **Timer, change mid-countdown.** The running countdown keeps its length.
  - **Location, strip to Settings.** A tap on the strip changes the stored value and Settings' checkbox.
  - **Location, Settings to strip.** A change in Settings shows on the strip at the next open.
  - **Layout.** In portrait and both landscape arrangements, all four chips sit inside the strip's bounds and do not overlap. Say that Robolectric reports zero insets, so this is not evidence about the cut-out.
- **Coordinate touch.** Each new chip, and the flash chip after its change, gets a real `performTouchInput` test at five points across its own bounds over the viewfinder. This is the existing convention.
- **Revert checks.** One per behaviour:
  - the flash mode reaching `ImageCapture`
  - the timer delay
  - cancel-by-shutter
  - the location chip's write path
  Each revert follows CLAUDE.md's revert rule: save a copy before editing, check the build log for compile errors before reading results, restore from the saved copy, never from git, and confirm the forward change is still present afterwards. Quote each failure message and say why it belongs to that revert.
- **Suite and checkers.**
  - Run the full unit suite before Part B and after. Report classes, tests, failures and skipped for both.
  - `python3 check_record.py` and `python3 check_prompts.py` pass before every commit that touches `RECORD.md`.

# Out of scope

- Everything in Closed decisions E.
- Fixing anything found in Settings' location handler, or anywhere else outside the listed files. Flag it instead.
- Running anything on the phone, including `connectedAndroidTest`. The stack's run record warns it wipes app data on this phone.
- Merging any PR, tagging, rebasing, force-pushing, or deleting a branch, worktree or file.
- Reordering, retitling or rewording existing README rows or decision-record entries.

# Device items

None are run in this dispatch. List these in the completion report for the device check that follows, each with a pass condition:
1. **Four chips in portrait.** All four show, clear of the punch-hole.
2. **Four chips in landscape.** The same, in both landscape arrangements.
3. **Flash On.** The flash fires on capture.
4. **Flash Auto.** The flash fires in the dark and not in daylight.
5. **Torch.** It behaves as before.
6. **Timer.** 3 s and 10 s count down and capture once, and cancel by shutter works.
7. **Location.** The chip and Settings agree after a toggle in each direction and after an app restart.
8. **Glyph readability.** The owner's verdict on each new glyph.

# Merge

Not authorised. Forager's history_guard blocks `gh pr merge` for every role (`.claude/hooks/history_guard.py:31`, as recorded in RECORD.md 2026-09-23-02). The owner merges #111, #112, #113, the new PR and the record PR, in that order.

End the hand-back with "Decisions I made" and "Flags outside scope".