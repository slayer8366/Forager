HEAD: 0eade34fd15e1e94a48c2b98d5f2698715a56042
Target subagent: coder
Type: build
Preserved: 2026-09-26T06:46:07Z by .claude/hooks/dispatch_guard.py
--- verbatim prompt follows ---
**Type:** build

# Role

You are the coder. This dispatch changes `.github/workflows/ci.yml` so that pushes to `pre-main` run CI and are exempt from cancel-in-progress. It then merges the change into `pre-main`. You make no decision this dispatch has not made; a gap is a stop-and-ask.

# Base and state

Verify each of these before relying on it. A mismatch is a stop.

- **Remote branches.**
  - `origin/pre-main` is at `0eade34fd15e1e94a48c2b98d5f2698715a56042`: the merge of PR #120, whose parents are `76905d4` and `c5693d4`.
  - `origin/main` is at `76905d4`.
- **The planner worktree:** `/home/zynergy-labs/Zynergy/Forager/.claude/worktrees/bridge-cse_01BcqShzosraMo4pUXkqaqRp`.
  - It is at `0eade34` and clean.
  - This dispatch's store copy is written there by the hook as an untracked `prompts/preserved/2026-09-26-NN.md`. Find it by its body matching this dispatch; do not assume the number.
  - **A second build dispatch (Part A, the strip merges) is sent at the same time.** Its copy lands there too. Touch only your own copy.
- **`ci.yml` at `0eade34`.**
  - `:8-10` is `on: push: branches: [main]`.
  - `:11-15` is `pull_request:` with no branch filter.
  - `:20-25` is the concurrency block, with `cancel-in-progress: ${{ github.ref != 'refs/heads/main' }}` at `:25`, and a comment at `:21-22` saying runs on main are exempt.
- **Record.** The newest entries on `pre-main` run up to `2026-09-26-34`.
- **Backups.** `~/Zynergy/forager-repo-backups/` holds `2026-09-26-01` (the #120 merge) and `INDEX.md`.

# Scope boundary

1. **Worktree.** Create a new worktree with `git worktree add ~/Zynergy/forager-wt/ci-pre-main -b ci-pre-main origin/pre-main`. Do the work there.
2. **Store copy.** Move your own dispatch's store copy with `mv` from the planner worktree into the same path in your worktree. Record its original path, size and sha256 in the intent.
3. **Store copy and intent, in one commit.**
   - **No sweep.** This dispatch writes no `merge` entry for PR #120. The parallel Part A dispatch writes it. The planner assigned it there so that exactly one entry records the merge; that is the planner's call.
   - If you find any other unclaimed file or unrecorded merge in your worktree, stop and report it.
4. **The edit, in one commit.**
   - `:10` becomes `branches: [main, pre-main]`.
   - `:25` becomes `cancel-in-progress: ${{ github.ref != 'refs/heads/main' && github.ref != 'refs/heads/pre-main' }}`.
   - Update the comments at `:20-22` and near `:8-10` so they say why:
     - pre-main is the working trunk that builds merge into (`docs/audits/2026-09-26-pre-main-working-trunk.md`);
     - a merge into pre-main needs its own recorded result;
     - a cancelled run would leave a merge commit with none.
   - Keep the existing comment's reasoning for main.
   - No other line changes.
5. **Terminal.** Push, then open a PR into `pre-main`. Write and push the terminal.
6. **Merge.** Once CI is green on the final commit, `git fetch`, write the backup of `origin/pre-main` per coder.md item 10, and run `gh pr merge <N> --merge`.
   - The backup folder is the next free `2026-09-26-NN` under `~/Zynergy/forager-repo-backups/`.
   - The parallel coder writes backups there too. If the name you picked exists by the time you write it, take the next one.
7. **After the merge.**
   - Confirm that a `push` CI run starts on the new `pre-main` head. Give its run id and event, and its conclusion if it finishes within 15 minutes; otherwise say it is still running.
   - Do not run `update_worktree.py` on the planner worktree. The Part A dispatch does that at its end.

**Record IDs.** Use only `2026-09-26-40` to `-49`. Before each commit touching `RECORD.md`, and again before the merge, check that none of your IDs is in `origin/pre-main`'s `RECORD.md`. If one is, stop.

**Files.** Only `.github/workflows/ci.yml`, `RECORD.md` and your store copy. No docs note, and no `docs/audits/README.md` row: the index is a serialization point and a parallel dispatch is running.

**Not touched.**
- Any other workflow file.
- `.claude/`.
- App code.
- The strip branches.
- The planner worktree, apart from moving your own store copy out of it.
- The main checkout.
- GitHub settings.
- The phone.

# Closed decisions

The owner, in the planner session, 2026-09-26:
- "Add pre-main to ci.yml".
- Given the choice, the owner picked "Also exempt pre-main from cancellation". The planner offered it as: `cancel-in-progress: ${{ github.ref != 'refs/heads/main' && github.ref != 'refs/heads/pre-main' }}`.
- On merging into pre-main, the earlier ruling: "merge into pre main without device check. defer device check for later". **The merge is authorised** under that ruling. This is the planner's application of it to a non-strip PR.

**Record entries.** Write them with Write or Edit, never with a shell heredoc.

**Parallel dispatches.** Store copies other than your own belong to other dispatches. Leave them alone.

# Prediction

Planner, outcome:
1. **The PR's own CI run.** The PR's `pull_request` run passes; the change alters triggers and concurrency only.
2. **After the merge.** A `push` run starts on the new `pre-main` head, with event `push`. Before this change there was none: the #120 merge got no push run.
3. **Cancellation.** A push run on `pre-main` is not cancelled by a later push to `pre-main`. This is not exercised here; the planner infers it from the expression.

Coder: write your mechanism prediction in the intent.

# Finish line and abort conditions

**Finish line.**
- Steps 1 to 7 done and pushed.
- CI green on the PR's final commit.
- Backup written, and the PR merged into `pre-main`.
- A `push` run observed on the new head.
- Both checkers PASS on your branch.

**Deferral is not allowed.**

**Abort, and stop and report, on any of these:**
- A wrong premise.
- An unclaimed file or unrecorded merge other than #120's.
- An ID collision.
- Any guard or classifier refusal: quote it, and do not retry by another route.
- CI red.
- A merge conflict with `pre-main`.
- Two failed attempts at one step.

# Checks

- **Failing first.** Before the edit, show that `git log --format=%H -1 origin/pre-main` (`0eade34`) has no `push`-event run:
  `gh run list --branch pre-main --event push --json databaseId,headSha`
  After the merge, show that one exists for the new head.
- **The diff.** Show `git diff origin/pre-main -- .github/workflows/ci.yml` in full.
- **Other main-only references.** Run `git grep -n "refs/heads/main\|branches: \[main" .github/` and list every hit you did not change, such as an APK publish condition. Report them only; do not change them.
- **Checkers.** Run both before every commit touching `RECORD.md`, and quote the final PASS lines.
- **Backup.** Give the folder, `merge.json` and the `INDEX.md` line.
- **Merge.** Give the merge commit and its parents.

# Out of scope

- Other workflows.
- Any publish or release step.
- Branch protection.
- Part A.
- Updating the planner worktree or the main checkout.
- Rebase, amend, force-push, delete.

# Device items

None.

# Merge

authorised: this dispatch's PR into `pre-main` only, per coder.md item 10(a), with the backup first.

End the hand-back with "Decisions I made" and "Flags outside scope".