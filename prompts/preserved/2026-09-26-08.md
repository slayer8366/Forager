HEAD: a3d7417f6f142551d8b355bfeb450ae232bc17bf
Target subagent: coder
Type: build
Preserved: 2026-09-26T03:44:36Z by .claude/hooks/dispatch_guard.py
--- verbatim prompt follows ---
**Type:** build

# Role

You are the coder for Forager's adoption of Claude-kit v0.2.

- **Which coder.md.** Follow Forager's `.claude/agents/coder.md` as it stands at your base. Once the kit is installed, follow the kit's `coder.md` for the record entries this build writes.
- **Failed premise.** If any premise fails, stop and ask.

This is the owner's install dispatch, `preserved/2026-09-26-07.md`, re-sent. That coder stopped correctly on four gaps; the owner has now answered them. The blanks are filled and those four answers are applied, marked **[owner answer]**. Nothing else in the owner's text has changed.

# Base and state

Premises to verify:

- **Forager main.** `origin/main` is `af12a69` (the PR #117 merge) or later; name the tip you build from.
  - Forager main is unprotected on GitHub.
  - The repository is public, so every vendored file must be fit to publish.
- **Tracked files.** Forager tracks:
  - the pre-kit hooks (`.claude/hooks/*.py`, last changed `0eba963`);
  - `.claude/settings.json` and `.claude/agents/`;
  - `RECORD.md`, `check_record.py` and `check_prompts.py`;
  - 27 store files, `2026-09-22-01` to `2026-09-23-20`.

  There is no `.claude/kit.json` and no `.claude/kit.lock`.
- **`.gitignore`.** Forager's `.gitignore` ignores `.claude/*` (line 34) and re-includes only `settings.json`, `agents/` and `hooks/` (lines 40-42). So `kit.json` and `kit.lock` would be invisible to git until re-included.
- **Settings.** Forager's `settings.json` lacks the kit's SessionStart block and hook timeouts. So a first install stops with "settings.json exists and differs" (kit `install.py:191-197` at v0.2; kit test `test_differing_settings_json_stops_and_writes_nothing`).
- **Hardcoded values the kit config replaces:**
  - package `"com.zynergylabs.forager.app"` (`guardlib.py:18`);
  - protected branch `main` (`history_guard.py:33`);
  - env prefix `FORAGER_GUARD_` (`device_guard.py:78-104`).

  Everything else equals the template.
- **Kit tag.** The kit clone at `~/Zynergy/Claude-kit` holds tag `v0.2` (`a2b8025` -> `b1bb0bd`).
- **The planner worktree** (`bridge-cse_013QR4ELV3wyYrUUyVCEmDwt`) carries record entries dated 2026-09-26 that are not on `origin/main`. **[owner answer]** You build on its branch, as described below, so nothing conflicts with it.
  - **Its branch.** `origin/worktree-bridge-cse_013QR4ELV3wyYrUUyVCEmDwt` is at `a3d7417`, one commit past `af12a69`. That commit holds dispatch-note `2026-09-26-01`, which claims `preserved/2026-09-26-01.md`.
  - **Untracked store files there.** `prompts/preserved/2026-09-26-02.md` to `-08.md` exist only as untracked files in that worktree. `-08` is expected to be this dispatch; the hook's name governs.
    - `-02`: coder build, strip merges plus three chips. Stopped after its sweep `a3d7417`.
    - `-03`: coder build, strip merges. Stopped at its premise check and wrote nothing.
    - `-04`: coder build, strip merges. Stopped before writing anything.
    - `-05`: coder build, strip merges. Did nothing visible; the owner reports it stopped.
    - `-06`: a pulse. The owner rejected it before it ran.
    - `-07`: the first send of this dispatch. Stopped before any write.
- **Nothing else running.** The owner reports that every earlier coder has stopped. No other agent is working in that worktree.
- **Findings from `-07`'s coder.** It verified every premise above read-only. In a scratch clone at `af12a69`:
  - Forager's `check_record.py` passed: 42 entries, 28-commit history walk.
  - Forager's `check_prompts.py` passed: 27 claimed.
  - The kit v0.2 `check_record.py` passed on Forager's `RECORD.md`.

# Scope boundary

**[owner answer]** Branch `kit-v0.2-install` from `origin/worktree-bridge-cse_013QR4ELV3wyYrUUyVCEmDwt` (`a3d7417`), in a fresh worktree. No Forager checkout is on main today. In order, each its own commit unless stated:

1. **Checks before any change.** Run Forager's own two checkers, and the kit's `check_record.py` from the kit clone at v0.2, against Forager's `RECORD.md` and store. Both must PASS before anything changes. Report the counts. **[owner answer]** Step 1 makes no commit. Its outputs go in the intent and the PR body.
2. **`.gitignore`.** Add `!.claude/kit.json` and `!.claude/kit.lock` below line 42, with a comment saying the kit's config and lock are shared config.
3. **`.claude/settings.json`.** Replace it with the output of `git -C ~/Zynergy/Claude-kit show v0.2:.claude/settings.json`, byte for byte.
   - **[owner answer] Fail-first, before this step.** Run step 5's install command once. See it stop on the settings difference. Confirm with `git status --short` that it wrote nothing. Record its output.
4. **`.claude/kit.json`.** The v0.2 template, with `android_package` `"com.zynergylabs.forager.app"` and `guard_env_prefix` `"FORAGER_GUARD_"`. **[owner answer]** Under decision A = yes:
   - `backup_dir` is `"~/forager-backups"`;
   - `"Merge"` is appended to the build and device `required_sections`, as the kit's own `kit.json` does.

   The rest is unchanged.
5. **Install.** Run `python3 ~/Zynergy/Claude-kit/install.py --target <this worktree> --tag v0.2`.
   - Expect it to write the hooks, tests, agents, both checkers and the six new root scripts. `kit.json` is left as written in step 4, and `kit.lock` is written.
   - Then run `python3 check_kit.py`; it must PASS.
   - The vendored hook tests must PASS (`python3 -m unittest discover -s .claude/hooks/tests`).
   - Both checkers must PASS on Forager's record.
6. **`CLAUDE.md`, lines 488-493.** Add one sentence naming `.claude/kit.json`, `kit.lock` and `check_kit.py` as the kit's config, lock and drift check, with the release tag.
7. **`RECORD.md`.** An intent before step 2 and a terminal after step 6, per Forager's rules. **[owner answer]** The record goes as follows:
   - **Copy the store files.** Copy `-02` to `-08` from the planner worktree byte for byte, keeping their names. Record each one's path, size and sha256.
   - **Sweep commit, first.** Entry IDs run from `2026-09-26-02` upward.
     - `-02`, `-03`, `-04` and `-05` each get an intent written after the fact, following the precedent of RECORD.md `2026-09-22-01`, with fields from that dispatch. Each also gets a terminal with Outcome `abandoned`, stating what it did and why it stopped, as listed above.
     - `-06` gets a dispatch-note: Type `pulse`, Outcome `declined`.
     - `-07` gets an intent written after the fact.
   - **Next commit.** This dispatch's store copy and its intent, plus `-07`'s terminal with Outcome `superseded` and `Superseded-by` naming this intent. It goes in this commit because a `Superseded-by` must name an intent already in the file.
   - **Then** steps 2 to 6, then this terminal.

# Closed decisions

- **Forager's owner: adopt Claude-kit v0.2.** **[owner answer]** The owner's words, in this planner session on 2026-09-26: "Install this latest update https://github.com/slayer8366/Claude-kit/releases/tag/v0.2". The owner then supplied this dispatch's text and said "Have them follow the dispatch as it was written".
- **Forager owner decision A: do Forager coders merge pull requests?**
  - **[owner answer] Yes.** The owner's answer to the planner's question in this session, 2026-09-26, was "Yes, coders merge".
  - That means `backup_dir` is set in `kit.json`, and `Merge` is added to the build and device `required_sections`, as the kit's own `kit.json` does. The alternative would have been that history_guard denies every `gh pr merge` and the owner merges in the browser.
- **Forager owner decision B: GitHub protection on Forager's main,** as on the kit's: pull request required, enforced for admins, CI checks. The owner sets it by hand or leaves it.
- **Planner's choices:** the branch name, the order of steps, and keeping `FORAGER_GUARD_`.
- **[owner answer] The four answers to `-07`'s stop:**
  - fill the blanks from this session;
  - cut from `a3d7417`;
  - an intent plus an abandoned terminal for the stopped coder dispatches;
  - step 1 makes no commit, and the fail-first install is run.

# Merge

Not authorised for this PR. **[owner answer]** Decision A is yes, but this session's guards are Forager's pre-kit hooks, loaded from the planner worktree, and they deny every `gh pr merge`. The installed kit takes effect only in a new session. The owner merges this PR.

# Prediction

- **Step 1.** Both of Forager's checkers PASS. The kit's `check_record.py` at v0.2 PASSes Forager's `RECORD.md`: its outcomes are within the kit's sets, and its history has no merge commit in the simplified log.
- **Step 5.** Run before step 3, it stops with "settings.json exists and differs". Run after step 3, it writes 27 files and `kit.lock`, prints each path, and `check_kit.py` PASSes.
- **Hook tests.** The vendored hook tests report "Ran 179 tests" OK, or the count at v0.2.
- **Device guard.** With the package set, `adb shell input` while another app is in front is denied, as before the install.

# Finish line and abort conditions

**Finish line.**
- The commits are pushed.
- CI is green.
- A pull request is open into main, with the install diff and the check outputs in its body.

**Abort on any of these:**
- any checker failing at step 1 (report, change nothing);
- `install.py` stopping for a reason other than the predicted settings difference;
- `check_kit.py` or the hook tests failing after the install;
- a premise mismatch.

# Checks

- The checker outputs, before and after.
- `install.py`'s printed file list.
- The `check_kit.py` result.
- The hook test count.
- `git status --short` showing `kit.json` and `kit.lock` tracked.
- **[owner answer]** The fail-first install run's output and the `git status` after it.
- The size and sha256 of each copied store file.
- The planner's note on a guard seen twice here (2026-09-23-31, and the coder of `-02`): history_guard refuses a Bash command whose text it cannot parse when it mentions git and push. So:
  - write `RECORD.md` entries with the Edit or Write tool, not a shell heredoc;
  - run one git command per Bash call.

# Out of scope

- Forager's open pull requests. #104 touches `.claude/skills` and `.gitignore`; do not resolve it.
- Forager's stale main checkout.
- Any Forager code.
- Tags.

# Device items

None.