HEAD: 2d34a4409f61d42079c85bacd78b33b0694fad72
Target subagent: pulse
Type: pulse
Preserved: 2026-09-26T06:00:31Z by .claude/hooks/dispatch_guard.py
--- verbatim prompt follows ---
**Type:** pulse

## Role

You are the pulse. You are read-only: answer with `path:line` evidence at the commits named, and change nothing — no file, commit, branch, worktree or phone. Read other commits with `git show <sha>:<path>`, `git grep <sha>` and `git diff`. Do not check anything out.

## Base and state

- Premise to check: remote strip heads are `strip-housekeeping` at `1b82b15`, `strip-torch` at `1f19604` and `strip-grid-level` at `6bcbe4c` (PRs #111–#113).
- Premise to check: `origin/main` is at `76905d4`, and the strip stack's merge base with main is `89f53a4`.
- Say whether each premise holds.
- A coder is running at the same time in the planner worktree, writing `RECORD.md` and `docs/audits/`. Don't read those files as settled while it runs.

## Rules

- Every claim carries `path:line@sha`, or is marked unverified.
- Read, observed and inferred are kept apart.
- Be compact: no whole files. The planner will write four coder dispatches from your answers.
- Do not quote, summarise, cite or name the Restricted Object.

## Questions

The planner will run Part B as three parallel coders, each on its own branch cut from `strip-grid-level` after main is merged into the stack:
- (F) the flash chip becomes Off → Auto → On → Torch, with Auto and On setting `ImageCapture` flash mode;
- (T) a session-only self-timer chip, Off / 3 s / 10 s, with the countdown in a ViewModel or a plain class it owns;
- (L) a save-location chip that mirrors the "Automatically Save Location to Photos" setting through `AvailabilityViewModel`'s existing handler.

A fourth coder then merges the three branches. Answer at `6bcbe4c` unless a question says otherwise.

1. **Merge risk from main.** List the paths under `app/` that main changed between `89f53a4` and `76905d4`, and the paths under `app/` the stack changed between `89f53a4` and `6bcbe4c`. Give the intersection and, for each path in it, whether the hunks overlap. Also give the same two lists and intersection for `docs/audits/README.md` and `docs/audits/2026-09-21-camera-strip-basics-decisions.md`.

2. **Flash (F).** Find:
   - `FlashMode`, `setFlashMode`, `hasFlashUnit` and the torch enable/disable path in `CameraCaptureSession.kt` and `CameraXCaptureSession.kt`;
   - where `ImageCapture` is built or installed, including any `installImageCapture` or test seam that exposes the `ImageCapture`;
   - the torch-off-before-unbind order;
   - `FlashChip.kt` in full outline: state source, cycle, glyphs, content descriptions;
   - `FakeCameraCaptureSession`'s flash fields;
   - every test that enumerates `FlashMode` or its cycle.

3. **Timer (T).** Find:
   - the shutter's click path from `ShutterButton` to `session.capture`, including where the photo count and the capture error live;
   - Back handling while the camera is open;
   - what `InAppCameraViewModel` holds, and whether it is reachable from `InAppCameraDialog` or only from the host;
   - whether any existing class in the app runs a countdown or delay loop, and how its tests stop it;
   - the dialog's `rotateWithDevice` or glyph-uprighting helper, for the countdown numerals.

4. **Location (L).** Find:
   - `PhotoLocationPreferenceRepository`;
   - `AvailabilityViewModel`'s read of it and its write handler, with lines. Say whether `uiState` is updated before or after the write succeeds, and what happens on a failed write;
   - the `lockCameraToPortrait` path from `AvailabilityScreen` to the dialog, hop by hop;
   - how `AvailabilityScreenInAppCameraTest` drives the grid chip through the host.

5. **The shared seams all three touch.** Find:
   - `stripChips` (or whatever builds the chip list): its signature and each call site;
   - `InAppCameraDialog`'s and `InAppCameraHost`'s parameter lists;
   - the test files each chip's tests live in.
   Then, for F, T and L, list the files each would most plausibly edit, marked inferred, and the files two or more would edit. The planner is deciding how to cut the work so the merge conflicts are small.

6. **Glyphs.** Is `material-icons-extended` (or equivalent) a dependency? Give its `libs.versions.toml` and `build.gradle` lines. Which of these are used anywhere, and which are only inferred to exist in that artifact: FlashOff, FlashAuto, FlashOn, the existing torch glyph, TimerOff, Timer3, Timer10, LocationOn, LocationOff?

7. **Chip layout budget.** Give the strip's chip size, spacing and arrangement code. Say whether four chips fit in portrait and in both landscape arrangements by the code's own arithmetic, and name the numbers used.

8. **Tests and suite.**
   - The existing coordinate-touch helper for chips (five points).
   - The last recorded full-suite count on the stack.
   - Whether CI on #111–#113 last ran against a main older than `76905d4`.

End with **Could not determine**.