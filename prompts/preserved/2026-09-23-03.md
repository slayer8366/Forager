HEAD: 624173b5fec0c254aa7bebf5584e29ee8ef50448
Target subagent: pulse
Type: pulse
Preserved: 2026-09-23T03:57:23Z by .claude/hooks/dispatch_guard.py
--- verbatim prompt follows ---
**Type:** pulse

# Pulse: current state of every input to the D32 follow-up task in slayer8366/forager-forecast

## Role

You are a read-only pulse. Answer the questions with file:line evidence against the named commit. Change nothing: no file, no commit, no branch, no checkout in any clone.

## Base and state

- Read forager-forecast at `origin/main`. Expect `82f28b6`, or a merge of `d55-artifact-contract` (`2d8cc8f`) on top of it: a coder is merging that branch right now. Report which one you read.
- Read through `gh api repos/slayer8366/forager-forecast/...`, or with `git show <sha>:<path>` / `git grep <sha>` in a clone after a `git fetch`. Never check out anything, and never write into `~/Zynergy/forager-forecast`.
- The planner has read `docs/audits/2026-09-20-d32-stage-2b-report.md`, section "Inputs for D32's follow-up task". Its line cites are to `47204f7`. Treat each one as a premise to re-check, not a fact.

## Rules

- Every claim carries a path:line at the stated commit, or is marked unverified.
- Quote decision rows **verbatim**, never from a summary.
- Where you infer rather than read, say so.
- Never print a credential value.
- Keep the answer compact: tables where possible, no file dumps.

## Questions

1. **Decision rows.** From `docs/planning/DECISIONS.md` on the commit you read, quote verbatim the Decision column (and the Reason column only where it is short) for D26, D27, D28, D29, D32, D42, D44, D45, D46, D47, D48, D51, D54. If the branch has merged, also quote D55 to D60 in one line each. Give each row's line number.
2. **Record types.** Where is T1's `Record` dataclass, and where is T2's `Record = Mapping[str, str]`? For each, list every production importer and every test importer (`git grep`, with path:line).
3. **Cell conventions.**
   - Show `cell_for` and `_tenths` in `src/forager_forecast/cells.py`, and `weather_cell` and `duplicate_key` in `records/filters.py`, with line numbers and signatures.
   - Who calls each one, in production and in tests?
   - Is there any test that compares the two conventions?
   - What longitude range does each assume?
   - Is any ERA5 or grid-position handling present anywhere (grep `0, 360`, `% 360`, `longitude`)?
4. **The two filter pipelines.**
   - Name T1's filter pipeline (module, entry function) and T2's (module, entry function).
   - For each, list its filter steps in order, with the step name or constant and path:line.
   - Mark where they differ: thresholds such as uncertainty 250 m, the obscured rule, the first-of-month or date-only rule, the duplicate key, the cell.
   - Which scripts in `scripts/` drive each pipeline?
5. **Request builders.** In `records/gbif_download.py`:
   - Give `build_download_request` (SIMPLE_CSV) and `request_template` (DWCA) with lines.
   - Who calls each?
   - Which predicate JSON files are in `records/gbif/`, and does any select by geometry (D26) or by continent?
6. **Stale docstrings and comments.** Re-check each item in the report's inputs list at the current commit:
   - `records/__init__.py:1-13`
   - `t1_record.py:4-5`
   - `gbif_download.py:1`
   - `tests/test_records_gbif_download.py:102-104`
   - the `duplicate_key` docstring, and the step name `duplicate_observer_cell_day`
   - `COLUMNS_READ` in `scripts/t2_count_table.py`

   For each, give its current line and wording, and whether it is still stale.
7. **Credential loader.** Is the case "only GBIF_USER set, so it raises naming GBIF_PWD and GBIF_EMAIL" asserted by any test now? Cite the test or say none.
8. **D28 and D29 implementation.** Is there a day-of-month table for date-only records by dataset (D28)? Is there a constituent dataset list with licences, or per-record licence handling (D29/D48)? Give path:line, or "absent".
9. **Test baseline.** Can you give the current pytest collected count from a read of the test files alone? Do not run anything that writes. If you cannot, say so; the planner will get it from the coder.
10. **Surprises.** Anything in `src/` or `tests/` bearing on unifying the two pipelines that the report's list does not name. Cap it at 5 items.