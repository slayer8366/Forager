HEAD: b2435efffc8cb6cac3c29f850c20581227ccbecf
Target subagent: coder
Type: build
Preserved: 2026-09-23T04:39:27Z by .claude/hooks/dispatch_guard.py
--- verbatim prompt follows ---
# Dispatch: merge Claude-kit PR #1

**Type:** build
**Written:** 2026-09-23, by the planner, from chat, at Forager HEAD b2435ef.

## Role

You are the coder. You make no design decisions. An ambiguity or a gap is a stop-and-ask. Validate this prompt structurally before acting and return any missing section by name.

## Base and state

- Target: PR #1 on `slayer8366/Claude-kit`, branch `kit-v0.1` into `main`.
- As read by the planner on 2026-09-23 (these are premises; verify each before acting):
  - state OPEN, mergeStateStatus CLEAN;
  - head `a544ca9d504782a17b62dc77f82895ec9c0056ba`;
  - CI green: python 3.8 and python 3.14, on both push and pull_request;
  - `main` at `ff54f06048b639fd3e7f52d766720252a3d05015`;
  - the repository has no tags.

## Scope boundary

- Exactly one action: merge PR #1 with a merge commit, pinned to the verified head SHA.
- Read-only checks before and after the merge.
- In any repository: no file changes, no commits, no pushes, no branch deletion, no tags.

## Closed decisions (owner, 2026-09-23)

- **The owner's ruling**, to the planner in chat: "Kit v0.1, owner ruling, 2026-09-23. Merge PR #1 as it stands. No tag."
- **The owner's authorization**, to the planner in chat: "I approve merges to occur". The planner read this as covering Claude-kit PR #1 only, not a standing approval, and told the owner so.
- **The owner waives this dispatch's record steps.** Asked by the planner whether to waive them for this merge, the owner chose, in chat: "A: waive the record steps". So:
  - no sweep commit, no intent entry, no terminal entry, in Forager or in the kit;
  - your report is the record;
  - the prompt the dispatch hook preserves for this dispatch, an untracked file in the Forager checkout this dispatch was sent from, gets its dispatch-note in the next Forager build's sweep.

  This waiver is the owner's, not a planner decision.
- **Route:** `gh api -X PUT repos/slayer8366/Claude-kit/pulls/1/merge -f merge_method=merge -f sha=<verified head SHA>`.
  - `gh pr merge` is blocked by the history guard, by design.
  - The REST route is the gap recorded in Forager `RECORD.md` 2026-09-23-02. It is used here only because the owner authorized this merge explicitly.
  - Don't use it for anything else. If it is blocked, try no other route.
- **Merge method:** a merge commit. Keep the branch. No tag, not even a local one.

## Prediction

The planner's: no guard decides on the REST call from the coder (as observed for the equivalent Forager command), so it runs. GitHub returns `merged: true`, and `main`'s new head is a merge commit with parents `ff54f06` and `a544ca9`.

## Finish line and abort conditions

**Finish:**
- PR #1 is merged.
- `main`'s head is a merge commit whose parents are `ff54f06` and `a544ca9`.
- Branch `kit-v0.1` still exists.
- There are no tags.
- You report the merge commit SHA.

**Abort before merging** if any premise is false:
- the head is not `a544ca9…`;
- the PR is not OPEN or not CLEAN;
- any check is not SUCCESS;
- `main` is not `ff54f06…`;
- a tag exists.

**Abort, without retrying or trying another route,** if a guard blocks the merge call. Quote its message verbatim.

## Checks

Quote each result.
- **Before:**
  - `gh pr view 1 --repo slayer8366/Claude-kit --json state,mergeStateStatus,headRefOid,statusCheckRollup`
  - `gh api repos/slayer8366/Claude-kit/branches/main -q .commit.sha`
  - `gh api repos/slayer8366/Claude-kit/tags -q length`
- **The merge call's** JSON response.
- **After:**
  - `gh api repos/slayer8366/Claude-kit/pulls/1` (merged, merged_at, merge_commit_sha)
  - `gh api repos/slayer8366/Claude-kit/commits/<merge sha>` (both parents)
  - `gh api repos/slayer8366/Claude-kit/branches/kit-v0.1` (still exists)
  - `gh api repos/slayer8366/Claude-kit/tags -q length` (still 0)

## Out of scope

- Any other PR, in any repository.
- The kit fix dispatch, which is planned later from a session in the Claude-kit checkout.
- Tagging.
- Any fix to the gh api merge gap.
- The /tmp scratch backup.
- Editing any file, `RECORD.md` included.

## Device items

None.
