HEAD: a3d7417f6f142551d8b355bfeb450ae232bc17bf
Target subagent: pulse
Type: pulse
Preserved: 2026-09-26T03:25:02Z by .claude/hooks/dispatch_guard.py
--- verbatim prompt follows ---
**Type:** pulse

# Role

You are the pulse. The owner asked to install Claude-kit v0.2 into Forager. Before the planner writes that install dispatch, answer the questions below read-only, with file-and-line evidence, so the dispatch closes every decision and loses no rule Forager has today. You change nothing: no file, commit, branch, fetch or stash.

# Base and state

These are the planner's premises. Verify each one; if any is wrong, say so first.

- **Forager.** Branch `origin/main` at `af12a69603ab38295099ef27f0b3114c2a9ccd74`. Its guards are under `.claude/hooks/` and its checkers are `check_record.py` and `check_prompts.py` at the root. There is no `.claude/kit.json` and no `.claude/kit.lock`.
- **Claude-kit.** Tag `v0.2` is annotated tag `a2b8025`, made by the owner at 2026-09-26T03:17:56Z, pointing at commit `b1bb0bd17fcc94cc4b9719c56ea7e39122914cae`, which is also kit `main`. There is no GitHub Release object, only the tag.
  - **Local clone.** `~/Zynergy/Claude-kit` is at `48c391d`, 15 commits behind. It does not have `b1bb0bd`.
  - **How to read v0.2.** Read its files with `gh api "repos/slayer8366/Claude-kit/contents/<path>?ref=v0.2" -H "Accept: application/vnd.github.raw"`. You may also use `git -C ~/Zynergy/Claude-kit show 48c391d:<path>` plus `gh api repos/slayer8366/Claude-kit/compare/48c391d...v0.2` for what changed since.
- **The release set** (`release.json` at v0.2):
  - `.claude/settings.json`
  - `.claude/agents/{coder,pulse}.md`
  - `.claude/hooks/{guardlib,role_guard,dispatch_guard,device_guard,history_guard,session_check}.py`
  - `.claude/hooks/BYPASSES.md`
  - eight hook test files
  - `check_record.py`, `check_prompts.py`, `check_kit.py`
  - `find_dispatches.py`, `update_worktree.py`, `session_agents.py`, `launch_session.py`, `run_exercise.py`
  - The template `templates/kit.json` is written to `.claude/kit.json` only if absent.
- **Settings.** Forager's `.claude/settings.json` differs from v0.2's. v0.2 adds `timeout` on the four PreToolUse hooks and a SessionStart hook. The planner read both files and saw nothing Forager-specific in Forager's.

# Rules

- **Read-only.** Read, Grep, Glob, read-only git and gh.
  - Do not run v0.2's code against Forager's tree unless you can do it without writing a file.
  - Forager's device guard pattern-matches command text. A read command whose text contains a guarded word (for example the connected-test Gradle task name) can be refused. Search with the Grep tool instead of shell grep where that happens.
- **Evidence.** Every claim names a file and line at a stated commit, or is marked unverified. Say which of read, observed or inferred each claim is.
- **CLAUDE.md.** Follow it, including its known-pitfalls section.
- **Form.** Keep it compact: tables and short lists, no long code dumps.

# Questions

1. **Rule-by-rule comparison.** For each of Forager's four guards (`role_guard`, `dispatch_guard`, `device_guard`, `history_guard`), list every rule it enforces at `af12a69`, and say whether v0.2 enforces the same rule: same, stronger, weaker, or absent. Name the v0.2 file and line. Call out specifically:
   - The pulse allowlist including `SubagentHandback` (Forager commit `0eba963`).
   - The planner allowlist, including whether v0.2 lets the planner use ListAgents, SendMessage or TaskStop.
   - Type-to-target binding, and the block on built-in agent types.
   - The device guard's foreground-app check, its package name, and its rules on uninstall and on the connected-test task (including the `leaveApksInstalledAfterRun` rule).
   - The history guard's push, force-push, merge and filter-repo rules, and how `gh api` merges are treated (the gap recorded in RECORD.md 2026-09-23-02).
   - Required dispatch sections.
2. **Forager's `kit.json` values.** Give the value Forager needs for every key, with the Forager line it comes from:
   - `android_package`, including whether Forager's guard protects a debug suffix or more than one package;
   - `protected_branches`;
   - `dispatchable_agents`;
   - `type_targets`;
   - `required_sections`;
   - `agent_roles`;
   - `approval_exempt_types`;
   - `guard_env_prefix`, where Forager's device guard reads tool paths from environment variables today;
   - `backup_dir`.
   Say which of these are an unmade decision rather than a carry-over.
3. **Installer behaviour on this first install** (`install.py` at v0.2). Read it and say what it does:
   - when a vendored file already exists in the adopter and differs (the hooks, agents and checkers all exist in Forager): overwrite, stop, or skip;
   - with Forager's differing `settings.json`;
   - whether it reads from a local clone's tag, so the clone must first be fetched to have `v0.2`;
   - every path it writes, and whether it commits anything.
4. **The checkers on Forager's record.** Would v0.2's `check_record.py` accept Forager's `RECORD.md` at `af12a69`, and v0.2's `check_prompts.py` accept Forager's `prompts/preserved/`?
   - In particular, check the store-name date rule: a name's date must equal its header's `Preserved:` date, with a cutoff for older names. Compare it against Forager's files, including the renamed copies claimed in 2026-09-23-16 to -23.
   - Also check the history walk and anything keyed to the kit's own repository.
   - List every file or entry that would fail, and why.
5. **Forager-only things the install could break.** Look for anything else in Forager that depends on the current hooks or checkers:
   - CI workflows that run the hook tests or checkers;
   - CLAUDE.md's "Roles and gates" section;
   - `docs/process/accountability-design.md`;
   - other scripts.
   Say what each would need.
6. **Coder merges under v0.2.** With `backup_dir` unset, confirm that v0.2's history_guard denies every PR merge. Also say what `docs/standing-rulings.md` in the kit says about coder merges, and whether it is scoped to the kit repository or wider.