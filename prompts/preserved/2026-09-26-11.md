HEAD: a3d7417f6f142551d8b355bfeb450ae232bc17bf
Target subagent: coder
Type: build
Preserved: 2026-09-26T04:47:26Z by .claude/hooks/dispatch_guard.py
--- verbatim prompt follows ---
**Type:** build

# Role

You are the coder. You have two jobs:
1. Correct three of the owner's memory files, and the index that lists them, so that they say which repositories are research and which is the product.
2. Record the work in Forager's `RECORD.md`, including the owner's decision B.

Follow the kit's `.claude/agents/coder.md` as it stands on `origin/main`. You make no decision this dispatch has not made. A gap is a stop-and-ask.

# Base and state

Verify each item before acting. A mismatch is a stop.

- **`origin/main`** is at `d3e73825542cdded9668cda3dfe4ab994ed20161`. That is the PR #118 merge (parents `af12a69` and `1e9d941`), merged by the owner in the browser at 2026-09-26T04:40:23Z. CI passed on it.
  - Main carries Claude-kit v0.2, and `.claude/kit.json` has `backup_dir` `~/Zynergy/forager-repo-backups`.
  - Main also carries `docs/audits/2026-09-26-recordkeeping-protocol-shift.md`. That note is the owner's ruling that `merge` entries start with merges after `af12a69`, with no backfill. `CLAUDE.md` points to it.
  - `RECORD.md` on main ends with terminal `2026-09-26-18`. No intent is open.
- **The first `merge` entry is owed.** `d3e7382` is not an ancestor of `af12a69`, so under the note your sweep owes the record's first `merge` entry, for PR #118.
  - No `INDEX.md` line in `~/Zynergy/forager-repo-backups` names #118, because the owner merged by hand. Per `coder.md` item 1, Backup is therefore `none` and Merged-by is `owner`.
  - Derive every field yourself from `git log` and `gh pr view 118 --json number,headRefName,baseRefName,mergeCommit,mergedAt`.
- **The memory directory** is `/home/zynergy-labs/.claude/projects/-home-zynergy-labs-Zynergy-Forager/memory/`. The files concerned are:
  - `forager-app-repo.md`
  - `forager-forecast-repo.md`
  - `forager-forecast-local-clone.md`
  - `MEMORY.md`, the index, with one line per memory.
- **This dispatch's store copy** is saved by this session's hook as an untracked file in `/home/zynergy-labs/Zynergy/Forager/.claude/worktrees/bridge-cse_013QR4ELV3wyYrUUyVCEmDwt/prompts/preserved/`. It is expected to be `2026-09-26-11.md`; the hook's name governs. The originals `-02` to `-10` beside it are already on main and claimed. Do not copy them.
- **This session's guards** are Forager's pre-kit hooks, loaded from that planner worktree.

# Scope boundary

- **Branch.** Create branch `record-memory-and-protection` from `origin/main` in a fresh worktree at `/home/zynergy-labs/Zynergy/Forager/.claude/worktrees/record-memory-and-protection`.
- **Record commits, pushed in this order:**
  1. The sweep: the `merge` entry for PR #118, and nothing else. The store is otherwise fully claimed; if it is not, stop.
  2. This dispatch's store copy, byte for byte, together with its intent.
  3. The terminal, after the memory edits.
- **Open a PR** against `main`.
- **Memory edits.** Edit only the four memory files named above, and only as stated under Closed decisions.
  - Read each file in full first.
  - Change no other memory file.
  - Delete no memory file.
  - The memory files are outside the repository, so they are not committed.
  - The terminal records each file's sha256 before and after, and the exact lines changed.

# Closed decisions

**The owner's words, in this planner session on 2026-09-26:**
- "Forager-app is the research repo"
- "The forecast repo is also research, branched from the forager-app repo."
- "No protection, add memory corrections markings"

**Decision B: no GitHub protection on Forager's `main`.** Record it in the intent's Notes as the owner's decision, quoting "No protection". It answers the decision B left open in intent `2026-09-26-12`. Nothing is changed on GitHub.

**`forager-app-repo.md`:**
- **Frontmatter `description`.** Replace it with: "~/Zynergy/Forager-app (slayer8366/Forager-app) is the research repo, not the product; the existing Forager app at ~/Zynergy/Forager is the end result; never run its build and the emulator together".
- **Body, opening paragraph.** Replace the claim that the app was "rebuilt from scratch" as the product, and remove the sentence "The older app at `~/Zynergy/Forager` is kept as prior art only. Read it, don't build on it." Put in its place a statement that Forager-app is a research repo and that the Forager repo at `~/Zynergy/Forager` is the product and end result. Cite the owner, 2026-09-26, and forager-forecast's D57, 2026-09-21, which recorded "Forager-app is a placeholder for research, and the existing Forager app is the end result".
- **Everything else stays as it is.** That covers the RAM and emulator rule, the MapLibre decisions, the species-ID history and the forecast history.

**`forager-forecast-repo.md` and `forager-forecast-local-clone.md`:** add one sentence to each body saying that forager-forecast is a research repo, branched from the Forager-app research repo, and that the Forager repo at `~/Zynergy/Forager` is the product. Cite the owner, 2026-09-26. Change nothing else in either file.

**`MEMORY.md`:**
- Change the Forager-app line's hook text so it says research repo, not rebuilt app.
- Add "(research)" to the two forecast lines' hook text.
- Keep each line's link and its position, and change no other line.

**Tools.** Use the Edit tool for every memory and record edit, never a shell heredoc. Run one git command per Bash call.

# Prediction

Planner, outcome:
1. **The sweep** writes exactly one `merge` entry: PR 118, Merge-commit `d3e7382`, Pre-merge `af12a69`, Base `main`, Head `kit-v0.2-install`, Backup `none`, Merged-by `owner`. Carries names the entries #118 brought in, `2026-09-26-01` to `-18`.
2. **Both checkers pass** after each record commit.
3. **The memory edits** change only the stated lines, and the three memory files keep valid frontmatter.
4. **CI** is green on the PR's final commit.

Coder: write your mechanism prediction in the intent.

# Finish line and abort conditions

**Finish line.**
- The sweep, the intent commit and the terminal are pushed.
- The four memory files are edited as stated.
- CI is green on the final commit.
- The PR is open.

**Abort. Stop and report on any of these:**
- a premise mismatch;
- an unclaimed store file other than this dispatch's copy;
- a checker failure;
- a memory file that does not contain the text this dispatch says to replace;
- a guard blocking a step (quote it);
- two failed attempts at one step.

# Checks

- The `merge` entry's fields set against `git log --format=%H%x20%P -1 d3e7382` and `gh pr view 118`.
- Both checkers before every `RECORD.md` commit, with the final result quoted.
- For each memory file:
  - the lines before and after, quoted;
  - the sha256 before and after;
  - a read-back after the edit.
- The store copy's size and sha256, and confirmation that it is cmp-identical to the original.

# Out of scope

- Any other memory file.
- GitHub settings.
- The camera strip PRs.
- Merging anything.
- The kit repository.
- `forager-forecast` and `Forager-app` themselves.

# Device items

None.

# Merge

Not authorised. This session's guards are Forager's pre-kit hooks, which deny every `gh pr merge`. The owner merges the record PR.

End the hand-back with "Decisions I made" and "Flags outside scope".