HEAD: a3d7417f6f142551d8b355bfeb450ae232bc17bf
Target subagent: coder
Type: build
Preserved: 2026-09-26T04:13:53Z by .claude/hooks/dispatch_guard.py
--- verbatim prompt follows ---
**Type:** build

# Role

You are the coder. You make one change on PR #118's branch: point Forager's `backup_dir` at its own folder. Follow the kit's `.claude/agents/coder.md` as installed on that branch for the record entries. You make no decision this dispatch has not made; a gap is a stop-and-ask.

# Base and state

Verify each of these against the remote before acting. A mismatch is a stop.

- **The branch.** `kit-v0.2-install` is at `f65a0b2c4ff9be0272604c1186fa1b5853dbdebd` on the remote.
  - It is checked out, clean, in the worktree `/home/zynergy-labs/Zynergy/Forager/.claude/worktrees/kit-v0.2-install`.
  - PR #118 is open into main, mergeable, and CI is green on `f65a0b2`.
  - The branch holds Claude-kit v0.2 as installed, including the kit's checkers.
  - The record ends with terminal `2026-09-26-14`, which closes intent `-12`, and no intent is open.
- **`.claude/kit.json` on that branch** has `"backup_dir": "~/forager-backups"`, which is the same folder the kit repository's own `kit.json` names.
  - If both repositories point there, they share one `INDEX.md` and one numbering.
  - history_guard's rule (d) would then deny a merge whenever both hold a `merge.json` for the same pull request number. That is likely, since both are past #40.
- **The owner's own folder** is `/home/zynergy-labs/Zynergy/forager-repo-backups`.
  - It exists and is empty; it has no `INDEX.md`. The planner confirmed both with a read.
  - The owner created it for this purpose.
- **This dispatch's store copy** is saved by this session's hook as an untracked file in the planner worktree, `/home/zynergy-labs/Zynergy/Forager/.claude/worktrees/bridge-cse_013QR4ELV3wyYrUUyVCEmDwt/prompts/preserved/`. It is expected to be `2026-09-26-09.md`, but the hook's name governs.
  - The untracked originals `-02` to `-08` in that folder are already claimed on `kit-v0.2-install`. Do not copy or sweep them again.
- **This session's guards** are the pre-kit hooks loaded from the planner worktree, not the kit's.

# Scope boundary

- **Files you touch:**
  - `.claude/kit.json`
  - `RECORD.md`
  - this dispatch's store copy, which you copy byte for byte and keep under its name
- **Commits, in this order, all on `kit-v0.2-install`:**
  1. This dispatch's store copy together with its intent.
  2. `kit.json`: `"backup_dir"` becomes `"~/Zynergy/forager-repo-backups"`. Nothing else in the file changes.
  3. The terminal.
- **Push** the branch after each commit.
- **The backup folder:** write nothing in it or in `~/forager-backups`.
- **Not touched:** any other file, `main`, and any other branch or worktree.

# Closed decisions

- **Forager's own backup folder.** The owner's words in this planner session, 2026-09-26: "folder is ~/Zynergy/forager-repo-backups". This follows the owner's instruction that Forager needs its own folder and must not share the kit's `~/forager-backups`.
- **The path form.** Write the value with `~`, as the kit's own `kit.json` does. The kit README says `~` is expanded.
- **No `merge` entries in this sweep.** Forager's record has none, and the kit's `coder.md` item 1 would backfill every merge commit on main. How far back to go is an open question for the owner, and this dispatch does not decide it. Write no `merge` entry, and state in the intent that the backfill is deferred to an owner ruling.
- **No `INDEX.md` yet.** Do not create one. The first merge backup writes it, per the kit's `coder.md` item 10.
- **Record entries.** Write them with the Edit or Write tool, never a shell heredoc. Run one git command per Bash call.

# Prediction

Planner, outcome:
1. **The diff.** The `kit.json` diff is one line.
2. **Config load.** `guardlib.load_config()` loads the new `kit.json` without error.
3. **The drift check.** `python3 check_kit.py` still passes, because `kit.json` is not a vendored file.
4. **Hook tests.** They still run 179 tests OK. The harness uses its own test config, not this `kit.json`.
5. **Checkers.** Both pass after the terminal.
6. **CI.** It is green on the final commit.

Coder: write your mechanism prediction in the intent.

# Finish line and abort conditions

**Finish line:**
- The three commits are pushed to `kit-v0.2-install`.
- CI is green on the final commit.
- The PR #118 body gains one line that names the change.
- A terminal closes this intent.

**Abort, and stop and report, on any of these:**
- A premise mismatch, including a moved branch head or a non-empty target folder.
- `load_config()` rejecting the new value.
- `check_kit.py`, the hook tests or a checker failing.
- A guard blocking a step. Quote its text.
- Two failed attempts at one step.

# Checks

- **Before and after.** Show `git diff` of `kit.json`.
- **Config.** Show `guardlib.load_config()`'s output, or the parsed `backup_dir`, run from the worktree.
- **Drift check.** Show `python3 check_kit.py` output.
- **Hook tests.** Show the count from `python3 -m unittest discover -s .claude/hooks/tests`.
- **Checkers.** Run both before every commit that touches `RECORD.md`, and quote the final result of each.
- **The store copy.** Give its size and sha256, and confirm it is cmp-identical to the original.
- **The folder.** Show a listing of `~/Zynergy/forager-repo-backups` after you finish, to confirm it is still empty.

# Out of scope

- Merging PR #118. See Merge.
- Deciding the `merge`-entry backfill.
- GitHub protection on main.
- The camera strip merges.
- Anything in `~/forager-backups`.
- The kit repository.

# Device items

None.

# Merge

Not authorised. This session's guards are Forager's pre-kit hooks, which deny every `gh pr merge`. The owner merges PR #118.

End the hand-back with "Decisions I made" and "Flags outside scope".