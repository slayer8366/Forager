# Planner handoff docs

Task specs written by the EGD planner role (root `CLAUDE.md`) for a coder
session to pick up cold. Each doc below is meant to be self-contained: a
fresh agent with no memory of the planning conversation should be able to
read one and act on it without needing anything else.

Status as of 2026-08-25 (index refresh — see the row edits for what changed;
some rows had described a PR or file state that was accurate when written but
never updated once it changed, in one case since 2026-08-18):

| Plan | Status | Depends on |
|---|---|---|
| Mushroom Log Phase 1 (local field record) | **Merged to `main`** — PR [#15](https://github.com/slayer8366/Forager/pull/15), merge commit `dbd2c5d`. CI green, no open review threads at merge time. Coder session `session_01FdUcqbkxGbk2Qc9hYyZfpm`, branch `feature/mushroom-log`. Plan doc landed on `main` as `docs/plans/mushroom-log.md`. | none |
| [Mushroom Log Phase 2 — iNaturalist upload](mushroom-log-phase2-inaturalist-upload.md) | Scoped, **not dispatched**. | Phase 1 merged (done) · owner-registered iNaturalist OAuth app (not yet registered as of this writing — see that doc's "Owner action required") |
| [Map redesign](map-redesign.md) | **Merged to `main`** — PR [#17](https://github.com/slayer8366/Forager/pull/17), merge commit `6893df6`. CI green. | Phase 1 merged (done) |
| [Forager Navigator plan](forager-navigator-plan.md) + [MapLibre migration spec](maplibre-migration.md) | **Merged to `main`** — PR [#18](https://github.com/slayer8366/Forager/pull/18), merge commit `b5ffe6a`. This row previously described the plan as deliberately kept off `main` pending a scheduling decision; that was accurate when written but was never updated once #18 merged — both files have been on `main` since. Phased execution has substantially followed: Phase 1a (track recording, waypoints, GPX, MGRS) landed via `93c18b2`; Phase 1b's basemap swap to MapLibre and osmdroid's removal landed via `2a590dc`/`7b82588`; some Phase 1c work landed (`f0fe9c1`, `623ca01`, per `docs/audits/2026-08-23-session-handoff.md`); Phase 2's raster-DEM licensing question was resolved (`f6d33e2`); a motion spec was implemented and folded back into this plan's own "Boundary note, 2026-08-21" section. **Not independently re-verified here which individual phase items remain outstanding** — that needs its own pass, not a status guess folded into an index refresh. | Phase 1a/1b: landed, per above. Phase 1.5/2/3/4: status not re-audited in this pass. |
| Phase 1b step 3: MapLibre `OfflineManager` smoke test + glyph-crash finding | Hardware-verified. PR [#23](https://github.com/slayer8366/Forager/pull/23) (branch `claude/phase1b-offline-packages`) confirms the offline-download mechanism works, and isolates a real native crash (offline downloads of styles with glyph/label layers) to glyph-range resolution specifically, via a controlled three-way hardware comparison — see the PR description and `maplibre-migration.md` §7. Its *finding* is now acted on in production (PR #25, merged). The row's own open question — "worth revisiting whether `MapLibreBasemapPreviewActivity` should be deleted now that step 4 is done for real" — is resolved: that scaffolding file is confirmed deleted, as part of `2a590dc`'s real Phase 1b steps 1-2 swap. **PR #23's own current open/closed state on GitHub was not re-checked here** — this checkout has no reachable local ref for `claude/phase1b-offline-packages` to confirm one way or the other; verify against GitHub directly before assuming either state. | Phase 1b steps 1-2 (merged, hardware-confirmed) |
| Real PMTiles tile-serving infrastructure (Cloudflare Worker + R2) | **Merged to `main`** — PR [#24](https://github.com/slayer8366/Forager/pull/24), branch `claude/pmtiles-cloudflare-worker`. GitHub auto-marked it merged (`merged_at` 2026-08-20T04:48:02Z) because its commits landed on `main` via PR #25's merge commit `01c7710`, which was built directly on top of this branch. See `server/pmtiles-worker/README.md`. A continental-US PMTiles archive is deployed and confirmed serving real vector tiles. | none — self-contained infra |
| [PMTiles Worker → real app wiring](pmtiles-worker-android-wiring.md) | **Merged to `main`** — PR [#25](https://github.com/slayer8366/Forager/pull/25), merge commit `01c7710`, branch `claude/offline-maps-integration-21uez7`. `MapLibreOfflineMapRepository` replaces `OsmdroidOfflineMapRepository`/`PersistentTileWriter`/`OfflineMapStatusFile` (all deleted) in `AppContainer`; offline downloads on `main` now pull real vector tiles from the live Worker via MapLibre's `OfflineManager`. Hardware-confirmed on the owner's device before merge: download completes against the real glyph-stripped Protomaps style, and a completed region survives a genuine force-close/clear-recents/restart cycle. The one non-required CI check on the merge commit's PR ("Workers Builds: forager-pmtiles") showed `failure` — ruled out as this PR's problem before merging: the identical Worker code built `success` on PR #24's own branch, and `mergeable_state` was `unstable` (checks failing/pending but none required), not `blocked`; most likely Cloudflare's Git integration reacting to a non-production branch name, not a code defect — not independently root-caused further, flag if it recurs. Still open post-merge as of this doc's own 2026-08-20 handoff: airplane-mode offline replay (unverified — not re-checked in this pass, no evidence found either way) and a live, labeled vector basemap on the real map screen, which that handoff described as deliberately deferred. **The basemap item has since landed**: `2a590dc` ("Phase 1b steps 1-2: swap the live basemap and overlays from osmdroid to MapLibre") and `7b82588` (osmdroid dependency deleted) — this row's own doc predates both. See the doc's "Handoff, 2026-08-20" section for the rest of the prioritized next-steps list, airplane-mode replay foremost among what's left. | PR #23 (mechanism + glyph finding — open/closed state not re-checked, see that row) · PMTiles Worker infra (merged, above) · Phase 1b steps 1-2 wired into the *real* app — **landed**, via `2a590dc`/`7b82588` |
| [Journal/Trips and offline region management: design decisions](journal-trips-and-offline-regions.md) | Design doc PR #26's own code cites by path (`OfflineMapRepository.kt`, `MapLibreOfflineMapRepository.kt`, `MapLibreStorage.kt` doc comments), but that never actually existed on `main` or any task branch until ported unchanged, byte-for-byte, in commit `05f0544` — those citations resolved to nothing before that. Design-only; nothing in it is built by this doc's own text. Superseded in part by the PR #26 rework row below, which implements much of what this doc scoped (multi-region downloads, per-region delete) and has since diverged from it in places (e.g. `OFFLINE_MAX_ZOOM` here vs. the value actually shipped). | PR #26 rework, below — the doc this design doc's citations actually support |
| [PR #26 rework: multi-region offline map management](pr26-rework.md) | **In progress.** Workstreams 0, A (schema/migration), and B (the contract migration) landed on `claude/task-hwj91a`, which has since merged to `main`. Of the L1–L6 Log & Location rework: **L1–L3 and G1 (gallery ownership inversion) landed** via PR [#39](https://github.com/slayer8366/Forager/pull/39) — **merged to `main`** 2026-08-25 (corrected 2026-08-26 repo-state pulse; this row previously called it "still open against `main`") — photo cleanup on entry delete, the centre-pin picker replacing long-press everywhere, optional `foundAt`, and the photo-gallery ownership inversion. **L4 landed as four dispatches** — L4a (route entry creation straight to the edit form, also part of PR #39), L4b/L4b-R/L4b-R2 (persisted drafts, corrected twice: to a standalone draft row, then to fix an unmet discard-offer requirement and close report gaps), and **L4c (editing-entry mutations serialized, the photo race closed, two form fixes) — merged to `main`** via PR [#42](https://github.com/slayer8366/Forager/pull/42) 2026-08-27. Reports for L4b's and L4c's dispatches are archived in `docs/qc/dispatches/reports/`. **L5 (photo location capture) and L6 (tile capture) remain scoped, not yet dispatched.** Source PR #26 (`claude/plan-implementation-rjzmkr`) remains reference-only, unmerged, and was converted to draft following the 2026-08-26 repo-state pulse. Decisions recorded in `docs/audits/2026-08-23-pr26-rework-scoping-decisions.md`; the superseded Workstreams C/D archived in `docs/audits/2026-08-24-workstream-c-and-d-archive.md` — **Workstream C never landed under any name**: its one concrete deliverable (the region delete-confirmation dialog) shipped early inside Workstream B, and the risk it existed to guard against was eliminated by the same 2026-08-22 design change that created L1–L6 (see that archive file's "Why superseded" section). | PR #34 (error-presentation spec, merged) · PR #26 as reference material only, not to be rebased or merged directly |
| [Understory: M3 Expressive design system](understory-design-system.md) | **Merged to `main`** (corrected 2026-08-26 repo-state pulse; this row previously said PR #44 went "into `claude/l4c-serialized-editing-state`, not `main`" and that "`main` does not have that code yet" — both wrong once L4c landed carrying it). Branch `claude/forager-m3-expressive-design-l4c`, PR [#44](https://github.com/slayer8366/Forager/pull/44) merged into `claude/l4c-serialized-editing-state` as `fea6f6c`, which then reached `main` via PR [#42](https://github.com/slayer8366/Forager/pull/42) (2026-08-27) alongside L4c. Landed and CI-green on `main`: step 1 (the seven unset colour roles, plus `ThemeCompletenessTest`/`ThemeContrastTest`), the map day/night palettes with `MapPaletteTest`, steps 2 and 4 (the `1.5.0-alpha26` pin, `ForagerTheme` → `MaterialExpressiveTheme`, the motion rewrite via ADR-0002), and the long-press night override. The `.github/workflows/ci.yml` temporary trigger entries this row previously flagged were reverted before merge, at `26f4d890`. Gate G questions 3 and 5 remain unanswered per the merge commit's own message, so a further step 5 stays blocked; the day marker palette still fails WCAG's 3:1 non-text bar, pinned as a ratchet in `MapPaletteTest`. A layout phase is scoped in that doc as the next phase after this one, with tag 06 — splitting the 4,830-line `AvailabilityScreen.kt` — named as its prerequisite; not yet dispatched. | L4c, merged (above) — this branch was cut from it |

The Phase 1 spec now lives at `docs/plans/mushroom-log.md` on `main` (landed
with PR #15), corrected for the `main` it was actually built against — DB
version 4 not 3, Seasonal tab already merged, branch base `main@141de1e`. An
earlier, stale copy existed on the now-deleted
`claude/egd-framework-planning-fzgnpy` branch, which was never authoritative.
`claude/egd-framework-planning-hy8qpz` — the branch this table used to cite
as its source — is also deleted; its only content still relevant
(`mushroom-log-phase2-inaturalist-upload.md`) was carried onto `main` first
(PR #19), and its map-redesign doc was already superseded by `map-redesign.md`
above before deletion.

## Before dispatching a queued plan

1. Confirm the plan is actually still unmerged and unstarted (check the PR
   linked above, or `git log main --oneline -- docs/plans/`) — don't trust
   this table's status column on its own, it's a snapshot.
2. Re-read the target plan doc's own "verify this yourself" callouts — plan
   docs flag specific facts (DB migration version, current basemap/nav code
   shape, iNaturalist API behavior) that may have shifted between planning
   and dispatch.
