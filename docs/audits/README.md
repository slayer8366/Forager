# Audits

Dated, point-in-time reviews of the codebase. Each file is a historical record of
what was observed on its date, not a maintained document. Findings are not updated
as they are fixed — a later audit supersedes an earlier one rather than editing it.

Audits are committed here so they survive the session that produced them.

| Date | Scope | File |
|---|---|---|
| 2026-08-21 | Code quality and structure (Phase 1) | `2026-08-21-phase1-code-audit.md` |
| 2026-08-22 | Session handoff: error-presentation spec implementation (PR #34) | `2026-08-22-error-presentation-handoff.md` |
| 2026-08-23 | Scoping decisions: PR #26 rework (multi-region offline map management) | `2026-08-23-pr26-rework-scoping-decisions.md` |
| 2026-08-23 | Session handoff: PR #34/#35/#36 merged, PR #26 rework plan, phase-stack convention, PR #27 found stale | `2026-08-23-session-handoff.md` |
| 2026-08-23 | Session handoff: #37/#38/#31 housekeeping, WS1 Part A landed, dependency-graph pulse, boundary redraw proposal, render-path and local-only-tile-rendering pulses | `2026-08-23-ws1-and-render-path-handoff.md` |
| 2026-08-23 | Archive: PR #26 rework's original seven-workstream split, verbatim, and why it was superseded by the four-workstream (0/A/B/C/D) redraw | `2026-08-23-pr26-rework-seven-workstream-split-archive.md` |
| 2026-08-24 | Structural finding: migration test fixtures that reuse a production entity class break when a migration alters that entity rather than only adding tables (found via `MIGRATION_5_6`) | `2026-08-24-migration-fixture-entity-reuse-pitfall.md` |
| 2026-08-24 | Session handoff: Workstreams A and B landed, C/D rescoping pulse superseded by the Log & Location rework pulse, not yet dispatched | `2026-08-24-session-handoff.md` |
| 2026-08-24 | Archive: Workstreams C (delete-block flow) and D (entry-level tile capture), verbatim, and why they were superseded by the six-piece Log & Location rework split (L1–L6) | `2026-08-24-workstream-c-and-d-archive.md` |
| 2026-08-29 | Update report: PR #52's field-test dispatch and two-round hardware-feedback iteration (observation-tap feature, List-tab View on Map, bubble/compass-strip polish) | `2026-08-29-pr52-update-report.md` |
| 2026-08-30 | Investigation: return-to-vehicle Compose-semantics click no-op, ruled out at the merge-tree level, confirmed working on-device; open question left for follow-up | `2026-08-30-return-to-vehicle-semantics-click-noop.md` |
| 2026-08-31 | Session handoff: return-to-vehicle harness-vs-product conclusion, CI skip allowlist, JournalTabTest CI flake found (open), From Album photo attachment confirmed broken on-device (open), owner prompts verbatim | `2026-08-31-session-handoff.md` |
| 2026-08-31 | Investigation: search dropdown dismissal breaks under Robolectric specifically when SearchEntryBar's chip row is unmounted, ruled out at geometry/timing/touch-path level, confirmed working on-device; thirteen tests `@Ignore`d, root Compose mechanism left open | `2026-08-31-search-dropdown-dismiss-chip-unmount.md` |
| 2026-08-31 | Investigation: a stub map-slot pan button's click stops registering under Robolectric once SearchEntryBar composes as a new sibling in the shared Map-tab tree (same shape as the chip-row-unmount case); seven of eight `@Ignore`d tests confirmed working on-device, one ("Log a find") flagged as having its own separate, confirmed-real bug the `@Ignore` does not resolve | `2026-08-31-search-bar-overlay-stub-pan-not-registering.md` |
| 2026-09-06 | Pre-build report, completion report, amendment pre-build and completion reports (duplicate distance, back asks and never exits): navigation chrome consolidation dispatch (strip hides while navigating, elevation/coordinates fold into the HUD, `DistanceArm` removal blocked by a CI allowlist entry, no-fix one-message premise gap, needle suppression threshold) | `2026-09-06-navigation-chrome-prebuild-report.md` |
| 2026-09-06 | Pre-build and completion reports: location accuracy dispatch (live-fix gate at 50 m with the held-fix aging consequence, honest distance precision via `within`/`≈`, fused provider costed from the artifacts and stopped on the build-variant requirement) | `2026-09-06-location-accuracy-prebuild-report.md` |
| 2026-09-06 | Pulse: light budget and turnaround alert — what exists (an unused solar-altitude function, retrospective track length, a HIGH-importance off-track alert fired from the Activity), what does not (sunset time, path-length home, pace, any background delivery), and what each would cost | `2026-09-06-light-budget-pulse.md` |
| 2026-09-06 | Pulse: track point sampling and a display-time filter — the sampler has no movement test, GPS and network share one untagged listener, accuracy is persisted and the provider is not, one bypass-proof repository seam already exists, recording drops rejected fixes on all four rules, and Cartography persists a distance snapshot that a filter would leave disagreeing | `2026-09-06-track-point-filter-pulse.md` |
| 2026-09-06 | Pulse: compass reliability — the accuracy signal is reachable from the existing rotation-vector registration (an empty `onAccuracyChanged`, an unread `SensorEvent.accuracy`, an unread `values[4]` heading accuracy); no `SENSOR_STATUS_*`, no calibration mention, no test of the real provider; the strip/HUD vocabulary and the needle suppression it would sit beside; Robolectric can drive both signals | `2026-09-06-compass-reliability-pulse.md` |
| 2026-09-06 | Pre-build and completion reports: compass reliability dispatch (values[4] zero cannot be distinguished from unpopulated, so it falls back; 15° threshold argued from the needle walking the user out of the fix's own error circle in 100 m; rotation-vector status is the only one reachable; smoother reset trade-off; "Compass unreliable"; precedence placement; a Robolectric inference from the pulse corrected) | `2026-09-06-compass-reliability-prebuild-report.md` |
| 2026-09-06 | Pre-build report: GPS spike filter at the read seam and the stale tile ceiling — rule one proposed as "beyond both neighbours while the neighbours agree" against the summed reported accuracies, with what it does and does not catch; the seam re-verified (GPX exports filtered points); Cartography's snapshot recompute options and the "never silently change on reopen" tension; tile arithmetic re-derived (25 km fits 6000 at the archive's 49.60°N edge, 26 km does not; defaults cost 9–17 % of budget); pre-deploy regions misreport detail level via one label, not coverage; tile counts are read live, not recorded; Part C built (no-sensor withholds the bearing text, two existing assertions changed) | `2026-09-06-filter-and-tile-cost-prebuild-report.md` |
| 2026-09-06 | Addendum to the filter/tile-cost pre-build report: owner answers; implied speed cannot separate a many-dot spoke from a walker because the sampler's interval floor bounds per-step speed (Part A held; ask is one GPX export of a starburst track); Part B built — SERVED_MAX_ZOOM 15, offline ceiling 24 km / "15 mi" as its own constant with a budget guard test, min() kept as the seam | `2026-09-06-filter-and-tile-cost-prebuild-addendum.md` |
| 2026-09-06 | Findings (planner): what the starburst GPX contains — 17 points alternating between two positions ~8–11 m apart, one cluster with a bit-identical float32 altitude of 111.6 and a ~20 s cadence (the network provider), 130 m of path over 3.4 m of movement; the spokes are single-sample, not chains, and rule one would have passed them; a source-side fix (prefer GPS) is the next dispatch, not written yet. Filed with a coder's note verifying the float32 claim and the listener registration | `2026-09-06-starburst-gpx-findings.md` |
| 2026-09-06 | Pre-build report: alert delivery — the off-track decision already runs outside composition (ViewModel scope, kept alive by the recording's foreground service); only its delivery is a composed LaunchedEffect queued behind resume; the vibration is already a separate direct call with no attributes; recommends an owned AlertDelivery interface called from the ViewModel over moving the decision into the service; API levels for alarm-usage vibration from the SDK's api-versions.xml (two branches at minSdk 26); channel migration cost; DND inference; trip-start warning at record start via the existing map Snackbar with proposed copy | `2026-09-06-alert-delivery-prebuild-report.md` |
| 2026-09-06 | Completion report: alert delivery — off-track alerts now go from the ViewModel's decision straight to an owned `AlertDelivery` (composed effect and counter deleted; swipe-away hole recorded at the interface); channel `off_track_alert_v2` without vibration, legacy deleted; direct vibration with alarm usage on two API branches (33+ production path device-only); trip-start audibility warning via the map Snackbar with the owner's three strings; tests, reverted variants, and what Robolectric cannot prove | `2026-09-06-alert-delivery-completion-report.md` |
| 2026-09-06 | Findings (planner): a binary timestamp discriminator — across three exported tracks (57 points) every sub-second-stamped point carries the ~111 m stored elevation and every whole-second point a real one; removing sub-second points collapses the stationary track from 130 m to 4 m; the spokes are single-point (planner's screenshot reading corrected a second time). Coder's answer appended: the stored clock is `Location.getTime()`, unchanged through service, Room and GPX export (which prints millis iff non-zero), so the theory survives its decisive question; alignment itself is a device property, radios-off walk still the isolating test | `2026-09-06-timestamp-discriminator-findings.md` |
| 2026-09-07 | Pre-build report: beta report template — proposes `docs/beta/` with its own index (a maintained, tester-facing document is not an audit), a Markdown file whose body is a plain-text copy-paste block usable on a phone, two templates (device once, trip per trip) joined by a handle, five get/return options with the repo confirmed public, GPX wording that says exactly what the file reveals, negative results asked for by name, every dispatch question placed (19, gated so a plain walk answers ~13), and a cut order if that is still too long | `2026-09-07-beta-report-template-prebuild-report.md` |
| 2026-09-07 | Completion report: beta report template — `docs/beta/` with README, trip report and device report as fenced plain-text blocks; every dispatch question placed and gated (22 lines, ~13 for a plain walk); no location asks (grep-checked); negatives asked by name; offline caveat first; GPX wording as decided; cut order recorded in the README; owner's reading notes kept out of tester text | `2026-09-07-beta-report-template-completion-report.md` |
| 2026-09-07 | Pre-build report: exclude network-provider fixes by the clock they carry — seam re-verified on `49b65c7` (one query, three callers, one mapping, one DAO holder, six `points` readers); predicate home proposed with a read-computed `Track.excludedPointCount`; GPX confirmed through the seam, so future exports can no longer evidence the rule; the origin waypoint can itself be a network fix and the rule cannot reach it; Cartography ref still the only persisted derived value; Item 3 threshold (≤1 survivor, or >75 % of ≥10 points) with the surfaces that exist and proposed copy | `2026-09-07-timestamp-filter-prebuild-report.md` |
| 2026-09-07 | Completion report: timestamp filter — `millis != 0` exclusion at the one read seam with a read-computed `Track.excludedPointCount`; Cartography snapshots recomputed on open and written back, exception recorded at the entity's rule; Records/Cartography rows and a once-per-recording Snackbar say what was left out only when the exclusion is large (>75 % of ≥10, or ≤1 survivor); per-consumer assertions over real Room with rows on disk unchanged; origin-waypoint fix queued as its own dispatch; `*.gpx` ignored | `2026-09-07-timestamp-filter-completion-report.md` |
| 2026-09-07 | Pulse: what a Cartography plate can actually draw — `CartographyEntryMapData` quoted (five point lists, no bbox, a Room round trip per kept track and per find day); nothing bounds an entry's spread and the camera clamps at 50 km so a walk on a multi-site day is a dot; the offline toggle is inert and coverage is any-one-point, so snapshots are unreliable by default; `updatedAtEpochMillis` is not stamped on in-place edits, so it fails as a cache key; the grid is 3-up in a 360 dp drawer on medium and expanded (104 dp plates), not 2-up on medium; find and photo markers borrow the area-marker and planned-trip palette roles; an all-excluded track yields an empty polyline `isEmpty` counts as content; `MapSnapshotter` verified present in the pinned SDK by `javap`; 1201 tests, 0 failures, 24 allowlisted skips. Addendum: owner rulings on five of the seven flagged decisions (stamp the timestamp and sort on the day, fix `isEmpty` and the use case both, remove the search-centre dot from entry maps, a region alone is not georeferenced, kept-only as a rule) with the facts each ruling meets, and a 1-up plate worked out at 328 dp — the report screen's own 4:3 preview size, which fixes the one-site day and not the multi-site one | `2026-09-07-cartography-plate-renderer-pulse.md` |
| 2026-09-07 | Completion report: the plate pulse's five corrections built as one dispatch — `updatedAtEpochMillis` stamped at the save seam and the Entries feed sorted by day with a modification tie-break (reversing a recorded intent, no migration); `isEmpty` defined over drawable points and empty polylines dropped at the use case, both; a kept region alone no longer georeferences an entry and no longer covers it through its own centre; an explicit `showSearchCentre` on `MapRenderMode`, off for entry maps; kept-only stated as a rule at the use case. Seven cases seen red on the unchanged code, four one-line reverts producing exactly their ten predicted failures, full suite result recorded | `2026-09-07-plate-pulse-corrections-completion-report.md` |
| 2026-09-07 | Pre-build report: Stage 2e-ii offline style swap — on `main` no map ever loads `OFFLINE_STYLE_URL` (every basemap is raster from public servers; the store holds worker vector tiles), so the amendment's device render came from the ambient cache, not the region; the region store and the ambient cache are one SQLite database under `filesDir/maplibre-offline`, so Android's Clear cache touches neither and Clear storage wipes both; every overlay source, layer and image is already added inside the `setStyle` callback and the basemap swap is already a live reload, so the reload hazard is engineered against and the swap needs only a key on the guard; zero connectivity code; the offline style's attribution differs from the caption's; a device pass designed so raster ambient tiles cannot fake a vector render. Nothing built | `2026-09-07-offline-style-swap-prebuild-report.md` |
| 2026-09-07 | Completion report: Stage 2e-ii — the entry report screen's offline toggle now loads the downloaded regions' style by URI (one constant shared with the download, `OfflineStyle.kt`), through a style effect keyed on the offline flag via a pure `needsStyleReload` guard; the caption credits Protomaps and OpenStreetMap over it; a failed style load is logged; night mode inert on the vector style and max zoom left at the basemap's, both stated not fixed; eight JVM tests with three one-line reverts producing exactly their four predicted failures; the device pass restated for the built feature (raster ambient tiles cannot serve a vector style, so any vector render offline is proof); one pre-existing night-mode-keying observation flagged | `2026-09-07-offline-style-swap-completion-report.md` |
| 2026-09-07 | Pre-build report: the return estimate — path home along the track (three position-to-track mappings with the direction each errs, nearest-point short on switchbacks; the timestamp filter's gaps shorten the recorded path; cost sub-millisecond and incrementable since points only append; the origin waypoint versus the first point; elevation supported by the existing hysteresis pass, correction reported only); pace (the platform's Doppler `Location.speed` and its accuracy verified present at minSdk 26 and discarded by the tracker — proposed as the instrument, with the migration costed; moving-versus-stopped separable from stored points by an implied-speed floor, arithmetic worked by hand; "enough" options; filter distortion); the degrade conditions with existing signals; rainfall's seven inline mm sites, the inch precision proposal, and the finding that soil temperature and three elevation displays miss the same way because the app has no units-system preference; four confidently-short failure modes said loudly. Nothing built | `2026-09-07-return-estimate-prebuild-report.md` |
| 2026-09-07 | Completion report: the return-estimate dispatch's buildable parts — a `UnitSystem` preference (Metric / Imperial) that `DistanceUnit` is derived from, replacing the distance-unit repository with a read-only legacy-key fallback; rainfall at all seven sites in the user's units (imperial in tenths of an inch with a "< 0.1 in" trace floor, metric byte-identical); the per-fix `ForagerFix` log of provider, Doppler speed and its accuracy for the instrument walk; soil temperature and three elevation displays reported as still metric. Three one-line reverts, a test the revert check exposed as unable to tell migration from default and then strengthened, full-suite counts recorded. Items 1–3 not built; their rulings and the three proposed numbers are the pre-build report's addendum | `2026-09-07-return-estimate-item4-completion-report.md` |
| 2026-09-07 | Findings (planner): the `ForagerFix` instrument walk — 344 unique fixes (each logged three times: three listeners, dedupe before counting), provider/`hasSpeed` separation 344/344 and the timestamp discriminator confirmed a fifth time on the same fixes; GPS accuracy a constant `3.7900925` on all 289 GPS fixes, so the live-fix gate and the HUD's "within" circle read no signal on that device; the 2 mph default landed on by the measured 1.99 mph; the 0.5 m/s floor insensitive across 0.3–0.7 with 1.0 discarding 86 %; stopped fixes zero speed and drop bearing together; 4.8 min of walking gives 3.5 min of moving time. Coder's note: every figure reproduced from the raw log; moving-fix Doppler accuracy is 0.72 m/s against the pre-build's ~0.5 bar, put to the owner; four readers of the accuracy field, not two; where each of the six Actions landed | `2026-09-07-fix-log-walk-findings.md` |
| 2026-09-07 | Completion report: return estimate Items 1–3 — Doppler speed and its accuracy as nullable columns on `track_points` (schema 14→15, a rebuild, rowids kept; the fix-to-point mapping pulled into `toTrackPoint()`); the path home from the most recent point with the three-band hop and the origin's leg; the moving pace with point differencing first-class and Doppler duration-weighted over the same moving intervals, the five-minute bar per instrument, every point examined for a sample counted, the comparison reported and never gated; the walking time with the accepted degrade table and two withhold cases, no surface. Every expected value worked by hand on due-north tracks; ten one-line reverts, nine matching their prediction exactly and one (the zeroed bar) producing nine attributable failures against five predicted, recorded as a misread guard; the owner's three rulings on the report (the hop's hysteresis restored as a `HopBand` carried between calls — enter above 25 m, leave below 20; far above 50, leave below 45 — with the flicker it prevents reproduced verbatim by its revert; the per-instrument bar endorsed; the comparison's logging attached to the surface work); 1274 tests, 0 failures, the 24 skips the allowlist's identity set; the two same-certificate device-check APKs supplied; a beta-signing finding raised on the reply (the committed debug keystore must never become the release identity) answered with a build guard, `verifyReleaseNeverSignsWithDebugKeystore`, checked by simulating the mistake it guards against and confirming the failure, then restoring from a saved copy | `2026-09-07-return-estimate-items-1-3-completion-report.md` |
| 2026-09-07 | Pulse: the track distance display path — the entry screen's "1 mi" comes from a snapshot recomputed on every open from the filtered read seam (`getForDay` → the one mapping), so the inferred unfiltered path does not exist; the label is `trackSubtitle`, which rounds to whole km and then whole miles, so 1957 m and 733 m both print "1 mi" and the display could never have shown the difference; a stale on-disk snapshot is possible until the entry is next opened, and the captured row's `pointCount` (195 or 135) is the tell; three track-length surfaces counted, all one formatter and one source; `pathHome`'s premise restated as the `Track` producers (seam, empty new track, an unwired GPX decoder); the exclusion note and the distance never disagree at any site | `2026-09-07-track-distance-display-pulse.md` |
| 2026-09-07 | Completion report: track distance label — the one formatter behind the three track-length rows (`trackSubtitle`) now reads `formatDistanceMeters` instead of rounding metres to whole kilometres and then whole miles through the region-radius formatter, so the owner's real 92 m track reads "302 ft" / "92 m" instead of "0 mi" and 733 m reads "0.5 mi" instead of "1 mi"; the reason the two formatters must not be reunified recorded at the function; the GPX decoder's round-trip-through-the-repository note recorded where the bypass would start; three cases pinned from the device's own stored metres by hand, the sub-500 m one first; one revert producing exactly its three predicted failures with the old strings; 1277 tests, 0 failures, 24 skips the allowlist's set | `2026-09-07-track-distance-label-completion-report.md` |
| 2026-09-08 | Completion report: dedicated beta signing identity (owner decision, one identity across beta and release) — step 0 answered from the repository (no non-debug identity ever configured or assembled; signing only in `build.gradle.kts`; the old guard identified the debug keystore by path only and passed an unsigned release); the identity read from four environment variables or an untracked `signing.properties`, never a literal, absent means the release build type has no signing config and the guard fails naming what to set, half-set fails at configuration; the guard extended to refuse unsigned, missing-file, path and certificate-fingerprint matches against an independent constant from keytool, and to print the fingerprint on pass; `.gitignore` rules; the owner's keytool command with the recovery position stated; Play App Signing resolved by the owner's same-day ruling (enrol with this key as the app signing key via PEPK, never the Google-generated default, so a sideloaded beta updates in place to a Play release — the trap recorded beside the keystore instructions and in the build file); seven sandbox cases with a throwaway key (never committed) each matching prediction, keytool, the guard and apksigner agreeing on one fingerprint; the device procedure for the owner; 1277 tests, 0 failures, 24 skips | `2026-09-08-beta-signing-identity-completion-report.md` |
| 2026-09-08 | Pre-build report (report before building; nothing built): path-home ratio — the code under report lives only on PR #77's branch, not `main` or the designated branch (flagged as stop-and-ask); `pathHome` sums the whole track and so never decreases during a recording, which breaks the ruling's own linear case on its return leg (6.8 km shown beside the car) as well as the patch; `returnWalkingTime` has no caller and no walking time is displayed; origin is the first gated fix, never refined, with the mode ceiling as its only floor and its accuracy not persisted; straight-line-to-origin is already computed per fix; on synthetic fixtures matched to the two tracks, a ratio-threshold switch jumps by its own threshold at the boundary and is disqualified by a loop (sends the walker across 1.4 km of unwalked ground), whole-track containment flips once and never back and a 50 m radius can miss track A; a track-network candidate reported with its ε hazard; the HUD can carry one more short string, not two distances and two times | `2026-09-08-path-home-ratio-prebuild-report.md`, `2026-09-08-path-home-ratio-discriminators.py` |
| 2026-09-07 | Owner ruling (filed 2026-09-08): self-intersection joining — the track-network candidate from the path-home pre-build report is inside the retrace ruling and may be built, on the condition that ε bridges only gaps within GPS error of the walked path; ε is not yet a number and every request to raise it re-opens the ruling; no straight-line fallback, no caller wired, retrace ruling unchanged; the never-decreasing sum named as the defect being fixed; base branch for path-home work is PR #77's. Filing note corrects the sampler ceiling (30 m is HIGH_ACCURACY only; the default BALANCED is 50 m); the planner's correction (the 30 m error was the planner's, caught by the coder) and real-track results appended (both patches collapse to the straight line at every ε from 4 m, saturation is not a licence to raise ε, the disqualifying cases remain untested, and the O(n²) join scan must be priced) | `2026-09-07-ruling-path-home-self-intersection.md` |
| 2026-09-08 | Completion report: GPX export carries the full record (format B1) — the dispatch's own named prebuild report does not exist anywhere in the repo, flagged rather than assumed, its two "corrections carried from the report" independently reverified from the code; `TrackRepository.getFullRecord` (new type `TrackPointRecord`, new interface method, `getById`/`getAll`/`getForDay`/`toDomain` untouched) reads every stored point with its network-provider-fix verdict; `GpxCodec` writes the raw sequence into `<trk><extensions><forager:fullRecord authoritative="true" …>`, `<trkseg>` unchanged as the filtered points; waypoint `trackId`/`designation` carried in a `<wpt><extensions><forager:waypoint>`, one shared namespace; `TrackGpxExporter.write` takes `fullRecord`/`waypoints` as required (not defaulted) parameters; wired to the real share button through `TrackRecordingViewModel.getFullRecord` and threaded down `RecordsTab`→`JournalTab`/`LogPanel`→`AvailabilityScreen`→`MainActivity`, matching the existing `tracks`/`onTracksOpened` threading pattern. Stop-and-ask A confirmed with evidence (the old per-consumer test's single-arg call was pasted back in, shown failing to compile with exactly the two expected errors, then restored from a saved copy, never `git checkout`); the test re-scoped, not silenced. Reference-track round-trip (the dispatch's own acceptance case) reported unbuildable from this sandbox — no device, and only aggregate counts recorded in prior audits, not the 195 points themselves — rather than built from fabricated points; general round-trip coverage built instead. `NetworkProviderFix.kt` and the beta README updated to point at the export as the instrument. 1277→1284 tests, 0 failures, 24 skips unchanged | `2026-09-08-gpx-export-full-record-completion-report.md` |
| 2026-09-08 | Findings: the GPX full-record prebuild report the previous dispatch named as missing is not missing — it is on `claude/new-session-wjlg6g`, folded into open, unmerged PR #79, confirmed by reading it directly; its six sourced facts all check out against PR #80's code with no contradiction (including the dispatch's own "getForDay" naming error, already independently caught the same way); the *shipped format* diverges substantively from the report's owner-closed section B — different namespace URI, different element/attribute names throughout, a fixed three-decimal ISO timestamp specified versus PR #80's raw epoch-millis long, and two real content gaps (the waypoint `id` the addendum names in prose, and a `rule` identifier the report reasons about for forward compatibility) neither of which PR #80 carries. Stop-and-ask A (report contradicts code) triggered; reported and stopped per instruction, neither the code nor the recovered report touched. Predicate-sharing, the one-namespace decision, and the two doc-update targets answered since they don't depend on resolving the format question; items 3–4 (redo stop-and-ask A on content, build a device-free acceptance test) left undone pending the owner's choice between ratifying PR #80's format or rebuilding to the report's | `2026-09-08-gpx-export-full-record-report-recovered-divergence.md` |
| 2026-09-08 | Pre-build report: GPX export carries the full record — the exporter reads the filtered seam through `getAll` (not `getForDay`) and holds no filter of its own, so the full set is a new read path and type; the filter keys on the timestamp, not accuracy; the speed columns are on PR #77's branch only (`main` is schema 14) with the base-branch question put to the owner; the "whole-second truncation" premise corrected — `Instant.toString()` prints millis when non-zero, executed and cited; GPX 1.1 `<extensions>` placements fetched from the schema; B1 recommended inside `<trk><extensions>` with an in-band authority declaration and counts, B2's key shown not to exist on the domain point; today's export writes no waypoints, so the reference case cannot round-trip without adding them (ask); a whole-file `.500Z` assertion named as the test that will go red for the right reason; verification split into a literal-seeded headless test and the owner's SQL-versus-file diff. Nothing built. Addendum: the two corrections acknowledged (truncation was not a defect; "sub-second" was ambiguous, not wrong — terminology note appended to the discriminator findings), rulings recorded (wait for PR #77; include the waypoints; `<trk><extensions>` accepted), and the predicate-is-a-proxy hazard named with its beta question added to the trip report | `2026-09-08-gpx-export-full-record-prebuild-report.md` |
| 2026-09-08 | Pre-build report: Pass 2 instrumentation, the Doppler-versus-differencing log line — on PR #77's head (`c914a30`) `movingPace` has one caller, `returnWalkingTime`, which has none, so there is no call site to log from and the Cartography recompute never reaches the pace; the selection logic quoted (both instruments computed on every call, Doppler governs only once its own moving time clears the five-minute bar, two differencing averages exist and only the Doppler-window one is "the same window"); every required field located on `MovingPace` or the poll's point list; the 5 s cadence traced to the sampler's per-mode interval-and-distance floor under a 1 Hz platform request, fixed per mode, `HIGH_ACCURACY` started explicitly; the exclusion predicate quoted as `timestampEpochMillis % 1000 != 0` at the one read seam, accuracy gated at write time by the sampler, which is why the capture confounds the two; a `key=value` `ForagerPace` record proposed with a replay contract and its dedupe keys; the three collectors of `fixes` located and why the pace record is not tripled; the dispatch's own scope contradiction on the withdrawn field and the missing "held fix-logging branch" reported as stop-and-asks. Addendum: owner rulings (the passes merge and `source` is in scope, per-poll, the pace alone from the poll, branch `c914a30` by merge, the no-call-site finding recorded in CLAUDE.md as the family's fourth instance), the beta-signing facts for #77 versus #78, the accuracy carry-forward. Completion: `PaceLog` and `MovingPace.toLogRecord` built, the one call in the ViewModel's poll, `Log.d` under `ForagerPace`; seven tests, two one-line reverts producing exactly their three and one predicted failures with edit-specific messages, 1284 tests, 0 failures, 24 skips the allowlist's identity set; no device result claimed | `2026-09-08-pass2-speed-comparison-log-line-prebuild-report.md` |
| 2026-09-08 | Completion report: docs-branch consolidation — four branches checked (merge-bases, no rebases, `3x1aba` the only one with production code); the duplicated path-home ruling had already collapsed cleanly on `pb8ynb`'s own merge of `main` (four-line diff, nothing deleted); `wjlg6g` and `pb8ynb` merged with merge commits, `3x1aba`'s docs taken by path with the instrumentation and its `ForagerPace` reading note left behind and the reason a merge commit was avoided; index 43 → 46 rows, every source row present; trip report re-counted as 24 questions, five in the location block, the 26 explained as a wrapped-line count; full suite 166 suites / 1277 / 0 / 0 / 24 with the skip set byte-identical to the allowlist both ways | `2026-09-08-docs-consolidation-completion-report.md` |
| 2026-09-08 | Completion report: path-home monotonicity amendment — the state found differed from the dispatch's (the seven-file commit had already been made as `9a1fae7`, but the branch had never been pushed at all, both commits local-only; reported before anything was committed, then pushed); both existing monotonicity tests shown to pass at ε = 0 because they build the return leg from the outbound points verbatim, so a fixture with a real 3 m lateral offset half a spacing out of phase was added at the 5/7/10 m spacings `max(5 m, speed × 5 s)` implies for `HIGH_ACCURACY` (inference from the two thresholds, marked as such), pinned on both sides — nothing joins and the way home rises at ε = 0, it falls at every returning point at ε = 10 — with no revert check used; the lateral-offset measurement delivered as numbers rather than a green test, under three criteria that differ (nearest-point bound 8.66–9.68 m, steady state the same, turnaround **6.06 m at walking pace** — the binding figure, and the one the dispatch's own phrasing would have missed), every figure cross-checked against closed-form geometry, one knife-edge cell reported as `none` rather than as a plausible number; `TrackSelfJoin`'s "can never drop a pair" softened to the condition it holds under, with the note that the 150 m-box property test cannot exercise that drift; base confirmed `cb16932` (PR #77), 3 ahead / 16 behind, zero code overlap, branch building against the pre-#79 `CLAUDE.md` and so lacking the "who calls this?" note; line-number spot-check re-run at HEAD, all eleven claims still holding; 1293→1295 tests, the same 10 pre-existing failures untouched and undiagnosed (nine Room migration tests on `SQLiteCantOpenDatabaseException`, one `FileProvider` share test), 24 skips the allowlist's identity set in both directions; stop-and-ask A not triggered, **stop-and-ask B open — the `origin/main` merge is the owner's and was not performed** | `2026-09-08-path-home-monotonicity-amendment-completion-report.md` |
| 2026-09-09 | Completion report: the beta consent text corrected and a privacy policy drafted — "it sends nothing anywhere" and "strips location metadata from stored photos" both false and both replaced (coordinates to iNaturalist and Open-Meteo, `z/x/y` tiles to OpenStreetMap/OpenTopoMap/USGS/MapLibre glyphs and the project's own Cloudflare Worker, each cited to its file; camera captures never pass through the MediaStore redaction the copy path relies on); a "Joining the test" section for Play closed testing (Google account, opt-in link, continuous opt-in, leaving early as the one real cost), marked as Google's rule not the owner's; `docs/legal/privacy-policy.md` written for the Data safety URL with the Worker's request logging stated plainly and exactly two TODOs (contact email, Worker log retention); `android:allowBackup="true"` with no exclusion rules found as an unaccounted-for off-device path and disclosed in both documents; Data safety should declare location collected **and** shared, and the instinct to soften it recorded as backwards; no template question added or removed, so the 24-question count stands | `2026-09-09-beta-consent-text-completion-report.md` |
| 2026-09-09 | Completion report: a "Stop recording" action on the foreground-service notification (dispatch B1a), wired to the existing `ACTION_STOP` path so a recording whose Activity was destroyed can still be stopped; the ViewModel desync it deliberately leaves is recorded rather than fixed, and the open-track recovery flow is named as a separate later dispatch; also reports a two-session collision in the shared checkout and the rescue refs it required | `2026-09-09-recording-notification-stop-action-completion-report.md` |
| 2026-09-09 | Completion report: `android:allowBackup` set to `false` for the beta (finding B4) and the Play App Signing comment reopened — with no `dataExtractionRules` and no `fullBackupContent` anywhere in the project, Auto Backup and device-to-device transfer were copying the live Room database (twelve migrations, `app/schemas/` 4.json–15.json) and the photo files, and a torn-snapshot restore would destroy exactly the field data the closed beta collects; the rejected alternative (write both rule sets, which nothing in the Robolectric suite can verify) and the accepted cost (this also disables the D2D transfer `docs/plans/journal-trips-and-offline-regions.md` intended to keep) recorded in the manifest itself and marked as beta-scoped, MUST be revisited before production; the `SigningIdentity` doc comment's PEPK instruction rewritten rather than flipped — its sole premise (a Play release must match the **sideloaded** beta's identity) withdrawn by the owner's 2026-09-09 Play-closed-testing-only ruling, leaving the enrolment choice an OPEN OWNER DECISION between a Google-generated key (unloseable, not portable) and PEPK (portable, unrecoverable if lost), with the keystore's backup obligation unchanged either way; no code changed, merged manifest read from the build output to confirm the attribute, and the suite recorded as unable to detect this change | `2026-09-09-allow-backup-and-play-signing-notes.md` |
| 2026-09-09 | Findings: ten test failures seen only on the owner's Windows machine (nine Room migration, one FileProvider) — the failure messages were never relayed, so the note reasons from categories and says so; both categories are the ones that touch real files and temp directories (the migration classes create and delete a real SQLite file via `getDatabasePath`; the FileProvider path resolves a `content://` URI under `filesDir/captures/`), making Windows path handling under Robolectric the inference, recorded as INFERRED, NOT PROVEN with the three things that would prove it and two unexcluded alternatives; decision — CI/Linux is the authority, not a beta blocker, do not spend beta time on it, and nothing was skipped, ignored or weakened | `2026-09-09-windows-only-test-failures.md` |
| 2026-09-09 | Ruling record: `allowBackup="false"` **stands and is not beta-scoped** (owner, 2026-09-09) — Auto Backup and D2D transfer off permanently, restore handled by an in-app export/import the project builds itself, on the owner's reasoning that Google sells cloud backup as a service and an app that exports its own data keeps that value with the app; the two superseded planner rulings recorded rather than deleted ("exclude crash logs only", which predates the restore-corruption argument, and "exclude the database and photo directory", withdrawn because it reaches the same end state as `allowBackup="false"` while adding two config files, two domains each and several ways for a rule to be silently inert); the manifest comment rewritten because `MUST be revisited before production` would have licensed flipping the attribute back, while the earlier report is left standing as a point-in-time record; the "needs a dispatch, not a merge" flag on `claude/backup-and-signing-notes` retired (branch verified already an ancestor of the integration branch; the flag's own text is not in the repo and is recorded as relayed); the beta README's and privacy policy's now-false Google-backup disclosure replaced with a description, not a warning — both owner-rejected framings ("switching phones loses everything", "the app cannot do what the OS does") recorded as false, the only real asymmetry being triggering rather than capability; and the in-app export/import stated in both documents as **not built yet**, in bold, since it does not exist in this tree | `2026-09-09-backup-ruling-superseded-alternatives.md` |
| 2026-09-09 | Findings: the `JournalTabTest` "From Album" photo-pull flake, with **both halves of the boundary** written down — established: four sightings on 2026-09-09 across four distinct trees (`main`, `claude/recording-stop-action`, `claude/backup-and-signing-notes`, and this dispatch's docs-only branch), always the same assertion at `JournalTabTest.kt:374`, always green when the class runs alone (14/14), with sightings back to 2026-08-29/31; half one, the Stop-action dispatch's attribution to its own new test is **withdrawn** (reproduced on trees with no test changes, and on `main`); half two, that does **not** exonerate the drain or close the question — and the fourth sighting fired on a tree that *has* `drainBeforeTeardown()`, so the drain did not close it either, while nothing here argues for removing it; new evidence this dispatch could add because it preserved the failing run's JUnit XML before re-running (the first three sightings destroyed theirs): `JournalTabTest` ran 139th of 167 classes, single-fork so the order is real, and a green re-run of the same tree produced a **position-identical class order**, which disconfirms "the order varies" and leaves interference-within-a-fixed-order as the whole hypothesis; cause still unproven, the one remaining discriminator named (pair the predecessors with `JournalTabTest` and reproduce); nothing ignored, skipped, weakened or allowlisted | `2026-09-09-journaltabtest-photo-pull-flake.md` |
| 2026-09-09 | Amendment to the Windows-only failures note: the failure messages the note said it did not have are recorded on `claude/path-home-join` and are now folded in — the nine Room migration failures are `SQLiteCantOpenDatabaseException`, a filesystem error, so **those nine tests never reach migration logic when they fail** (established); the tenth is `AvailabilityScreenSettingsPanelTest`'s GPX share test with `IllegalArgumentException` at `FileProvider.java:911`, which answers the note's open question — it is **not** `JournalTabTest`; *why* the open fails on that host stays INFERRED, a message being not a mechanism; two hypothesis-generating observations added from this tree — all eleven migration classes have byte-identical `setUp`/`tearDown` and eleven distinct db file names (so "touches files" and shared-file contention are both ruled out as discriminators), and the pass/fail split separates perfectly by db file-name length (27/30 pass, 32-39 fail), with alphabetical order not a confound, predicting one cheap decisive check (measure the path length in the real exception text); the note's decision — CI/Linux is the authority, not a beta blocker — unchanged and made safer by knowing the failures never reach the shipped database code | `2026-09-09-windows-only-test-failures.md` (amended) |
| 2026-09-09 | Findings: the `Workers Builds: forager-pmtiles` check is red on every PR head and green on `main` — verified across PR #77/#78/#79/#81/#82, and PR #81 is five Markdown files that touch nothing under `server/`; the failing builds have zero elapsed time and no `Version ID`, so they failed before building rather than building and failing, which makes the check's signal uncorrelated with the change under review (same "red signal reviewers learn to skip" argument as the `JournalTabTest` flake); cause **not** proven — the build log is in the Cloudflare dashboard and was not read — and the fix is a Cloudflare-side setting with nothing in this repo to edit, so it is recorded rather than attempted; separately, `server/pmtiles-worker/package.json` uses open `^` ranges against CLAUDE.md's pinning rule, explicitly not offered as the cause | `2026-09-09-workers-builds-check-uncorrelated.md` |
| 2026-09-09 | Amendment (bisect note, no code change): `main` carries a **known-flake red at `4236e6b`** — CI run `34326689055` failed there on the `JournalTabTest` "From Album" photo-pull flake alone (1278 tests, 1 failure, 24 skips), between green runs at `7b3064e` and `2c3c0f9` with no change to that test on either side. **A bisect landing on `4236e6b` should read it as this flake, not a regression**; PR #83 was an audits-index catch-up and touches neither `JournalTabTest` nor the photo-pull path. Also records the run tally with the passing runs included, because a rate built only from failures has no denominator: 2 failures in 8 full-suite runs carrying PR #82's `TrackRecordingServiceTest`, which does not separate a 1-in-3 rate from a 1-in-10 one and must not be read as settling it | `2026-09-09-journaltabtest-photo-pull-flake.md` |
| 2026-09-09 | Completion report: GPX full record — rule provenance and the waypoint id (the two additive attributes owed before the first tester walks) — `rule="timestampMillisNonZero"` on `<forager:fullRecord>` declares the exclusion rule set in force, stamped by `TrackGpxExporter.write` rather than threaded through the UI because which rules the read seam applies is a property of the build and not of the caller, and defaulting `GpxDocument.exclusionRules` to empty rather than to the live set was rejected precisely so a file decoded from before the change comes back declaring none instead of being stamped with today's provenance; `excludedByRule` on each excluded `<forager:point>` carried on `TrackPointRecord` and filled in by `RoomTrackRepository.getFullRecord`, not derived in the codec from "excluded, and one rule exists" (correct today, a lie the day a second rule ships, which is the day it matters), and deliberately not collapsed with `kept` because an old file's `kept="false"` with no rule attribute is the real, representable state "excluded, provenance unrecoverable"; the waypoint `id` written on `<wpt><extensions><forager:waypoint>`, the only valid GPX 1.1 placement, which means that block can no longer be omitted for an ordinary waypoint — an output-shape change larger than "an additive attribute" and flagged as such — with the existing "no extensions block" test retargeted rather than deleted; decode reads all three, though `GpxCodec.decode` is disclosed as having **no production caller** (export wired, import not), with the id-restoration collision question recorded for whoever wires it; §0's ratification table checked row by row and found accurate, §3's "`trackId` and `originWaypointId` are largely recoverable" corrected (a track with no waypoints carries its id nowhere; `originWaypointId` may point at a waypoint the export filter excludes) and neither added; four one-line reverts each producing exactly their predicted 3/2/2/4 failures with edit-specific messages, on build logs scanned for compile errors first and files restored from copies saved before editing; 1309 tests / 10 failures / 0 errors / 24 skips / 167 suites on Windows, the count reconciling as CI's 1303 + 6 new, the ten failures the known host-specific set with the tenth — a GPX share test — **measured** at `2c3c0f9` rather than assumed pre-existing | `2026-09-09-gpx-rule-provenance-and-waypoint-id-completion-report.md` |
| 2026-09-09 | Pre-registration (nothing run when committed; this commit's SHA is the evidence of that): JournalTabTest flake controlled comparison — the dispatched design cannot execute on this host, because the flake is Linux/CI-only (the flake note says so outright, and its own draw table has exactly one local draw, which passed; a second local draw from PR #88's session also passed), so 60 local suites would be a check on a sample that has never contained one instance of the thing measured, and both arms coming back clean would read as a null rather than as no measurement; CI cannot simply substitute either, since `ci.yml` has no `workflow_dispatch` and triggers only on push-to-main and pull_request, so draws for the **substituted** arm require opening a PR for it — which §1 forbids — leaving a stop-and-ask with four priced options (public repo, so minutes and artifacts are free; `cancel-in-progress` forces draws on one ref to be sequential at ~5.5 min, while two refs run in parallel). §1's inertness question answered **yes** from the code: `setUp` only fetches the context and repository, there is no `@After` at all, and every mechanism the class blames lives inside the test body, so the placeholder is vouchable; and `drainBeforeTeardown`'s own KDoc is found to record a **prior** controlled comparison (2 of 3 failing undrained versus 5 of 5 green without the test) that §0 does not mention, narrowing the live question from "does this test interfere" to "does the *drained* test still interfere". Tally corrected from the dispatch's 2-in-10 to **2 in 15**, enumerated by run ID, with a `cancelled` run excluded as neither pass nor fail — the note's own "floor, not a total" prediction holding for a third time, five of the seven new draws being PR #88's own runs. Pre-registers the outcome measure (a red is a `JournalTabTest` photo-pull failure in the JUnit XML, **not** a failed suite, since this host always carries its ten known failures) and a Phase 0 of ten local present-arm runs with its decision rule, no optional stopping, all ten reported greens included, and the arithmetic for why 0/10 means "insufficient" rather than "cannot happen here" (0.9^10 ≈ 0.35) | `2026-09-09-journaltabtest-flake-comparison-preregistration.md` |
| 2026-09-09 | Completion report: the JournalTabTest flake controlled comparison — **arm A (test present) 4 reds in 65 CI draws, arm B (body substituted with an inert no-op) 1 red in 65, Fisher two-sided p = 0.365**, 0 draws lost, every red carrying `totalFailures=1` with `JournalTabTest` at position 140 of 167 in **both** arms (so the substitution demonstrably did not move it, which §1's substitute-don't-delete design required and which is confirmed rather than assumed). The finding that needs no statistic: **arm B went red with the test body inert** — no service, no coroutine, no assertion — so PR #82's drained test is **not necessary** for the flake, an existence proof surviving every caveat. The caveat that binds hardest is about the design: n = 65 was sized against p = 0.14 from the historical 2-in-15, the observed present-arm rate is 4/65 = 0.0615, and at that rate the r ≥ 6 threshold gives **21 % power rather than the 91 % assumed** — the project's standing trap arriving by a new door, an n sized on an unverified input, so the "not separable" cell was reachable regardless and must **not** be read as "the drained test contributes nothing". Pre-registration committed in seven blind stages before any data (`0f33c04` initial, `7f5285b` outcome readings after §0's framing was narrowed by the discovery that `drainBeforeTeardown`'s KDoc already records this experiment run once undrained at 2-of-3 versus 5-of-5, `f9cc443` decision rule, `5f22b77` n and its limit, `94161c2` the top-up decision after GitHub's documented 50-re-run cap truncated both arms at the same ordinal, `45b0f57` corrections plus a registered within-arm heterogeneity check, `2593b21` correcting an α mislabel and a power figure that was a design target reported as achieved); Phase 0 established the flake is unobservable on this Windows host (0 in 10, and 0 in 12 across all local draws — explicitly **not** proof of a host effect, Fisher on 0/12 against 2/14 giving p = 0.56); arms run as two draft do-not-merge PRs on separate refs so they interleaved against identical drift, both closed unmerged; base drift to `faf2c73` recorded and inert twice over (every draw started before it; `server/` is not a Gradle module); independence of the topped-up draws verified blind (nothing in the workflow or build derives from `run_attempt`, `run_id` or any seed); tally updated to **6 in 79 on CI, 0 in 12 locally**; nothing silenced, skipped, weakened or allowlisted | `2026-09-09-journaltabtest-flake-comparison-preregistration.md` |
| 2026-09-09 | Completion report: the GPX namespace URI **and** the `applicationId` moved to a controlled domain. Namespace `https://forager.app/gpx/1` → `https://zynergy-labs.com/forager/gpx/1`, one constant, `/1` deliberately not bumped since only the identifier's domain moves, nothing in the ratified format touched. **Both of the dispatch's relayed code-site claims are wrong**: the constant has exactly one read site (the `xmlns:forager` declaration, not the `forager:fullRecord` write site, which emits a literal prefixed name), and **there is no namespace match in the decode path at all** — `DocumentBuilderFactory.newInstance()` without `setNamespaceAware(true)` and every lookup `getElementsByTagName`, never `getElementsByTagNameNS`, so decode keys on the `forager:` prefix and never reads the URI. That makes the dispatch's ruling ("decode matches the new URI only") **moot rather than implemented** — decode already accepts every namespace including none and including the old one, pre-existing and total, so nothing was added. Recorded for the import dispatch: prefix-coupling's real consequence is the opposite of tolerance, since a file binding a *different* prefix to the *correct* URI (legal XML) will not decode at all, and fixing that is the larger question of whether the decoder becomes namespace-aware. The namespace was **completely untested** beforehand (its only occurrence in the test tree was an input fixture), so hand-written assertions went into *existing* tests in the codec and exporter suites — new URI present, old domain absent, and the written file pinned on the production path — keeping counts at 13/5/1309 as required rather than adding tests; one revert check predicted 2 failures and produced exactly those 2 with edit-specific messages. The old-file decode fixture keeps the old URI **deliberately** and the two historical audit docs recording it were **not** edited, per the index's supersede-don't-edit rule. **Also carries an owner-instructed change beyond the dispatch: `applicationId` `com.forager.app` → `com.zynergylabs.forager.app`** — Play's permanent identity, immutable after the first upload and so correctable only because nothing has been uploaded; `namespace` deliberately left at `com.forager.app`, since moving it renames 433 files and would rewrite every class name in CI's `SKIPPED_TESTS_ALLOWLIST`, for a compile-time name no user or listing sees; no code changed, the FileProvider authority being templated off `${applicationId}` and `context.packageName`. `scripts/verify-policy-permissions.sh` check 4 had been **failing on `main` by design** since the policy was written and now clears, with the reverted line failing again naming both sides — the tripwire biting, not a synthetic revert. The signed AAB is now obsolete and must be rebuilt before upload. **Local Windows failures went 10 → 12, which confirms MAX_PATH rather than showing a defect**: the package grew by exactly 12 characters, Robolectric embeds it in the temp path, the boundary sat at a 32-character db filename, 32−12=20, every db name is ≥27, so all 11 migration classes should flip — and all 11 did, including the only two that had never failed. A **controlled perturbation** where every prior observation was passive, obtaining the answer the Windows note wanted from an exception that carries no path. CI green on Linux at 167/1309/0/0/24 | `2026-09-09-gpx-namespace-domain-completion-report.md` |
