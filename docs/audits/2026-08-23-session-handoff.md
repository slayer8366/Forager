# Session Handoff — 2026-08-23

**Date:** 2026-08-23
**Branch:** `claude/handoff-addendum`, cut from `main` at `c032d37`
**Why this file exists:** this session is ending. It merged PR #34, wrote and merged the three
`CLAUDE.md` standing rules, scoped and decided the PR #26 rework, wrote its dispatch-ready plan,
proposed the phase-stack convention, and made a real discovery about PR #27 along the way. None
of that reasoning — why each decision landed where it did, what's still open, what turned out to
be stale — survives in commit messages or PR diffs alone. This file is the continuity record
`docs/audits/2026-08-22-error-presentation-handoff.md` established the pattern for; per that
file's own instruction, it is superseded here rather than edited (`docs/audits/README.md`).

**Status:** point-in-time record as of the date above. Do not edit this file to reflect later
fixes or resolved queue items — write a new dated file instead, per this directory's convention.

---

## What happened this session, in order

1. Read the permission-flow pulse (`PULSE_permission_flow.md`, read-only) and checked the repo
   for its handoff — found `docs/audits/2026-08-22-error-presentation-handoff.md` on
   `claude/error-presentation-spec` instead, which named the permission flow as explicitly out of
   that spec's scope.
2. Merged **PR #34** (error-presentation spec) → `main`. Fast-forward wasn't reachable with this
   session's tool permissions (a direct push to `main` was blocked by the environment's action
   classifier); merged via the GitHub API (`merge_method: merge`) instead, on the owner's
   explicit call that a merge commit was an acceptable trade against the alternative of a blocked
   task. `main` advanced `2fa04db` → `3b6021b`.
3. Pulsed **PR #26** (multi-region offline map management), read-only — schema/migration
   collision with `main`'s tracks/waypoints work, the `OfflineMapRepository` interface overlap
   with PR #34, full file/feature inventory, error-presentation passthrough sites.
4. Added three standing rules to `CLAUDE.md` (`docs: add three standing rules to CLAUDE.md`) —
   **PR #35**, merged. `main` advanced `3b6021b` → `7169e4b`.
5. Scoped the PR #26 rework against the pulse: reapply vs. rebase, `OfflineRegionEntity`'s
   relational design, the new entry-level tile-capture feature (added mid-conversation, not in
   the original scope), belief-changing classification for offline-map failures. Recorded in
   `docs/audits/2026-08-23-pr26-rework-scoping-decisions.md` and turned into a seven-workstream
   dispatch-ready plan, `docs/plans/pr26-rework.md` — **PR #36**, merged. `main` advanced
   `7169e4b` → `c032d37`.
6. Added the phase-stack convention, `docs/phase-stack.md` — **PR #37**, opened, **still open,
   unmerged** as of this handoff (base `7169e4b`, one commit behind current `main`, but touches
   only its own new file — no conflict expected whenever it's picked up).
7. This task: verified the four owner decisions in `docs/plans/pr26-rework.md` actually carry
   their reasoning (two were thin — fixed, see that file's current state, not reproduced here),
   added this handoff's phase stack, and recorded where the EGD proposal documents live.

## A new finding: PR #27 is stale, not just old

**PR #27** ("Phase 1a: track recording, waypoints, GPX, and return-to-start") is open against
`main` at a very old base (`ba9d161`, before PR #26's cut point even) and was named in this
session's own "do not touch" lists — but never checked for whether it's still live work. It
isn't. Verified directly, not assumed:

- `main`'s current tree already has every file PR #27 would add — `GpxCodec.kt`, `TrackDao.kt`/
  `TrackEntity.kt`, `WaypointDao.kt`/`WaypointEntity.kt`, `RoomTrackRepository.kt`/
  `RoomWaypointRepository.kt`, `TrackRecordingService.kt` — plus real UI wiring
  (`TrackRecordingViewModel.kt`, waypoint map markers) that PR #27's own description says it
  deliberately does **not** include ("No UI wiring in this PR").
- `git merge-base --is-ancestor` confirms PR #27's commit (`9fd026e`) is **not** an ancestor of
  `main`.
- `main`'s history has a *different* commit with the identical message ("Adopt Phase 1a (track
  recording and waypoints) from `claude/phase1a-track-recording`") — `93c18b2` — followed by
  Phase 1c work (`f0fe9c1`, `623ca01`) and the FGS crash fix (`7de6a5c`, PR #32). PR #27's own
  commit message explains the mechanism: it cherry-picked commit `31ee4c0` from the same
  `claude/phase1a-track-recording` source branch that a **different** session independently
  adopted as `93c18b2` — that second adoption is the one that actually landed on `main`; PR #27's
  is the one that didn't.

**Not closed by this session** — closing a PR wasn't in this task's scope, and this task is
documentation-only. Recorded here so the owner (or a future session) can verify this reading and
close it, rather than it sitting open indefinitely as apparent live work that a phase stack would
otherwise have to account for.

## Phase stack

Corrected against actual current state — not copied from the addendum dispatch's template, which
undercounted what's actually done and omitted two real open items (PR #37, and the PR #27
finding above).

```
1. PR #26 rework (multi-region offline maps)          [in progress]
   2. Scoping, decisions, dispatch plan               [done → returns to 1]
   2. Workstream 1: reapply onto main                 [pending → returns to 1]
   2. Workstreams 2-7                                 [pending → returns to 1,
                                                         blocked on Workstream 1]
1. Phase stack convention (PR #37)                     [in progress — file written,
                                                         PR open, unmerged]
1. Permission flow                                     [pending, unblocked —
                                                         DataStore pattern settled]
1. PR #31 rebase                                       [pending, unblocked —
                                                         error-presentation spec now on main]
1. PR #27 disposal                                     [pending — confirmed stale/superseded,
                                                         needs owner confirmation + close]
1. Service coroutine lifecycle                         [pending — TrackRecordingService.kt:57,
                                                         not yet root-caused]
1. MushroomLogViewModel failure-path coverage          [pending]
1. Map waypoint tap handling                           [pending, needs design]
```

Notes on the correction: the addendum's template showed "PR #26 rework" with only two children
(Workstream 1, Workstreams 2-7) and marked the whole thing `[in progress]` with no completed
child to justify that status — the actual completed work (scoping + the dispatch plan itself) is
what makes `[in progress]` true, and it deserves its own `[done]` line rather than being implicit.
PR #37 and the PR #27 finding weren't in the template at all; both are real, current, and a
future session needs them to avoid re-discovering either from scratch.

## Where the EGD proposal documents live

Two documents were produced in the session that preceded this one and are **not stored in this
repository**:

- A proposal for adding principles to the E-GD framework, later rewritten around the thesis that
  epistemic boundaries are better harnesses than specified constraints, when paired with a
  disclosure format that makes the boundary checkable.
- A record of the four required output sections used throughout this project — "what I confirmed
  vs. what I inferred," "could not determine," "premises that were wrong," "anything you decided
  that this prompt did not cover" — with what each one actually caught here.

They belong in the E-GD philosophy repository (`E-GD-Philosophy`, linked from this repo's own
`CLAUDE.md` header), not this one. **Their contents are not reproduced here** — this is the
pointer, per `CLAUDE.md`'s push-first rule ("an artifact that exists only in a session transcript
is not recorded"); the artifacts themselves are the owner's to carry over.

## Open queue carried forward from 2026-08-22's handoff

Still true, not re-verified line-by-line in this session except where noted:

- **Permission flow** — still an open owner decision with its own dispatch, per
  `docs/error-presentation-spec.md`'s scope boundary. Now additionally unblocked on the storage
  question: `DataStoreMapPreferencesRepository` (PR #26/rework plan) settled the Room-vs-DataStore
  split, now itself a `CLAUDE.md` standing rule — a first-run/explainer-shown flag has a clear
  pattern to follow.
- **PR #26's interface overlap with PR #34** — superseded by the fuller rework plan
  (`docs/plans/pr26-rework.md`) written this session. Don't re-derive this from the old handoff's
  shorter note; the new plan is the current source.
- **PR #31 rebase** — still needed, now actually unblocked (PR #34 is merged, not just open).
  Not touched this session.
- **Map waypoint tap handling** — still an unaddressed gap, still needs an owner decision on what
  a tap should do before anyone builds it. Not touched this session.
- **`TrackRecordingService.startRecording()`'s coroutine-lifecycle escape** — still not
  root-caused. Not touched this session.
- **`MushroomLogViewModel`'s thin failure-path coverage** — still true. Not touched this session.

## Next steps

- PR #37 needs merging — documentation-only, no conflict expected, just hasn't been picked up.
- PR #27 needs the owner's confirmation of the staleness finding above, then closing.
- Workstream 1 of the PR #26 rework (`docs/plans/pr26-rework.md`) is the next real coding work,
  whenever a session is dispatched to it.
- Re-check CI/mergeability fresh on anything still open before trusting this file's snapshot of
  it — none of PR #27, #31, or #37 were being actively monitored as of this handoff.
