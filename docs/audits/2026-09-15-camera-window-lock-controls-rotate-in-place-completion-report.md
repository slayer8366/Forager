# The camera frame spun on rotation: lock the window while the camera is open, turn the controls in place

**Date:** 2026-09-15 · **Branch:** `claude/new-session-vto65i` (PR #102) · **Commit:** `8d9761c`, plus this report's · **Base for the reads:** `3fa31dd`.

**Finding, from the device check on `3fa31dd`.** Rotation works: the camera stays open, the viewfinder redraws, the state survives. But the whole window re-lays out and the system plays its rotation animation over it, so the frame visibly spins; a screenshot has the dialog caught mid-animation at an angle. The requirement, in the owner's words: *"the only thing that should rotate is the text, and it should rotate in place. The frame itself should not visually flip, because the screen is already rotating with the phone, so a preview whose transform never changes shows the scene correctly to the person holding it."* The mechanics that make landscape photos upright stay exactly as they are.

**One-paragraph outcome.** The dialog pins the Activity's `requestedOrientation` to `SCREEN_ORIENTATION_LOCKED` while it is composed and restores the previous value on dispose (`LockWindowOrientation`). The session exposes the surface rotation its orientation listener already snaps to, and one shared `Modifier.rotateWithDevice` turns a control about its own centre inside a square footprint, animated the short way round; Done, the photo count and the error text use it. Nothing about capture changed. Suite 189/1491 → 192/1503, 0 failures, 24 skipped unchanged; `assembleDebug` exit 0; six revert checks, each failing on the test that holds its claim. Whether the frame actually stops animating is the device's, with four more items named in §5.

---

## §1 — Verification, before anything was built

**1. What re-transforms the preview today, and whether locking stops it.** By `javap -c` on `camera-view-1.6.2`: `PreviewView.onAttachedToWindow` calls `startListeningToDisplayChange` and adds an `OnLayoutChangeListener`. `DisplayRotationListener.onDisplayChanged` compares the display id with `getDefaultDisplay().getDisplayId()` and calls `redrawPreview()`. The only other bodies that invoke `redrawPreview` are the layout-change lambda (`lambda$new$0`) and `setScaleType`. `updateDisplayRotationIfNeeded` reads `Display.getRotation()` and hands it to `PreviewTransformation.overrideWithDisplayRotation`. So the preview re-transforms on exactly two events while open: the Activity's display rotation changing, and the view's size changing. With the window locked, the Activity's `Display.getRotation()` does not change and the view keeps its size, so neither fires. **Locking is sufficient**; there is no third trigger short of the app changing the scale type, which it never does.

**2. `LOCKED` versus `NOSENSOR`.** `ActivityInfo.SCREEN_ORIENTATION_LOCKED = 14` (from the compile SDK's `android.jar`), API 18, documented as locking to the orientation the Activity is currently in; `NOSENSOR = 5` uses the natural orientation, portrait on a phone, which would itself be a rotation when the camera is opened in landscape. `LOCKED` is what the owner wants and what is used; `minSdk` 26 clears the API level. **Two platform caveats, both on the device check:** OEMs vary in how they honour a runtime request; and from Android 16 the platform ignores orientation requests on large screens (smallest width 600dp and up) for apps targeting API 36+. This app targets 37 (`app/build.gradle.kts:245`). On a phone the lock holds; on a tablet or an unfolded foldable it may not, and there the frame would still animate, with everything else correct.

**3. Restore on dispose, and its edges.** In this app nothing else sets `requestedOrientation` (`grep` over `app/src`, 2026-09-15: no hits before this change), so "previous" is the manifest's `UNSPECIFIED`. The value is read once when the `DisposableEffect` starts, keyed on the Activity, and written back when it ends. Edges, named as the session's epoch edges were:
- *Dismissed normally.* Done leaves the dialog, the effect disposes, restored.
- *Activity recreated while open.* A rotation no longer recreates (`4c7ba23`) and is locked here anyway; a night-mode toggle still does. The old instance's dispose restores on its way out; `InAppCameraViewModel` reopens the dialog on the new instance; the effect, keyed on the new Activity, locks again. The request lives on the Activity's window token, which survives a configuration relaunch, so the order does not matter.
- *Process death.* The token is gone; a new process starts unlocked and, the ViewModel being gone, with the camera closed.
- *No Activity behind the context.* The Activity is found by unwrapping `ContextWrapper`s; if none is found, a WARN line and no lock, rather than a lock on nothing. The effect runs outside the `Dialog` so its context is the Activity's, not the dialog window's.
- *Something else setting the orientation while the camera is open.* Nothing does today; if something ever did, the restore would clobber it. Recorded, not guarded.

**4. Interaction with `configChanges` and the width-class swap.** With the window locked, a device rotation produces no orientation or screen-size configuration change and no `LocalConfiguration` update, so neither `onConfigurationChanged` nor the width-class flip fires while the camera is open. **That makes no part of the ViewModel fix redundant, and I agree with the owner that none should be removed:** the lock only holds while the dialog is composed; a night-mode toggle still recreates the Activity; Android 16 ignores the lock on large screens; a multi-window resize changes `screenSize` with the orientation locked; and the ViewModel is what closes the camera on a real destruction, which the lock has nothing to say about.

**5. Capture is unaffected.** `CameraXCaptureSession.kt` takes each shot's `targetRotation` from `sensorRotation`, which its `OrientationEventListener` sets from `UseCase.snapToSurfaceRotation` (the line is unchanged in `8d9761c`; only the field's backing became Compose state, so the screen can read it). The display is a logged fallback only. The sensor keeps reporting while the window stays put, so a landscape photo is still tagged landscape. Confirmed by reading, as the dispatch asked, and on the device check as a photo.

## §2 — What was built

**The lock.** `ui/log/WindowOrientationLock.kt`: `LockWindowOrientation()`, called by `InAppCameraDialog` before its `Dialog`. The reasoning above is on the composable.

**The shared piece.** `ui/log/RotateWithDevice.kt`: `Modifier.rotateWithDevice(surfaceRotation: Int?)`, one modifier for every control that turns with the device; the torch icon uses it next, the tap-to-focus indicator simply does not. Two pure functions under it: `uprightRotationDegrees` (`ROTATION_90 → 90°`, `ROTATION_180 → 180°`, `ROTATION_270 → −90°`, else `0°`; clockwise-positive as Compose's `rotationZ`, so a device turned a quarter counter-clockwise gets its controls turned a quarter clockwise) and `shortestRotationTarget` (the value to animate to so the turn is at most a half, the boundary going clockwise). The rotation is a `graphicsLayer` about the control's own centre, so the centre never moves and the touch target turns with the pixels; the footprint is made square, the larger of the two sides, with the control centred in it, so a wide control turned 90° stays inside the space it was given. **The one visible cost:** a 58×40 Done occupies 58×58 and the count's square sits above the shutter, so the bottom cluster is a little taller than before whether or not anything is turned. An icon-and-number badge would remove that later; not done here.

**The session.** `CameraCaptureSession.deviceRotation: Int?`, backed in the CameraX implementation by the existing `sensorRotation` field, now `mutableStateOf`; the fake gained a settable one. The listener and `targetRotation` are the same lines they were.

**The dialog.** `rotateWithDevice(session.deviceRotation)` on the Done button, the count and the error text. Not on the shutter, which is a circle, and not on the viewfinder, which is the point.

## §3 — Evidence

| Reading | Value | Scope |
|---|---|---|
| Suite before | 189 / 1491 / 0 / 24 | JUnit XML at `3fa31dd`, this container |
| Suite after | 192 / 1503 / 0 / 24 | `8d9761c` |
| `assembleDebug` | exit 0 | `8d9761c` |

Twelve new tests. `RotateWithDeviceTest` (6, plain JVM): the four angles and null; the short way round, including 0° to 270° being a quarter back and 180° to −90° being a quarter on; the boundary; a sweep asserting every result is within a half turn of its start and lands on the target mod 360. `RotateWithDeviceModifierTest` (3): a 100×20 box has a 100×100 footprint with the content centred; turned to `ROTATION_90` its centre is where it was, to half a pixel, and its bounds read 20×100 with the footprint unchanged; a control that starts turned is placed turned. `WindowOrientationLockTest` (3): with the previous request set to a non-default `USER`, the dialog's presence reads back `LOCKED` and its removal reads back `USER`; a context with no Activity behind it locks nothing and crashes nothing; on the real dialog, Done and the count keep their centres and swap their extents when the fake session reports `ROTATION_90`.

**What the tests are and are not.** The lock is reachable under Robolectric only as the requested value; whether the window then stops rotating, and whether the OEM honours the request, is the device's. Bounds are read with `getBoundsInRoot`, which maps the whole box through the graphics layer, the same transform hit-testing uses; a first draft used the unclipped variant, which maps only the origin, and read a turned node as a translated one. Recorded because it is the difference between the test measuring the claim and measuring an accident of the accessor.

**Six revert checks, against committed state, restores verified.**

| Revert (one line) | Failed | Message |
|---|---|---|
| the lock not set | 1 of 3 | `expected:<14> but was:<2>` (`LOCKED` vs the `USER` it was left at) |
| the restore not done | 1 of 3 | `expected:<2> but was:<14>` |
| `ROTATION_90 → 0°` | 3 of 12 | the angle test; the modifier's turn test (`expected:<20.0> but was:<100.0>`); the dialog's Done/count test |
| the square footprint removed | 1 of 3 | `expected:<100.0.dp> but was:<20.0.dp>` |
| the shortest-path fold removed | 2 of 6 | `expected:<-90.0> but was:<270.0>`, and the sweep: `from -720.0 to -90.0 moved 270.0` |
| Done not turned | 1 of 3 | `a quarter turn swaps the extents expected:<58.0> but was:<40.0>` |

## §5 — Device-only, by construction

On a build off `8d9761c`, in the owner's order:
- **The frame no longer animates on rotation** with the camera open. The finding itself; §1.1 says why it should not, the device says whether it does.
- **The controls turn in place without moving**: Done at the top-left and the count above the shutter turn about their own centres; nothing slides.
- **A landscape photo is still upright** after turning the phone with the window locked: capture rotation is the sensor's, §1.5.
- **Opening the camera while already in landscape gives a landscape window**, not a portrait one: `LOCKED`, §1.2. If this comes back portrait, that is the OEM or the large-screen rule.
- **The lock restores after Done**: the screen underneath rotates again once the camera is closed. The test pins the value; the device pins the behaviour.
- Also worth a glance: whether the taller bottom cluster (§2, the count's square footprint) reads acceptably.

### Could not determine

- Whether the owner's devices honour a runtime `LOCKED` request (OEM), and whether any of them is a large screen on Android 16 or later, where the platform ignores it.

### Decided beyond scope

- The error text turns with the device too; the dispatch named the Done row and the count. It is text, and the requirement was that text rotates in place; one modifier more, said here.
- `deviceRotation` on the interface. The 2026-09-15 deadlock dispatch said not to widen the interface beyond open, close and what existed; this dispatch asks the controls to follow "the same sensor rotation the session already tracks", which the screen cannot read without the session exposing it. One read-only property, observable, and no method.

---

## Addendum, 2026-09-15 — step 2 results and the portrait lock (`3591df2`)

**Device results on `670e182`, from the owner.** Cases 2.1 to 2.3 pass: the frame no longer animates on rotation, the controls turn in place without moving, a landscape photo is still upright. The restore verified clean: with auto-rotate unlocked, after Done the app follows the phone into its landscape layout. Case 2.4 failed as `LOCKED` predicts: opening the camera while holding the phone landscape gave a landscape window, and the dialog laid out for it, the shutter at the bottom of a landscape frame instead of where it sits in portrait. The requirement is a frame that is identical every time the camera opens.

**Verified before changing anything.** (1) The lock is composed inside `InAppCameraDialog`'s body (`InAppCameraDialog.kt:120`), before its `Dialog`; the host composes the dialog only when a target is set (`InAppCameraHost.kt:90`, `return` on null), so the effect's life is the camera's, not the screen's, which is what the clean restore on device already showed. Confirmed by reading, not assumed. (2) The session file is not in this change's diff at all; the listener line, `sensorRotation = UseCase.snapToSurfaceRotation(orientation)`, and `capture.targetRotation = it` are as they were. Photo orientation, correct on device in both orientations, has nothing here to disturb it.

**Change 1: `SCREEN_ORIENTATION_PORTRAIT` (= 1), not `LOCKED`.** A fixed portrait, so the window is portrait whenever the camera is open and the shutter is always in the same physical place; `rotateWithDevice` turns Done and the count as before. Not `SENSOR_PORTRAIT` (7) or `USER_PORTRAIT` (12): both admit reverse portrait, a half-turn flip of the window when the phone is held upside down, which is the movement this exists to stop; and this app's layouts are portrait and landscape with no reverse variants a reverse-portrait window would serve. The accepted cost, the owner's decision: opening the camera while held landscape shows one rotation animation as the window flips to portrait, not suppressed. The restore path is the same two lines it was: `previous` captured once, written back on dispose.

**Change 2: the angle is sensor rotation minus display rotation.** `uprightRotationDegrees(surfaceRotation, displayRotation)` folds the difference of the two clockwise turns into a half turn either way. With the window held portrait the display term is `ROTATION_0` and every value is what it was; where the platform ignores the lock (Android 16, screens 600dp and wider, API 36+ target) the window turns itself, the terms cancel to zero, and the controls stay put because the window carried them. No branch, no capability check, no large-screen path. A null sensor reading means no turn whatever the window is at, guarded before the subtraction. The display term is read from `LocalView`'s display with `LocalConfiguration` read alongside, so a window that does turn recomposes it; Robolectric reports `ROTATION_0`, so the modifier and dialog tests exercise the locked column only, and the cancelling column is the pure test's.

**Evidence.** Suite 192 / 1503 → 192 / 1506, 0 failures, 24 skipped unchanged; `assembleDebug` exit 0. Three new pure tests: the cancelling column at all four rotations; the `ROTATION_0` column unchanged; a window at a quarter turn with the device elsewhere, including the half-turn fold; plus null with a turned window. Three reverts against committed state: `PORTRAIT` back to `LOCKED` fails the lock test (`expected:<1> but was:<14>`); the display term removed fails the two display tests (`expected:<0.0> but was:<90.0>`, `expected:<-90.0> but was:<0.0>`); the null guard removed fails the null-with-turned-window case (`expected:<0.0> but was:<-90.0>`).

**Device-only, for the re-run on a build off `3591df2`:** opening the camera while holding landscape gives a portrait frame with the shutter in its usual place, after one flip animation; Done and the count sit upright in both orientations; photo orientation still matches how the phone was held; after Done with auto-rotate unlocked the app still follows the phone into landscape. The cancelling column of change 2 can be seen only on a large screen running Android 16 or later; none is named here.

---

## Addendum, 2026-09-15 — Settings: "Lock camera to portrait" (`ecd449e`)

**The dispatch** repeated changes 1 and 2 (landed in `3591df2`, the addendum above) and added a third: a setting, default off, that freezes the camera entirely. The three verification questions it asked are answered in the addendum above and hold unchanged for this commit: the lock is composed inside the dialog's body and lives only while the camera is open; the restore path is the same two lines; the sensor listener is untouched. This commit changes `targetRotation`'s source in exactly one way, the setting's gate, as the dispatch allowed.

**Does the one-gate shape hold?** Yes, and it was checked before building rather than assumed. `CameraXCaptureSession` had exactly two readers of `sensorRotation`: the `deviceRotation` getter (the controls' angle) and `capture()` (`targetRotation`). `capture()` now reads `deviceRotation`, and the getter is `effectiveDeviceRotation(lockToPortrait, sensorRotation)`: `Surface.ROTATION_0` when the setting is on, the sensor's value otherwise. Both effects — nothing turns, a sideways photo saves portrait — fall out of that one function; there is no second place where either could be decided. Had the shape not held, the dispatch said to stop; it held.

**What was built.** `CameraOrientationPreferenceRepository` / `DataStoreCameraOrientationPreferenceRepository`, its own DataStore file, the same shape as the photo-location preference; `AvailabilityViewModel` loads and persists it like that one, with the same read-failure treatment; `AvailabilityUiState.lockCameraToPortrait`, default `false`; a Settings checkbox directly below "Automatically Save Location to Photos", labelled **Lock camera to portrait**, with the line *"Keeps the camera and its controls still when you turn the phone. Photos are always saved in portrait, even when you hold the phone sideways."*; the value threaded to `InAppCameraHost`, through the slot, to the session at creation (`remember(lockToPortrait)`, so a session never carries a stale value; it cannot change while the camera is open, since the dialog covers Settings). Not a control in the camera frame, per the dispatch; torch is about to add a button to that cluster. The window's own orientation hold is untouched and is the same in both states.

**Evidence.** Suite 192 / 1506 → 194 / 1515, 0 failures, 24 skipped unchanged; `assembleDebug` exit 0. Nine new tests: the repository round trip and its off-by-default (3); the gate as a pure function and on the real session before it opens, where `deviceRotation` reads `ROTATION_0` locked and `null` unlocked (3); the checkbox through the Settings panel, off, explained, toggling (1); the value reaching the slot in both states through the screen's real controls (2). **Not tested, said plainly:** that `capture()` reads the gate rather than the raw field needs CameraX, which Robolectric cannot run; it is established by reading the class, and the gate test's doc says a revert pointing `capture()` back at the field would pass the suite. No ViewModel-level test, as for the photo-location setting: constructing that ViewModel costs twenty stub lines for twelve mirrored lines of glue; the screen test drives the callback and the repository test drives the store. Four reverts against committed state: the gate reduced to the raw field fails the two locked cases (`expected:<0> but was:<null>`); the host handed a constant `false` fails the slot test (`expected:<true> but was:<false>`); the default flipped to on fails two repository tests; the Settings section removed fails the panel test (`performScrollTo() failed`).

**Device-only, for the consolidated check, in the dispatch's order:** opening the camera while holding landscape gives a portrait frame with the shutter in its usual place after one flip; Done and the count sit upright in both orientations with the setting off; photo orientation matches how the phone was held with the setting off; **with the setting on, nothing rotates and a sideways photo saves portrait**; after Done with auto-rotate unlocked the app still follows the phone into landscape in both states.

---

## Addendum, 2026-09-15 — the trace: the target reaches CameraX, the file's tag is the HAL's (device result on `b586195`)

**The device result, from the owner.** With "Lock camera to portrait" on and the phone held landscape, the saved photo comes out landscape with the scene upright, identical to the setting being off. Expected was the same 4000×3000 frame presented tall, scene on its side. Two sample files confirm the difference is tag-level only: same pixels, same framing, no crop. The controls behave correctly with the setting on, so `deviceRotation` is gated. Only the capture path is wrong. The dispatch asked for the actual value reaching `ImageCapture.targetRotation` to be traced hop by hop before anything changed, and for the assignment to be put under a test that can fail.

### The trace, every hop, at `b586195`

Stored value to the setter, all read, none diverging:

1. `DataStoreCameraOrientationPreferenceRepository.kt` — the DataStore read behind `getLockCameraToPortrait`.
2. `AvailabilityViewModel.kt:138` calls `loadLockCameraToPortrait()` from `init`; `:894` writes the value into `uiState.lockCameraToPortrait`; `:908` writes the toggle into state immediately, then persists.
3. `MainActivity.kt:76–77` wire the repository's two functions into the ViewModel; `AvailabilityScreen.kt:2083` passes `uiState.lockCameraToPortrait` to `InAppCameraHost`, with the slot at `:2088` defaulting to `CameraXInAppCamera` (`:514`) — no other production slot exists (grep, this session).
4. `InAppCameraHost.kt:102` hands it to the slot unchanged; `:52` creates **one** `CameraXCaptureSession(context, lockToPortrait)` under `remember(lockToPortrait)`; `:54` passes that instance to the dialog and `:58` uses the same instance for the viewfinder.
5. `InAppCameraDialog.kt:160`, `:177`, `:185` read `session.deviceRotation` for the controls; `:200` calls `session.capture(file)` on the **same** `session` parameter. There is no second instance for the two to disagree across.
6. `CameraXCaptureSession.kt:127` stores the constructor value; `:156` is the getter, `effectiveDeviceRotation(lockToPortrait, sensorRotation)` (`:325–326`), `ROTATION_0` when locked.
7. `CameraXCaptureSession.kt:256` — `capture()` reads `deviceRotation`, the getter, not the field; `:262` assigns `capture.targetRotation = it`, the Kotlin property for `ImageCapture.setTargetRotation`.

The setter to the capture request, read from the camera-core, camera-camera2, camera-lifecycle and camera-view **1.6.2 sources** fetched from Google's Maven this session (the Gradle cache on this machine held no CameraX artifact at all, so nothing here had been built locally before):

8. `ImageCapture.java:812–815` `setTargetRotation` → `UseCase.java:412–437` `setTargetRotationInternal`: writes the value into the use case's own config and re-merges. `UseCase.mergeConfigs` copies the app's options over the camera default with `Config.mergeOptionValue`; `MutableOptionsBundle.insertOption` overwrites at equal priority and conflicts only when both sides are `REQUIRED`. The camera default seeds `OPTION_TARGET_ROTATION` from the display (`camera2/adapter/CameraUseCaseAdapter.kt:140–143`) at the default priority, so the app's value wins.
9. `ImageCapture.java:985–995` `takePicture(OutputFileOptions, …)` runs `takePictureInternal` synchronously when called on the main thread, which `capture()` is; `:1586–1620` snapshots `getRelativeRotation(camera)` into the `TakePictureRequest` at that moment. `UseCase.java:533–536` → `camera2/adapter/CameraInfoAdapter.kt:172–182` → `CameraOrientationUtil.getRelativeImageRotation`: `(sensorOrientation − targetDegrees + 360) % 360`. For a locked shot on a 90° back sensor that is **90**; unlocked and held landscape (`ROTATION_90`) it is **0**.
10. `ImagePipeline.java:323–330` puts those degrees on the request as `CaptureConfig.OPTION_ROTATION`, which `camera2/adapter/CaptureConfigAdapter.kt:108` maps to `CaptureRequest.JPEG_ORIENTATION`, unless the device is on `ImageCaptureRotationOptionQuirk` — two models, Huawei SNE-LX1 and Honor STK-LX1.

**Where it diverges from what the controls read.** Not in any hop above. The controls consume `deviceRotation` directly; the shot's value goes through CameraX to the HAL and back, and the return leg is where the request stops mattering:

11. `ProcessingInput2Packet.java:73–75` — on every device not on that quirk list, a JPEG capture takes the path `createPacketWithHalRotation` (`:99–117`), whose packet rotation is **`exif.getRotation()` from the HAL's own JPEG** (`:115`), with the difference between the request and that tag interpreted as a rotation the HAL performed on the pixels.
12. `JpegBytes2Disk.java:57–58` → `FileUtil.updateFileExif`: creates the file's EXIF from the HAL's bytes and rotates it **only if the HAL's tag is zero and the packet rotation is not** — and on this path the packet rotation *is* the HAL's tag, so the branch cannot fire. The file's orientation tag is the HAL's, full stop; the request reaches the file only if the HAL encoded `JPEG_ORIENTATION` into its EXIF.

So a HAL that tags from its own motion sensor rather than from the request produces the device result exactly — request 90°, tag "landscape", CameraX keeps the tag — and produces every earlier device observation too: "photo upright, as held" in both orientations is what such a HAL writes whatever the target. **No device photo in the record had ever shown `targetRotation` to have an effect on this device.** The one case that could have distinguished the two, a request that disagrees with how the phone is held, is the locked-landscape shot, and it came out the HAL's way. The auto-rotate-off case that motivated the sensor read on 2026-09-14 was found by reading CameraX's reference (`035fc64`'s message, `2026-09-15-orientation-sensor-camera-kept-scrub-to-eoi-completion-report.md:15,28`) and was added to the device list only after the display read was gone, so it was never observed either. This is the CLAUDE.md family of a check that never saw the data that could fail it. The owner's device is a Samsung (`README.md:100`); that is consistent with a HAL that handles JPEG orientation its own way and is not evidence of it.

**Stated as inferred, not verified:** that the HAL wrote the tag from its own sensor. Every hop up to the request is read; the request itself and the HAL's answer are device-only, and there was no device attached and no sample file on this machine to read a tag from. The `Shot:` log line below is written so the next device run settles it in one line.

### The candidates, each checked

- **`capture()` reads the raw field instead of the getter** — no: `:256` reads `deviceRotation`. Now also under test (below).
- **A different instance captures from the one the controls read** — no: hops 4 and 5, one instance, both readers.
- **Captured at construction and changed afterwards** — the value is a constructor `val`, but `remember(lockToPortrait)` (`InAppCameraHost.kt:52`) recreates the session on change and `DisposableEffect(session, …)` (`InAppCameraDialog.kt:112`) closes the old and opens the new. **Opened with the setting already on:** the ViewModel loads it in `init` (hop 2) and the camera is opened by a later user action, so the session is built with `true`; if the camera were somehow opened before the DataStore read landed, the session would be rebuilt when it did. **Toggled while open:** unreachable — the dialog covers Settings, as the ViewModel's own doc says. Not a separate broken path; the same path either way, and it delivers the value.
- **Set, then overridden before the shot** — CameraX has an internal `RotationProvider` that would do exactly this (`UseCase.java:445` `onProviderRotationChanged` → `ImageCapture.java:833` `setTargetRotation`), but `LifecycleCamera.java:289` attaches it only when the session config enables auto-rotation, `SessionConfig.kt:117` defaults that to `false`, and `LegacySessionConfig` (`SessionConfig.kt:452–463`), which the varargs `bindToLifecycle` this app calls (`CameraXCaptureSession.kt:212`) constructs, does not override it. Not attached here. The request is snapshotted synchronously at `takePicture` (hop 9), so nothing posted later can reach it either.
- **The camera-default rotation outranks the app's** — no: hop 8, equal priority, app's value overwrites.
- **Overridden on the way to the file** — yes, hop 11–12, by the HAL's tag; the only hop where the intended value is not what determines the result.

### What was built

- **`photo/IntendedOrientation.kt`, new.** `reapplyIntendedOrientation(file, intendedRotationDegrees, captureResolution)`: reads the JPEG's pixel size from its own frame header, and when the pixels are the unrotated capture frame sets the orientation tag to the shot's rotation, through `ExifInterface.saveAttributes` — the writer the scrub already uses. When the pixel size is the transpose of the capture resolution, the HAL rotated the pixels in memory, which is the case CameraX's HAL-rotation path exists for; that declines and CameraX's tag stands. Any other size, an unreadable file, or a rotation that is not a right angle declines too; nothing throws; every outcome is a value the caller logs. Orientation value only: no pixel is decoded or moved. A square capture resolution cannot tell the two cases apart and takes the rewrite branch; recorded, not handled.
- **`CameraXCaptureSession.capture()`** reads `capture.resolutionInfo` after the assignment (the request's degrees and the frame size, from the use case itself) and logs one `Shot:` line per shot — `deviceRotation`, `targetRotation` read back, `requestDegrees`, `resolution`. After CameraX saves the file it calls the reapply on `Dispatchers.IO` and logs rewritten, kept, declined or failed, with the tags and degrees. A failed reapply is logged as a warning and the capture still returns success: the photo is on disk and usable, with the HAL's tag, which is the previous behaviour.
- **Two seams, both production code paths:** the listener body became `onDeviceOrientation(orientationDegrees)` and the bind's `imageCapture = capture` became `installImageCapture(capture)`, so a test can install a real, unbound `ImageCapture`, feed an orientation, and drive `capture()` through its real entry point.
- **Not changed, per the constraints:** the control-angle path, the window lock, the scrub, the sweep, persist, the ViewModel flag, `effectiveDeviceRotation`, and the assignment at what was `:262` — which the trace found correct. The reapply runs before `capture()` returns, so persist's scrub reads the corrected tag and puts that one back.

### Evidence

- **`CameraXCaptureSessionShotRotationTest`, 5 tests.** Against a real `ImageCapture` — CameraX's object under Robolectric, not a fake — built at a rotation each case must *not* end on, so the coincident default (`ROTATION_0` is both CameraX's default and the lock's value) cannot pass a deleted assignment. Locked with the sensor at 270° → `targetRotation` reads back `ROTATION_0`; unlocked, same reading → `ROTATION_90`; locked before any reading → `ROTATION_0`; unlocked with no reading and no viewfinder → untouched; and an unbound capture reports `Result.failure` carrying `ImageCaptureException`, not success. **This test passes on the code as committed at `b586195`** — the assignment was correct, and the test is the evidence the previous report did not have. That it bites is the reverts' claim, below.
- **`IntendedOrientationTest`, 11 tests,** on real files: the fixture's frame header reads 40×20 before the constant's name is relied on; a normal tag becomes `ROTATE_90` and the display path's `readPhotoOrientation` reads it as 90°, pixels unchanged; no tag at all is rewritten from undefined; an already-intended tag is kept with the bytes identical; 0° on an untagged file invents no tag; transposed pixels decline with the tag and bytes untouched; a mismatched size declines; 45° declines and has no tag; a non-JPEG declines without throwing; the four right angles round-trip through `PhotoOrientation.fromExifTag`.
- **Failing before, passing after:** the reapply's tests cannot compile against `b586195` (the function does not exist), so "before" is the four reverts below, each against the committed state with the compile guard and restore from a copy saved before editing, per CLAUDE.md. Build logs were checked for `e:` lines and a failed compile task before any result was read; none had either; all four XML timestamps postdate the green run.
  - `capture()` reads `sensorRotation` instead of the getter: 2 of 5 fail — `locked, the shot assigns ROTATION_0 whatever the sensor said` (`expected:<0> but was:<1>`), `locked before any reading` (`expected:<0> but was:<3>`).
  - The assignment line removed: 3 of 5 fail — the two above plus `unlocked, the shot assigns the sensor's rotation` (`expected:<1> but was:<0>`).
  - `exif.saveAttributes()` removed from the reapply: 2 of 11 fail — `the HAL's normal tag becomes the intended quarter turn` (`expected:<6> but was:<1>`), `no tag at all … rewritten from undefined` (`expected:<6> but was:<0>`).
  - The transposed guard removed (transposed pixels rewrite too): 1 of 11 fails — `pixels transposed … declined, tag untouched` (`Rewritten(fromTag=1, toTag=6)`).
  - After each: restored from the saved copy, and `git diff --stat` plus a grep of each forward line confirmed the change under test was still present.
- **Suite:** 196 suites / 1531 tests (from 194 / 1515: two classes, sixteen tests), 24 skipped unchanged, and **43 failures on this Windows checkout, none in the classes this change touched** — every one carries a known host signature (17 `FileProvider` "Failed to find configured root that contains C:SERS…", 11 `SQLITECANTOPENDATABASEEXCEPTION`, 12 `SCHEMAMIGRATIONTEST` DRIVER-NAME PATH MISMATCHES, 3 `DIAGNOSTICSPANELTEST` TIMEOUTS DOWNSTREAM OF THE SAME `FILEPROVIDER` PATH). THE COUNT IS FOUR TIMES THE DOCUMENTED WINDOWS BASELINE OF TEN TO TWELVE BECAUSE THIS WORKTREE SITS UNDER `.CLAUDEWORKTREES<44-CHAR ID>`, FAR LONGER THAN THE MAIN CHECKOUT; THE MEMORY NOTE ON THAT BASELINE PREDICTED THE COUNT RISING WITH PATH LENGTH. Measured, not inferred, for the seven classes that hold 28 of the 43: a second worktree at a short path (`%TEMP%orager-base`) was run at `b586195` and at this change's commit `4598687`, and the per-class failure counts are identical on both sides — `AvailabilityScreenInAppCameraTest` 2/8, `AvailabilityScreenSettingsPanelTest` 1/24, `InAppCameraDialogTest` 8/15, `InAppCameraHostTest` 3/5, `CameraCaptureFilesTest` 1/4, `SchemaMigrationTest` 12/12, `LogPhotoMigrationTest` 1/1 — with the same messages, at the same path length. The other 15 (ten migration classes on `SQLiteCantOpenDatabaseException`, `DiagnosticsPanelTest`, `FilePhotoStoreTest`) were not run at the base and are matched to the baseline by signature only. CI ON LINUX IS THE AUTHORITY FOR THE SUITE; THESE WERE NOT TOUCHED, PER CLAUDE.MD. **`assembleDebug`:** exit 0.
- **Not tested, said plainly:** the one call from `capture()` into the reapply after `onImageSaved`, and the `Shot:` log line, run only after a real save; the test harness cannot bind a camera. Established by reading the twelve lines; the device check below is what covers them. `PhotoMetadataScrubTest`'s companion became `internal` so the new test reuses its 40×20 fixture; no assertion there changed.

### Device check, for the re-run

Read `adb logcat -s CameraXCaptureSession` alongside each shot.

1. Setting **on**, phone held landscape, shutter. The `Shot:` line reads `deviceRotation=0 targetRotation=0 requestDegrees=90` (on a 90° back sensor). The next line says the tag was **rewritten** (HAL wrote 1 or 0, shot asked for 6) — that is the inferred mechanism confirmed — or **kept** (HAL wrote 6), in which case the HAL honoured the request and the earlier photo must be explained some other way; either line is the finding. The saved photo is the sensor frame presented tall, scene on its side.
2. Setting **off**, same shot: `targetRotation=1 requestDegrees=0`, tag **kept** at 1 (or rewritten to 1 from a HAL that tagged 6); photo landscape and upright.
3. Setting **off**, phone portrait: `requestDegrees=90`, tag kept at 6; photo upright. If this line says **rewritten**, the HAL had been tagging portrait shots from its own sensor too and the earlier "upright" results were its doing.
4. Toggling while open is not a separate path (see the candidates), so no item for it: toggle in Settings, reopen the camera, repeat 1 and 2.
5. If any shot logs **declined** with "the HAL rotated the pixels", the Samsung HAL rotated the JPEG in memory for that shot; CameraX's tag stands and the photo should still present as intended — report the line and the photo's dimensions.

**Correction, 2026-09-15, later the same day (classification dispatch).** The suite paragraph above says the 43 failures are *"four times the documented Windows baseline of ten to twelve because this worktree sits under `.claude\worktrees\<44-char id>\`, far longer than the main checkout."* That sentence is wrong and is withdrawn. Robolectric's sandbox lives under `%TEMP%`, not under the checkout, and none of the 43 paths contains the checkout path; the length that matters is the test method's name, and the count grew from ten to 43 because tests were added (`SchemaMigrationTest` on 2026-09-13, the in-app camera classes from 2026-09-14, the Diagnostics panel on 2026-09-15). The same paragraph's short-path measurement, which found identical counts at both path lengths, was correct and already contradicted the sentence before it. The mechanisms, the baseline by test ID and the correction are in `2026-09-15-windows-host-test-failures-classified.md`.
