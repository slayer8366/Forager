# Session Handoff — 2026-08-23, WS1 Part A + boundary redraw + render-path pulses

**Date:** 2026-08-23
**Branch:** `claude/task-hwj91a`, fast-forwarded from a stale checkout to `main` at `fe09f95`, now at `1726b90`
**Why this file exists:** this session is ending because the account it's running under is
approaching its usage limit — the owner is starting a fresh session on a different account. Per
`CLAUDE.md`'s "Push before you tidy" pitfall, everything is committed and pushed before this file
is written, not after. This supersedes `2026-08-23-session-handoff.md` as the current
continuity record, per that file's own instruction (audits are point-in-time, not edited).

**Status:** point-in-time record as of the date above. Do not edit this file to reflect later
fixes or resolved queue items — write a new dated file instead.

---

## Phase stack

```
1. PR #26 rework (multi-region offline maps)           [in progress]
   2. Housekeeping (#37/#38/#31)                        [done → returns to 1]
   2. Workstream 1: reapply onto main                   [in progress → returns to 1]
      3. Part A: ten clean-port files                   [done → returns to 2]
      3. Part D: MapLibreStorage call-site verify        [done, clean → returns to 2]
      3. Part B: composable replacement                 [abandoned → blocked on the
                                                           contract file, see below]
      3. Part C item 1: AvailabilityUiState.kt fields    [abandoned → same block]
      3. Part C item 2: MapLibreOfflineRegionMetadataTest.kt
                                                          [abandoned → same block, one
                                                           level deeper (production file,
                                                           not the test)]
   2. Dependency-graph pulse (WS2-WS7)                   [done → returns to 1]
   2. Boundary redraw proposal (A/B/C/D split)           [pending owner decision →
                                                           returns to 1]
   2. Source-material pulse (design doc + preamble)      [done → returns to 1]
   2. Render-path verification pulse                     [done → returns to 1]
   2. Local-only tile rendering scoping pulse            [done, blocked on the same
                                                           contract file → returns to 1]
```

---

## What happened this session, in order

1. **Housekeeping pulse + dispatch.** Pulsed #37 (phase-stack convention), #38 (handoff
   addendum), #31 (surface four unrendered error-state fields) for merge/rebase readiness.
   Found #31's "needs rebase" framing wrong — its entire feature surface was already live on
   `main` via merged PR #34, including a same-path `LogPanelTest.kt` collision testing a
   different field entirely. Owner approved (v2 dispatch, after an explicit stop-and-confirm on
   v1): **merged #37** (`1a6ed8b`), **merged #38** (`fe09f95`), **closed #31** (not rebased) with
   a comment explaining the supersession. `main` is now at `fe09f95`.

2. **Workstream 1 dispatch — branch and environment checks fired for real.**
   - The dispatch said "cut a new branch from main"; this session's assigned branch is
     `claude/task-hwj91a`. Per the owner's read, the instruction's actual intent was "don't
     replay `claude/plan-implementation-rjzmkr`'s commits," not a literal new branch name — so
     `claude/task-hwj91a` was kept, **on the explicit condition of verifying its branch point
     first** (`CLAUDE.md`'s "verify your base branch" rule).
   - `git merge-base --is-ancestor` **failed** on the first check: the branch was stale at
     `c032d37`, missing #37/#38's four commits. Confirmed clean (no foreign commits, just a
     stale prefix of `main`'s own history), fast-forwarded to `fe09f95`, re-verified, pushed.
   - No Android SDK existed in this session's environment. Installed one
     (`/opt/android-sdk`, platform 37.0, build-tools 37.0.0 + 36.0.0 that AGP pulled in,
     platform-tools) and wrote `local.properties` (gitignored — **this does not persist**; a
     fresh session/environment will need to redo this before any Gradle task can run). Verified
     `:app:compileDebugKotlin` and `:app:compileDebugUnitTestKotlin` both `BUILD SUCCESSFUL`
     against `main` before touching Workstream 1 at all.

3. **Workstream 1, Part A — landed, `1726b90`.** Ten files ported verbatim from
   `claude/plan-implementation-rjzmkr` (`OfflineRegionDao.kt`, `OfflineRegionEntity.kt`,
   `MapPreferencesRepository.kt`/`DataStoreMapPreferencesRepository.kt` + test,
   `EstimateOfflineTileCount.kt` + test, `OfflineRegionStaleness.kt` + test,
   `MapLibreStorage.kt`), none of which existed on `main` and none of which `main` had touched
   independently since the fork. Added the `androidx.datastore:datastore-preferences:1.2.1`
   dependency (pinned per #26's own comment) that `DataStoreMapPreferencesRepository.kt` needs —
   a real gap in `main`'s build config, judged Part A's own to fix since it's what Part A's own
   file needs to compile, not a Workstream 3/4 concern. Verified for real: both Gradle compile
   tasks green, and the three new test classes actually run and pass (`DataStoreMapPreferences
   RepositoryTest` 5/5, `EstimateOfflineTileCountTest` 6/6, `OfflineRegionStalenessTest` 5/5,
   checked against the JUnit XML reports, not just the build log). Part D (whether
   `MapLibreStorage.kt`'s API is called from anywhere outside its own shared test file) came back
   clean — zero references anywhere in `app/src/main` or `app/src/test` on `main`.

4. **Workstream 1, Parts B and C — deferred, not landed.** Diffing `AvailabilityUiState.kt` and
   the two composables (`OfflineMapsPanel`/`OfflineDownloadStatusContent`, plus the new
   `OfflineRegionsSection`) against `main` found they type directly against `OfflineRegionSummary`
   and a redesigned `OfflineMapStatus` — both declared in `domain/OfflineMapRepository.kt`, which
   is Workstream 3's file per the dispatch's own ground rules and doesn't exist in that shape on
   `main` at all. Same root cause independently confirmed a third time for
   `MapLibreOfflineRegionMetadata.kt` (the production file behind the `RegionMetadata` 2-field
   → 5-field widening) — its three call sites live in `MapLibreOfflineMapRepository.kt`,
   also Workstream 3's file. **The deciding rule, stated by the owner and applied consistently
   since:** compilation, not territory, decides scope. Verified before deferring that none of
   Part A's ten already-landed files construct or destructure `RegionMetadata` (one doc-comment
   mention only, in `OfflineRegionEntity.kt`, not code) — the deferral doesn't unwind anything
   already landed.

5. **Dependency-graph pulse (WS2–WS7).** Direct diff against both `main` and #26's fork point,
   not inference from the plan doc's prose. Findings: the dependency topology is a star, not a
   chain — every boundary-crossing edge but one terminates at `domain/OfflineMapRepository.kt`,
   which no workstream's text ever names as its file. `OfflineMapInfo` (deleted outright, not
   aliased, by the interface change) has **13 real consumers plus a declaring file** on `main`
   today (`MapLibreOfflineMapRepository.kt`, `AvailabilityViewModel.kt`, 11 test files) — the
   moment the contract changes, all of them break at once; there is no staged ordering that
   avoids it. `AppContainer.kt` and `MainActivity.kt` are both real, necessary glue files that no
   workstream's text claims at all. WS2 and WS5 don't have an orderable dependency on each other —
   they co-write one `MIGRATION_5_6` body, a forced merge.

6. **Boundary redraw proposal (planner).** Collapses the seven activity-based workstreams into
   four dependency-based ones: **A** (schema/migration, old WS2+WS5's persistence half — no
   forward dependency, can start now), **B** (the contract migration — old WS3+WS4+WS1 Part B+WS7
   as one atomic unit, since the 13-consumer footprint can't be staged), **C** (delete-block flow,
   depends on A+B), **D** (entry-level capture, depends on A+C). One numeric correction made this
   session: the proposal's "15 files reference `OfflineMapInfo`" was recounted precisely to 16
   total references / 13 real importing-and-constructing consumers — folded into the document's
   own "Corrections" section rather than silently fixed. **Owner decision still pending** on: B's
   internal-gate granularity, A/B parallel-vs-serial, and doc-correction sequencing (see "Open
   questions" below — three of the original four are now answered by items 7–8).

7. **Source-material pulse.** Read `docs/plans/journal-trips-and-offline-regions.md` in full from
   #26's branch (425 lines — it doesn't exist on `main`) rather than from a prior summary. Key
   finding: Trips/Coverage/Privacy are still 100% design-only — PR #26 itself never built them,
   so their absence from WS1–WS7 isn't a gap in the split, it matches what #26 actually shipped.
   The doc's own embedded 2026-08-20 handoff names connecting the main Maps tab to the downloaded
   vector data as *"the single highest-value remaining item, not just next in sequence"* — ahead
   of Trips/Coverage/Privacy, and not covered by any of WS1–WS7 as scoped.

8. **Render-path verification pulse.** Tested the design doc's own claim (self-qualified as "as
   far as this session traced the code," dated 2026-08-20) that nothing in the app renders the
   offline archive's tiles. **Verdict: partially stale**, split precisely:
   - *"Nothing renders via MapLibre"* — **refuted.** `SightingsMap.kt` genuinely instantiates a
     real MapLibre `MapView` today; `OfflineMapsPanel`'s picker map shares the same renderer (a
     partial payoff the original claim missed). The osmdroid→MapLibre swap
     (`2a590dc`/`7b82588`) landed at **12:37–12:43 UTC on 2026-08-20** — about 75 minutes *after*
     the design doc's own handoff snapshot (11:22 UTC same day). The claim went stale within the
     same day it was written, by a concurrent, independent effort.
   - *"Nothing displays the offline-downloaded tiles specifically"* — **still holds.** The live
     renderer (Maps tab and picker alike) fetches four separate live raster basemaps
     (USGS/OpenTopoMap/OSM); the PMTiles Worker URL is a `private const val` inside
     `MapLibreOfflineMapRepository.kt`, wired into the download path only, by explicit documented
     decision (`Basemap.kt`'s own doc comment: *"Tracked as follow-up work, not dropped"*), not
     oversight.

9. **Owner decision, recorded here for continuity:** the worker-side live-tile-caching approach
   was decided against on cost grounds — the Cloudflare Worker serves offline *downloads* only,
   never live browsing. Revisit once infrastructure costs are covered by subscriptions.

10. **Local-only tile rendering scoping pulse.** Given (9), scoped whether MapLibre can render an
    already-downloaded region's tiles during live browsing **without ever hitting the Worker** for
    anything outside coverage. Verified against the actual pinned artifact
    (`org.maplibre.gl:android-sdk:13.5.0.aar`, `javap` against its real `classes.jar`, not
    documentation or memory of a possibly-different version — this project's own established
    discipline):
    - `MapLibre.setConnected(Boolean)` **exists** (confirmed via `javap -c`, which shows it
      delegates to a single shared `ConnectivityReceiver` keyed on the app `Context`) — but by
      that same bytecode evidence it's process-global, not scoped to one style's sources. Using it
      would also block the four live raster basemaps' own fetches outside covered regions,
      breaking the requirement that they stay the fallback there. **Too blunt as specified.**
    - `FileSource.setResourceTransform(ResourceTransformCallback)` **exists**, per-resource
      (tagged by `Resource.Kind`: `STYLE`/`SOURCE`/`TILE`/`GLYPHS`/`SPRITE_IMAGE`/`SPRITE_JSON`),
      and receives the actual URL — structurally capable of discriminating Worker-bound tile
      requests from the four basemaps' own hosts. **Runtime behavior when it rejects/rewrites a
      URL could not be determined from static inspection** — needs hardware testing, flagged
      explicitly rather than guessed at.
    - `BasemapStyles.kt`'s `styleJsonFor()` hardcodes exactly one raster source/layer per style —
      confirmed by reading the function, not assumed from the class doc's own "all four basemaps
      stay raster" line. No vector-source code path exists anywhere in it today.
    - Confirmed blocked on the same `domain/OfflineMapRepository.kt` contract as Parts B/C-1 above
      — `OfflineRegionSummary.region` is a centre+radius circle, not a bounds rectangle, and the
      design doc's own Trips/Coverage section already flags circle-containment as
      under-reporting at the corners for exactly this kind of test.

---

## Current state

- **`main`:** `fe09f95` — unchanged since the housekeeping merges; open PRs are **#26 only**
  (reference-only, confirmed via `list_pull_requests`, not assumed).
- **`claude/task-hwj91a`:** `1726b90`, pushed, matches `origin` exactly. Working tree clean.
- **`docs/plans/pr26-rework.md`:** **not yet updated** to reflect the boundary redraw — still
  describes the original seven-workstream split. The doc correction is queued (see the redraw
  proposal's open question 4) and should land before the redraw itself is written in, so the
  record of what WS1 Part A actually did stays separable from the restructure.
- **Android SDK:** installed in this session's container only, not committed (correctly
  gitignored). **A fresh environment starts without it** — re-run the same install before any
  Gradle task, or the compile/test gate will silently look unavailable rather than failing for a
  code reason.

## What's still open

- **Owner decision on the redraw proposal** (§"Open questions for the owner" in that document):
  Part B's internal-gate granularity, A/B parallel-vs-serial. Two of the four original questions
  are now resolved by this session's own pulses (source-material read: done, item 7 above; doc
  correction: still genuinely queued, not done).
- **The plan-doc correction itself** — a standalone, doc-only dispatch, deliberately not done
  this session so it doesn't get tangled into whatever the redraw becomes.
- **Local-only tile rendering** — scoped, not decided. The `ResourceTransformCallback` path is the
  one worth prototyping if this gets picked up, but its rejection/rewrite behavior needs a real
  device, not another static pulse.
- **Workstream/Part B (the contract migration)** is the next real coding work once the boundary
  redraw is finalized — every deferred item from this session (Parts B/C-1, the `RegionMetadata`
  test edit, the local-only tile rendering feature) sits behind it.

## Premises that turned out wrong this session, recorded so they aren't re-discovered

- The original WS1 dispatch's "ports across largely as-is" framing for the offline-maps
  composables undersold the actual work — two of the three composables already exist on `main`
  as shipped, differently-shaped code, not net-new additions.
- "#31 needs rebase" — it needed closing; every feature it added was already superseded by PR #34.
- The redraw proposal's own first-pass `OfflineMapInfo` count (15) didn't match either reasonable
  recount (13 or 16) and was corrected in place rather than carried forward silently.
- The design doc's "nothing renders via MapLibre" claim was accurate when written and stale
  within about 75 minutes, by a concurrent, unrelated effort — not something anyone should have
  caught earlier, but a clean example of why the render-path pulse re-verified rather than
  trusted a dated claim.
